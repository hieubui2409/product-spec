# Showcase SAMPLE telemetry data (not real usage) — feeds the telemetry hero example.
# Regenerate outputs: CK_TELEMETRY_DIR=$PWD python3 <telemetry>/analyze_telemetry.py --lens usage --format <f> --lang vi
