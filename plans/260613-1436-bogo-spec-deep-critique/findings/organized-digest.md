# ALL REAL FINDINGS — organized digest (171)


## BLOCKER (10)

### [BLOCKER] (market) PRD-AI — Điểm khác biệt AI tiếng Việt đang bị đối thủ lấp đầy trước khi BoGo ra mắt
- 📍 PRD-AI:36
- ⚠️ Spec tuyên bố trợ lý AI tiếng Việt gắn dữ liệu vận hành là khác biệt so với Base/1Office vì 'chưa có'. Tuy nhiên Base.vn đã công bố 'Hey Base AI' (Coming Soon, hiển thị tiếng Việt, kéo từ HR/task/comms) và 1Office đã có '1AI' với nút 'ĐĂNG KÝ NGAY' đang hoạt động. Tanca — đối thủ HR sát nhất — đã có 6 AI agent bao gồm HR Chatbot đang chạy thật. BoGo sắp ra mắt vào thị trường mà luận điểm 'AI tiếng Việt chỉ có BoGo' không còn đứng được. Nếu không có lớp khác biệt thứ hai, AI trở thành tính năng bắt buộc phải có (table stakes) thay vì moat.
- 🔧 Cần bổ sung một câu đặc tả CỤ THỂ vào PRD-AI về điều BoGo làm mà Base AI/1AI/Tanca không làm được: ví dụ AI gắn đồng thời dữ liệu HR + TASK + COMM của chính tổ chức trong một phiên hỏi-đáp duy nhất, phân quyền đến từng cấp phòng ban theo sơ đồ tổ chức thật — không phải AI cấp nền tảng generic. Nếu điểm đó thực sự là moat, viết thành AC kiểm chứng được. Nếu không, hạ kỳ vọng trong BRD-G3 xuống 'AI là tính năng giữ chân, không phải lý do mua đầu tiên'.

### [BLOCKER] (product) PRD-PLATFORM-E1-S1 — PRD-PLATFORM giải quyết thanh toán nhưng vẫn bỏ trống màn hình dựng phòng ban — GAP-ORG chưa đóng
- 📍 PRD-PLATFORM-E1-S1:26
- ⚠️ VISION mô tả kịch bản thành công của ORG_ADMIN là 'buổi sáng đầu tiên dựng xong sơ đồ 5 phòng ban, nhập 40 nhân viên từ file, gán trưởng phòng' (VISION:64). PRD-PLATFORM-E1-S1 chỉ tạo workspace và bàn giao cho AI-E2-S5 qua 'checklist setup tĩnh' — nhưng AI-E2-S5 (moscow:should) dẫn người dùng đến bước 'dựng sơ đồ phòng ban → thêm nhân viên' mà không có story nào build màn hình đó. Hơn nữa, điều kiện gia hạn trial tại E1-S1:27 đòi hỏi ≥60% nhân viên đã chấm công trong 7 ngày đầu — nhưng nhân viên không thể chấm công nếu phòng ban chưa được dựng và tài khoản chưa được tạo. Toàn bộ cơ chế kích hoạt (BRD-G3) dựa vào một bước setup mà không có story nào sở hữu. Đây là leo thang của GAP-ORG đã biết: PRD-PLATFORM lấp được mảng thương mại hoá nhưng vẫn bỏ trống mảng tự-cấu hình tổ chức.
- 🔧 Thêm ít nhất 2 story vào PRD-PLATFORM-E1 hoặc tạo epic mới: (1) ORG_ADMIN tạo/sửa/xoá phòng ban nhiều cấp, gán trưởng phòng; (2) ORG_ADMIN nhập hàng loạt nhân viên từ file (Excel/CSV). Hai story này là điều kiện tiên quyết cho bộ đếm gia hạn trial và cho AI-E2-S5 hoạt động được. Nếu không làm trước go-live, cần hạ AI-E2-S5 xuống wont và chấp nhận rủi ro tỉ lệ kích hoạt thấp hơn 70% mục tiêu.  _(known/escalated)_

### [BLOCKER] (product) PRD-PLATFORM-E1-S1 — Không có story nào cho tổ chức CHỦ ĐỘNG rời đi: huỷ gói, xuất dữ liệu, chuyển quyền ORG_ADMIN
- 📍 PRD-PLATFORM-E1-S1:30
- ⚠️ VISION hứa 'tự phục vụ' nhưng vòng đời hiện tại chỉ xử lý trường hợp khách KHÔNG trả tiền (dunning). Không có luồng để ORG_ADMIN tự đóng tài khoản, không có cơ chế xuất toàn bộ dữ liệu (bảng lương, chấm công, công việc) ra định dạng mở trước khi dữ liệu bị 'xử lý theo chính sách lưu trữ'. SME sợ nhất là bị khoá dữ liệu — đây là rào cản mua hàng trực tiếp. Đặc biệt: không có story chuyển quyền ORG_ADMIN sang người khác, khiến tổ chức bị kẹt nếu người phụ trách nghỉ việc — đúng vấn đề cốt lõi số 3 của VISION ('nhân viên nghỉ là mất dữ liệu') mà BoGo tự lặp lại với chính khách hàng của mình.
- 🔧 Thêm ba AC vào PRD-PLATFORM: (1) ORG_ADMIN tự đóng gói và nhận link xuất toàn bộ dữ liệu tổ chức ra CSV/Excel trong 24 giờ trước khi xoá; (2) ORG_ADMIN chuyển quyền admin sang thành viên khác trong tổ chức (PLATFORM_ADMIN không can thiệp); (3) ghi cam kết data portability vào điều khoản dịch vụ trong hợp đồng điện tử (PRD-PLATFORM-E3-S2). Đây là 'anti-lock-in promise' — điểm bán chống lại nỗi sợ bị khoá dữ liệu của SME.

### [BLOCKER] (spine) PRD-AI-E2-S5 — Đường kích hoạt tổ chức (BRD-G3, ≥70%) phụ thuộc vào một story 'should' có thể bị cắt
- 📍 PRD-AI-E2-S5:10
- ⚠️ BRD-G3 đặt mục tiêu ≥70% tổ chức đăng ký kích hoạt thành công trong 30 ngày (BRD:129). Trợ lý onboarding (PRD-AI-E2-S5) là cơ chế dẫn dắt ORG_ADMIN qua checklist setup (dựng phòng ban → thêm nhân viên → gán trưởng phòng → cấu hình chấm công). Đây là đòn bẩy trực tiếp của BRD-G3 và VISION cam kết 'tự phục vụ' (VISION:33). Nhưng story này được gán moscow: should — có thể bị cắt khi sprint quá tải. Nếu cắt, tổ chức mới đăng nhập lần đầu chỉ thấy 'checklist tĩnh' (fallback được ghi nhận trong PLATFORM-E1-S1:26) — không có hướng dẫn thông minh, ORG_ADMIN phải tự mò. Với 33 must stories trong horizon now và 37 must tổng cộng, áp lực cắt should là rất cao. North Star 100 tổ chức/12 tháng không thể đạt nếu activation rate thấp.
- 🔧 Nâng PRD-AI-E2-S5 từ 'should' lên 'must'. Nếu lo ngại về khối lượng build của AI, tách thành hai phần: (a) must — checklist tĩnh tự cập nhật theo dữ liệu thật (AC đơn giản hơn, không cần LLM); (b) should — trợ lý trả lời câu hỏi setup bằng LLM. Phần (a) đủ để đảm bảo activation flow hoạt động ngay cả khi AI chưa sẵn sàng.

### [BLOCKER] (spine) PRD-PLATFORM — Không có story nào xây màn hình quản lý phòng ban — xương sống của toàn hệ thống bị trống
- 📍 VISION:62
- ⚠️ VISION cam kết thứ hai là 'sơ đồ tổ chức là xương sống' (VISION:32). ORG_ADMIN cần tự dựng sơ đồ nhiều phòng ban, nhiều cấp trước khi làm bất cứ điều gì khác (VISION:62). Nhưng trong toàn bộ 80 story, không có story nào build màn hình tạo/sửa/xoá phòng ban. HR-E1-S6 chỉ gán nhân viên vào trường 'phòng ban thuộc về' nhưng không định nghĩa entity phòng ban tồn tại từ đâu. PRD-PLATFORM gồm 8 story đều là đăng ký, thanh toán, hoá đơn — không có story nào cho quản trị cơ cấu tổ chức. DEC-50 (quyết định khắc phục) đã hứa rõ 'sơ đồ phòng ban, tài khoản, phân quyền' nhưng khi triển khai chỉ xây được vòng đời thuê bao. Tất cả nhóm chat hệ thống (COMM-E1-S2), giao việc có scope phòng (TASK-E1-S2), báo cáo theo phòng (TASK-E4-S1), luồng duyệt đa cấp (APPROVAL-E1-S2) — tất cả đều giả định phòng ban đã tồn tại. Đây là phụ thuộc nền tảng bị bỏ qua: nếu không xây màn hình này, mọi phân hệ kia đều không thể khởi động.
- 🔧 Thêm một story mới vào PRD-HR hoặc tạo epic 'Quản trị cơ cấu tổ chức' (moscow: must, now): AC tối thiểu gồm ORG_ADMIN tạo/sửa/xoá phòng ban (nhiều cấp, không giới hạn số phòng hợp lý cho SME 200 người), gán vai trò trưởng phòng, nhập tài khoản nhân viên hàng loạt từ file Excel — đây chính là 'buổi sáng đầu tiên' trong kịch bản thành công của ORG_ADMIN (VISION:64). Gán vào PRD-PLATFORM hoặc tách epic riêng, nhưng phải là must và phải go-live trước mọi phân hệ khác.  _(known/escalated)_

### [BLOCKER] (spine) PRD-PLATFORM-E1-S1 — Không có story nào cho phép tổ chức chủ động rời BoGo và lấy lại dữ liệu của mình
- 📍 PRD-PLATFORM-E1-S1:29
- ⚠️ Spec hiện tại chỉ mô tả vòng đời thụ động: hết hạn → khoá ghi → tạm ngưng → 'xử lý theo chính sách lưu trữ'. Không có luồng nào cho ORG_ADMIN tự đóng gói, xuất toàn bộ dữ liệu (bảng lương, chấm công, tin nhắn, tài liệu) trước khi rời, hoặc chuyển quyền ORG_ADMIN khi người phụ trách nghỉ việc. SME mua phần mềm quản lý bảng lương và chấm công — không có cam kết lấy dữ liệu ra là rào cản mua thật sự và là đối nghịch trực tiếp với lời hứa của VISION ('tự phục vụ'). Khi người ORG_ADMIN gốc nghỉ việc mà không thể chuyển quyền, tổ chức kẹt với hệ thống không ai điều hành được — đúng nỗi đau VISION nói mình giải quyết nhưng BoGo tự tạo ra.
- 🔧 Thêm ít nhất một story vào PRD-PLATFORM-E1 cho ORG_ADMIN: (1) tự đóng gói / rời dịch vụ có xác nhận kèm AC xuất toàn bộ dữ liệu ra định dạng mở (CSV/Excel) trước khi khoá; (2) chuyển quyền ORG_ADMIN cho tài khoản khác trong tổ chức. Ghi cam kết data portability vào trang đăng ký / hợp đồng điện tử (PRD-PLATFORM-E3-S2) như điều khoản chống lock-in.

### [BLOCKER] (tech) PRD-COMM-E1-S1 — Hợp đồng ERP-link có entity_type 'order→SALES' nhưng PRD-SALES đã bị loại khỏi sản phẩm
- 📍 PRD-COMM-E1-S1:53
- ⚠️ Hợp đồng ERP-link ở S1 liệt kê entity_type ban đầu gồm order|shift|task, trong đó order gọi về PRD-SALES. Nhưng DEC-45 đã loại SALES khỏi BoGo và không có PRD-SALES nào tồn tại. HR-E1-S3 và TASK-E1-S1 đã có AC expose endpoint tương ứng, nhưng không có ai sở hữu endpoint xác thực quyền cho order. Kết quả: khi nhân viên gắn link đơn hàng vào tin nhắn, endpoint phân quyền không có — deep-link sẽ thành stub ngay khi build, và tính năng differentiator ERP-native (lý do BoGo xây COMM thay vì dùng Lark) không nghiệm thu được.
- 🔧 Gỡ 'order' ra khỏi danh sách entity_type ban đầu trong hợp đồng ERP-link ở S1 cho đến khi có PRD sở hữu đơn hàng. Danh sách khởi động thực tế chỉ là shift|task. Cập nhật ghi chú thiết kế S1 và thêm AC tường minh: 'hệ thống chỉ cho phép gắn entity_type trong danh sách đã có endpoint xác thực; các loại chưa có nhà sở hữu thì ẩn khỏi picker gắn link'.

### [BLOCKER] (tech) PRD-PLATFORM-E1-S1 — Chính sách xóa dữ liệu khi tổ chức đóng ('theo chính sách lưu trữ') không có story sở hữu — không kiểm thử được
- 📍 PRD-PLATFORM-E1-S1:30
- ⚠️ PRD-PLATFORM-E1-S1 ghi 'dữ liệu giữ thêm 60 ngày trước khi xử lý theo chính sách lưu trữ' nhưng không có story nào định nghĩa chính sách đó làm gì thực tế. Dữ liệu tổ chức chứa vector sinh trắc (NĐ13/2023), hồ sơ nhân viên, bảng lương — nếu pipeline xóa không được spec, đội build sẽ không biết cần xóa gì, theo thứ tự nào, và phải xác nhận gì với ORG_ADMIN. Không có AC kiểm thử nghĩa là không có DoD cho việc xóa dữ liệu — vi phạm quyền người dùng theo NĐ13 không có cửa thoát.
- 🔧 Thêm AC vào PRD-PLATFORM-E1-S1 hoặc tạo story riêng mô tả pipeline offboarding: (1) 60 ngày sau tạm ngưng → gửi email cảnh báo xóa cuối cùng tới ORG_ADMIN; (2) sau mốc xóa: xóa vector sinh trắc ngay theo NĐ13, xóa nội dung tin nhắn/tài liệu, ẩn danh hoặc xóa dữ liệu lương (trừ metadata audit 5 năm đã chốt tại DEC-3); (3) log xác nhận từng loại dữ liệu đã xóa. Định nghĩa 'chính sách lưu trữ' bằng AC cụ thể, không để trong ghi chú thiết kế.

### [BLOCKER] (tech) PRD-PLATFORM-E2-S3 — Không có AC cho trường hợp hoàn tiền/chargeback ngân hàng sau khi gạch nợ đã chạy
- 📍 PRD-PLATFORM-E2-S3:28
- ⚠️ E2-S3 chỉ bao phủ: thanh toán thành công, webhook không đến, giao dịch không khớp. Không có AC nào mô tả hành vi khi ngân hàng/cổng thanh toán phát sinh chargeback hoặc reversal SAU khi gạch nợ đã thành công và gói đã kích hoạt. Trong thực tế SaaS VN, chuyển khoản nhầm bị ngân hàng thu hồi là tình huống phổ biến. Hệ thống hiện tại không biết phải làm gì: hạ gói lại không có AC, hoàn HĐĐT không có AC, thông báo ORG_ADMIN không có AC — đội build sẽ xử lý ad-hoc dẫn đến mất doanh thu hoặc tổ chức bị hạ gói đột ngột không báo.
- 🔧 Thêm AC: 'Nếu cổng thanh toán hoặc dịch vụ đối soát thông báo giao dịch bị hoàn/reversal sau khi đã gạch nợ → hệ thống gắn cờ đơn vào hàng đợi xử lý tay của PLATFORM_ADMIN kèm thông tin chi tiết; KHÔNG tự động hạ gói hoặc khoá tổ chức cho đến khi PLATFORM_ADMIN xác nhận; ORG_ADMIN nhận thông báo rằng giao dịch đang được xem xét; mọi sự kiện hoàn tiền ghi log audit.' Trường hợp hoàn tiền chủ động (BoGo hoàn cho khách) cũng cần AC riêng xác nhận pipeline tắt HĐĐT tương ứng với MISA.

### [BLOCKER] (tech) PRD-TASK-E1-S2 — Tính năng "moat" HAI CHIỀU task↔ca làm chỉ được mô tả ở một phía
- 📍 PRD-TASK-E1-S2:26
- ⚠️ AC dòng 26 của E1-S2 tuyên bố "ca làm phản ánh NGƯỢC danh sách công việc gắn + tỉ lệ hoàn thành" khi task Hoàn thành — nhưng PRD-HR-E1-S3 (story sở hữu ca làm) không có bất kỳ AC nào nhận sự kiện này từ TASK. Tức là phần "chiều về" không có điểm nghiệm thu ở phía HR. Đây là tính năng được gọi là lock-in thật của BoGo trong PRD-TASK (DEC-29) — nếu phía HR không build phần nhận/hiển thị, moat này không tồn tại trong sản phẩm thật dù spec TASK nói đúng.
- 🔧 Thêm một AC vào PRD-HR-E1-S3 (hoặc tạo AC trong một story HR phù hợp) với nội dung có thể kiểm thử: "Khi công việc gắn vào ca làm chuyển trạng thái Hoàn thành, màn hình chi tiết ca đó hiển thị danh sách công việc gắn kèm trạng thái và tỉ lệ hoàn thành checklist tương ứng." Phải có story sở hữu AC này ở phía HR — hiện không có.


## HIGH (67)

### [HIGH] (craft) PRD-APPROVAL-E1-S5 — Lỗi đánh máy 'AI duyệt' trong AC kanban — đội build sẽ hiểu sai
- 📍 PRD-APPROVAL-E1-S5:26
- ⚠️ Trong mô tả thẻ kanban, dòng 'đang chờ AI duyệt (cấp mấy)' dùng 'AI' viết hoa — người đọc (và công cụ sinh code) có thể hiểu đây là hệ thống trí tuệ nhân tạo đang duyệt tờ trình, trong khi toàn bộ phân hệ APPROVAL khẳng định chỉ người ký. Nhầm lẫn này khiến đội build có thể bỏ qua yêu cầu hiển thị 'ai đang duyệt (tên người, cấp mấy)' — trái ngược AC toàn vẹn phê duyệt ở S3.
- 🔧 Thay 'đang chờ AI duyệt (cấp mấy)' bằng 'đang chờ ai duyệt (tên người, cấp mấy)' — chữ thường, có thêm 'tên người' để rõ nghĩa hiển thị.

### [HIGH] (craft) PRD-REPORT — PRD-REPORT và toàn bộ epic/story con vẫn mang tên thương hiệu Cleanmatic và persona/phân hệ không tồn tại trong BoGo
- 📍 PRD-REPORT:8
- ⚠️ PRD-REPORT có `status: approved`, vì vậy bất kỳ ai đọc spec BoGo đều sẽ thấy `approved_by: 'Nhóm sản phẩm Cleanmatic'`, `owner: Nhóm sản phẩm Cleanmatic` (lines 8, 14), cùng với persona `ZONE_MGR`, `SYS_ADMIN`, `hub trưởng` và phân hệ `FINANCE`, `SALES`, `INVENTORY`, `PAYMENT`, `CARE` — không cái nào tồn tại trong BoGo. Điều này xung đột trực tiếp với nội dung pivot DEC-45. Đội build mới đọc spec có thể nhầm tưởng đây là yêu cầu còn hiệu lực của BoGo. Toàn bộ 9 file trong cụm REPORT (1 PRD + 3 epic + 5 story) bị ảnh hưởng.
- 🔧 Thêm ghi chú đầu file rõ ràng vào mỗi file REPORT: `> ⛔ TÀI LIỆU LỊCH SỬ — Cleanmatic. Không áp dụng cho BoGo.` Cập nhật `approved_by` và `owner` thành `(Lưu trữ — Cleanmatic)` để tách khỏi chuỗi phê duyệt BoGo. Hoặc di chuyển toàn bộ cụm sang thư mục `archive/cleanmatic/` và cập nhật một dòng chú thích trong `PRODUCT.md` trỏ đến đó.

### [HIGH] (craft) PRD-REPORT-E1-S1 — AC của PRD-REPORT-E1-S1 đặt tên persona Cleanmatic (ZONE_MGR, SYS_ADMIN) và phân hệ không có trong BoGo (FINANCE, SALES, INVENTORY, PAYMENT, CARE)
- 📍 PRD-REPORT-E1-S1:25
- ⚠️ AC ở dòng 22, 25, 27 tham chiếu tới `ZONE_MGR`, `SYS_ADMIN`, `FINANCE`, `SALES`, `INVENTORY`, `PAYMENT`, `CARE` — tất cả là khái niệm của Cleanmatic ERP đã loại bỏ theo DEC-45. File có `moscow: wont` nhưng `status: approved` và không có cảnh báo đủ rõ. Đội QA hoặc người đọc mới không thể phân biệt đây là lịch sử hay yêu cầu thật của BoGo khi nhìn vào nội dung AC.
- 🔧 Thêm ghi chú `> ⚠️ LỊCH SỬ CLEANMATIC — AC bên dưới không áp dụng cho BoGo` ngay trước phần acceptance_criteria. Không cần sửa nội dung AC (giữ để tra cứu lịch sử quyết định).

### [HIGH] (market) BRD — Tanca ra mắt gói miễn phí vĩnh viễn — so sánh đối thủ trong BRD đã lỗi thời
- 📍 BRD:147
- ⚠️ BRD ghi nhận Tanca là 'threat: high' nhưng định vị BoGo như thể Tanca chỉ cạnh tranh trên tính năng chấm công/lương. Thực tế, Tanca hiện có gói Starter miễn phí vô thời hạn với nhân viên không giới hạn, chấm công cơ bản, eContract và app iOS/Android — tức là phân khúc SME nhỏ nhất (trigger 'lương sai lần 2–3') có thể chọn Tanca miễn phí thay vì trả 89k/user/tháng cho BoGo. Nếu BoGo không có phương án đối phó, tệp khách nhỏ nhất — dễ kéo nhất — sẽ mất trước khi bắt đầu.
- 🔧 Bổ sung vào BRD §Bối cảnh thị trường: ghi nhận gói free của Tanca và giải thích rõ BoGo nhắm vào phân khúc nào không bị gói free đó phủ (ví dụ: tổ chức ≥20 nhân viên cần hợp nhất HR + giao việc + giao tiếp, không chỉ chấm công). Xem xét thêm gói free/freemium giới hạn người dùng hoặc tính năng để cạnh tranh ở điểm gia nhập.

### [HIGH] (market) BRD — Ba kênh GTM đều chưa có người chịu trách nhiệm, không ngân sách, không mốc thời gian
- 📍 BRD:183
- ⚠️ BRD §Go-to-Market ghi rõ '(đề xuất — PO rà soát, gán owner khi vận hành)'. Cả ba kênh (referral từ pilot, đối tác đại lý, tự phục vụ) đều để trống owner. Mục tiêu G1 là 100 tổ chức trong 12 tháng — nhưng không ai được giao nhiệm vụ đưa khách đến. Không có kênh nào hoạt động thì trial tốt đến đâu cũng không có người thử.
- 🔧 Trước khi ra mắt thương mại, mỗi kênh GTM phải có: (1) tên người/nhóm chịu trách nhiệm, (2) ngân sách ước tính hoặc cam kết thời gian, (3) KPI đầu tiên và mốc kiểm tra (ví dụ: sau 60 ngày). Ít nhất một kênh phải chắc chắn trước go-live — không thể để cả ba là 'đề xuất'.

### [HIGH] (market) BRD — AI là điểm bán chính nhưng kinh tế AI chưa được đo — bán trước, kiểm sau là rủi ro mô hình doanh thu
- 📍 BRD:179
- ⚠️ BRD §Mô hình doanh thu ghi: 'LLM cost envelope THẬT đo trong 30 ngày pilot đầu — điều kiện giữ AI trong gói chuẩn.' Tức là nếu chi phí LLM vượt 15% doanh thu, BoGo buộc phải hoặc nâng giá hoặc cắt AI khỏi gói chuẩn — đúng lúc đang mở rộng khách hàng. PRD-AI là phân hệ được gán cho G3 (kích hoạt + giữ chân), nếu phải cắt hoặc chuyển AI thành add-on trả thêm, toàn bộ luận điểm 'AI đồng hành hợp nhất' sụp đổ.
- 🔧 Trước pilot chính thức, chạy một bài đo chi phí LLM nhỏ (mock hoặc internal sandbox): số token trung bình/câu hỏi × giá API hiện tại × ước lượng query/user/tháng. Lấy số đó so với ngưỡng 15% (89k × 15% = ~13k VND/user/tháng). Nếu con số vượt ngưỡng ngay cả ở mức sử dụng thấp, cần chốt trần quota AI hoặc điều chỉnh giá TRƯỚC khi bán — không phải sau.

### [HIGH] (market) BRD — Trần chi phí LLM 13k/user/tháng chưa có con số thật — nếu sai thì margin âm hoặc AI bị cắt khỏi gói
- 📍 BRD:179
- ⚠️ BRD neo cứng giá 89k/user/tháng và đặt trần LLM ≤ 13k (15% doanh thu). Con số này là 'đề xuất — pilot điền số thật'. Nhưng nếu thói quen dùng thực tế (câu hỏi/user/ngày × chi phí token) phá trần này, BoGo có hai lựa chọn đều tệ: (1) cắt AI khỏi gói chuẩn và mất luận điểm khác biệt, hoặc (2) giữ AI trong gói và margin thấp dưới 60% mục tiêu. Với mô hình SME đại trà, nhân viên hay hỏi trợ lý theo kiểu tán gẫu, không phải câu hỏi ngắn gọn — chi phí LLM thực tế thường gấp 2-3x ước tính ban đầu khi scale.
- 🔧 Trước khi lock giá 89k cho pilot trả phí đầu tiên: chạy 1 tuần shadow mode trên tổ chức nội bộ, đếm thực tế số query/user/ngày × token/query × giá token (GPT-4o-mini hoặc provider đã chọn), so với ngưỡng 13k. Nếu vượt → hoặc nâng giá, hoặc thêm quota AI (số câu hỏi/user/tháng) trước khi bán thương mại. Spec cần ghi rõ nhà cung cấp LLM và model đã chọn để con số là có thể kiểm chứng được.

### [HIGH] (market) BRD — Năm trigger mua trong BRD không có trigger nào liên quan đến AI — AI không phải lý do tổ chức quyết định mua
- 📍 BRD:192
- ⚠️ Cả 5 trigger mua (BRD dòng 192-197) đều là pain HR/vận hành: lương sai, mở chi nhánh, cần chứng từ, thanh tra, mất hồ sơ. Không có trigger nào là 'muốn trợ lý AI tiếng Việt'. Điều này cho thấy AI không phải lý do tổ chức rút thẻ, mà là tính năng giữ chân sau khi đã mua. Nhưng PRD-AI lại được gán BRD-G3 (kích hoạt & giữ chân) và spec định vị AI như đòn bẩy kích hoạt chủ đạo ('điểm khác biệt', 'đòn bẩy kích hoạt tổ chức mới'). Nếu AI không nằm trong JTBD mua, đội sales/GTM sẽ dùng nó như tính năng bán thêm nhưng spec lại đặt kỳ vọng activation rate 70% một phần vào đó.
- 🔧 Tách rõ trong BRD: AI là 'lý do ở lại' (retention hook) hay 'lý do mua' (acquisition hook)? Nếu là retention: thì BRD-G3 metric nên đo tỉ lệ tổ chức có dùng AI ≥ 1 lần/tuần sau tháng đầu, không phải gán toàn bộ activation rate vào AI. Nếu PO tin AI là acquisition hook thì cần thêm ít nhất 1 trigger mua cụ thể ('không biết nhân viên đang làm gì / hỏi sếp liên tục') vào BRD.

### [HIGH] (market) BRD — Toàn bộ GTM ghi 'đề xuất — gán owner khi vận hành': không có ai chịu trách nhiệm đạt G1 (100 tổ chức/12 tháng)
- 📍 BRD:183
- ⚠️ G1 đặt mốc 100 tổ chức hoạt động tích cực sau 12 tháng. Đường đến G1 đi qua GTM (referral, đối tác, self-serve). Nhưng bảng GTM ghi chú 'PO rà soát, gán owner khi vận hành' — không ai được chỉ định. KPI kênh (30% từ referral, 2 đối tác ký quý đầu, tỉ lệ đăng ký→kích hoạt ≥40%) là số cụ thể nhưng không có người làm chúng. Sản phẩm self-serve tốt không tự bán được nếu không có content/SEO/ads. 100 tổ chức trong 12 tháng mà đội sales/marketing chưa được định hình là mục tiêu treo không có chủ.
- 🔧 BRD cần ghi ít nhất: kênh nào BoGo sẽ làm TRƯỚC (chỉ 1), ngân sách CAC kỳ vọng cho kênh đó, và ai trong team chịu trách nhiệm đo. Nếu hiện tại thực sự chưa có đội GTM, cần hạ mốc G1 xuống mức phù hợp với nguồn lực thực tế (ví dụ 20-30 tổ chức từ pilot referral + founder sales), tránh để mốc 100 làm chuẩn commit nhưng thực ra là nguyện vọng.

### [HIGH] (market) BRD — Tanca ra mô hình mua-một-lần + Starter miễn phí không giới hạn nhân viên — so sánh trong BRD đã lỗi thời về mô hình kinh doanh, không chỉ giá
- 📍 BRD:148
- ⚠️ BRD ghi Tanca là đối thủ 'chấm công + tính lương' và định vị BoGo vượt Tanca nhờ tích hợp + AI. Tuy nhiên Tanca hiện có: (1) gói Starter MIỄN PHÍ VĨNH VIỄN, không giới hạn nhân viên, gồm hồ sơ nhân viên + sơ đồ tổ chức + chấm công cơ bản; (2) gói Core mua-một-lần tiết kiệm '40–50% so với subscription 3 năm'; (3) 6 AI agent tiếng Việt 24/7 (HR Chatbot, Compliance AI, AI Scheduling, Anomaly Detection, Workforce Analytics, CV Screening) và 'Kho thông tin + AI' (thư viện tài liệu tìm kiếm AI). SME Việt Nam đang dùng Tanca miễn phí không phải vì không thể trả tiền — mà vì Tanca đã đủ nhu cầu cốt lõi. BoGo cần thuyết phục khách RỜI HÀNG MIỄN PHÍ để trả 89k/user/tháng subscription; lập luận 'hợp nhất + AI' trong BRD không còn đủ mạnh khi chính Tanca đã có AI và giá mua-một-lần.
- 🔧 Cập nhật phần so sánh Tanca trong BRD: ghi nhận mô hình mua-một-lần và free tier; tái định vị BoGo không phải ở AI (Tanca đã có) hay giá thấp hơn (Tanca free tier thắng) mà ở tích hợp TASK+COMM+AI trong một ứng dụng mà Tanca không có. Bổ sung trigger mua cụ thể: 'khi tổ chức cần cả giao việc, chat nội bộ VÀ HR trên một app — không phải HR riêng lẻ' thay vì so sánh giá thẳng với Tanca.

### [HIGH] (market) BRD — BRD-G3 đặt mục tiêu churn ≤3%/tháng nhưng không có tính năng chủ động giảm churn — toàn bộ phụ thuộc vào SUPPORT phản ứng sau khi tổ chức đã bỏ
- 📍 BRD:130
- ⚠️ BRD-G3 tuyên bố 'monthly-org-churn ≤ 3%/tháng' và liệt kê PRD-SUPPORT là phân hệ giữ chân. Tuy nhiên PRD-SUPPORT chỉ có 3 story: AI tuyến đầu, chat hỗ trợ, và hotline — tất cả đều là REACTIVE (khách phải chủ động liên hệ). Không có tính năng nào để PLATFORM_ADMIN hoặc hệ thống chủ động phát hiện tổ chức đang giảm hoạt động (dấu hiệu sắp churn: tuần thứ 3 chỉ còn 30% nhân viên active, không còn giao việc mới). Tanca quảng cáo 'Anomaly Detection' phát hiện ngưỡng bất thường 24/7. BoGo không có cơ chế can thiệp trước khi tổ chức quyết định không gia hạn.
- 🔧 Thêm vào PLATFORM_ADMIN dashboard (PRD-PLATFORM-E1) tối thiểu: (1) danh sách tổ chức có dấu hiệu giảm hoạt động (% nhân viên active/tuần giảm >20% so tuần trước) với nhãn 'at-risk'; (2) alert tự động khi tổ chức trial qua ngày 10 mà activation <30%. Đây là công cụ bán hàng kiêm chống churn có chi phí build thấp nhưng giá trị cao với đội vận hành BoGo, phù hợp đặt vào horizon now hoặc ghi là fast-follow.

### [HIGH] (market) PRD-AI — Tuyên bố 'AI tiếng Việt gắn dữ liệu vận hành' không còn độc quyền — Tanca đã có 6 AI agent tiếng Việt
- 📍 PRD-AI:36
- ⚠️ PRD-AI dùng luận điểm 'Base/1Office chưa có trợ lý AI tiếng Việt gắn dữ liệu vận hành' làm lý do khác biệt hoá và đòn bẩy kích hoạt G3. Thực tế hiện tại: Tanca đã triển khai HR Chatbot 24/7 tiếng Việt kèm Compliance AI, Anomaly Detection, Workforce Analytics trên dữ liệu vận hành của chính họ. Nếu khách hàng so sánh trực tiếp, lợi thế 'AI tiếng Việt' của BoGo nghe như tính năng ngang bằng, không phải đột phá.
- 🔧 Làm rõ lại điểm khác biệt AI thực sự của BoGo: không phải 'có AI tiếng Việt' (Tanca cũng có) mà là 'AI trả lời trên TÀI LIỆU NỘI BỘ của chính tổ chức đó + dữ liệu HR/TASK trên cùng một nền tảng hợp nhất.' Cập nhật BRD §Bối cảnh thị trường và PRD-AI §Phạm vi để so sánh chính xác hơn với Tanca AI, tránh dùng luận điểm đã bị đối thủ vô hiệu.

### [HIGH] (market) PRD-AI-E2 — Giá trị AI phụ thuộc kho tri thức tổ chức tải lên — nhưng spec không có cơ chế buộc tổ chức làm điều này
- 📍 PRD-AI-E2:50
- ⚠️ S1 (hỏi đáp quy chế) và S4 (đào tạo) chỉ hoạt động khi tổ chức đã tải tài liệu vào kho. S5 (onboarding) không có bước nào yêu cầu tải tài liệu. SME Việt Nam thường không có quy chế dạng file có cấu trúc — chính sách nằm trong đầu người quản lý hoặc nhóm Zalo. Nếu kho rỗng lúc go-live, trợ lý AI chỉ làm được S2 (hỏi phép/công) và S3 (danh sách việc) — tức là tính năng tiện lợi nhỏ, không phải 'trợ lý AI' như định vị. Kết quả: tổ chức thử xong thấy AI 'biết ít quá', không kích hoạt thành công → ảnh hưởng thẳng vào BRD-G3.
- 🔧 Thêm vào S5 (onboarding) một bước bắt buộc trong checklist: tải lên ít nhất 1 tài liệu quy chế trước khi hoàn thành onboarding (AC kiểm chứng: tổ chức chưa tải tài liệu → checklist chưa 100%). Cân nhắc cung cấp template quy chế mẫu (nội quy làm việc từ xa, chính sách phép) để admin không cần soạn từ đầu — giảm rào cản đầu tiên.

### [HIGH] (market) PRD-AI-E2 — Rủi ro NĐ13 về PII gửi qua API LLM bên thứ ba được 'chấp nhận tạm' nhưng không có ngưỡng quy mô cụ thể để bắt buộc xử lý
- 📍 PRD-AI-E2:43
- ⚠️ Spec ghi 'BẮT BUỘC rà soát luật sư + cân nhắc che PII / DPA / self-host trước khi mở rộng quy mô'. Nhưng 'mở rộng quy mô' là khi nào — 10 tổ chức? 50? Không có ngưỡng cụ thể. Nếu BoGo đạt được G1 (100 tổ chức, ~3000 người dùng) mà vẫn gửi dữ liệu nhân sự thô qua LLM API bên thứ ba, đây là rủi ro pháp lý nghiêm trọng theo NĐ13/2023. Các khách hàng SME là doanh nghiệp thật với nhân viên thật, có quyền khiếu nại. Mối lo không phải là pilot 5 tổ chức — mà là BoGo thành công nhưng không chuẩn bị điểm dừng pháp lý.
- 🔧 Thêm vào BRD hoặc PRD-AI một ngưỡng cứng: ví dụ 'khi đạt 30 tổ chức trả phí hoặc trước vòng gọi vốn đầu tiên — phải có ý kiến luật sư + chính sách che PII tối thiểu (anonymize tên/SĐT trước khi gửi LLM) đã kiểm tra và được PO phê duyệt'. Chốt deadline, không để open-ended.

### [HIGH] (market) PRD-PLATFORM — Không có story xuất dữ liệu / offboarding tổ chức — SME Việt Nam lo khóa vendor khi quyết định mua SaaS
- 📍 PRD-PLATFORM-E1-S1:30
- ⚠️ AC duy nhất đề cập dữ liệu khi rời là 'giữ 60 ngày trước khi xử lý theo chính sách lưu trữ' — tổ chức không có cách nào tự xuất toàn bộ dữ liệu của mình (nhân viên, bảng công, lịch sử lương, công việc, tin nhắn) trước khi đóng tài khoản. Đây là rào cản mua SaaS phổ biến nhất ở SME Việt Nam: 'nếu tôi bỏ thì dữ liệu đi đâu?'. Không có câu trả lời trong spec = đội bán hàng không thể phản bác objection này; cũng vi phạm tinh thần NĐ 13/2023 về quyền truy cập và di chuyển dữ liệu cá nhân.
- 🔧 Thêm story PRD-PLATFORM (hoặc story con trong E1) cho phép ORG_ADMIN xuất toàn bộ dữ liệu tổ chức (nhân viên + bảng công + bảng lương + danh sách công việc) dưới dạng file Excel/CSV trong giai đoạn khoá-ghi hoặc theo yêu cầu bất kỳ lúc nào. Ghi rõ trong trang sản phẩm/landing page như một cam kết chống vendor lock-in — chuyển thành lợi thế bán hàng thay vì rủi ro.

### [HIGH] (product) PRD-AI-E2 — BRD đặt ràng buộc cứng chi phí LLM ≤15% doanh thu — nhưng không có AC nào trong E2 đo hoặc enforce điều này
- 📍 PRD-AI-E2:48
- ⚠️ BRD (dòng 180) quy định chi phí LLM per-tổ-chức phải ≤15% doanh thu gói — nếu vượt thì phải xét quota hoặc gói AI riêng. E2 epic risk mitigation hứa 'theo dõi chi phí per-tổ-chức (S6 dashboard)' nhưng S6 không có AC nào về theo dõi chi phí. Không có cơ chế đo, không có ngưỡng cảnh báo, không có circuit breaker. Khi pilot chạy với vài tổ chức dùng nhiều, đội sẽ không biết mình đang lỗ hay lãi trên từng org cho đến khi nhận hoá đơn LLM cuối tháng. Đây là rủi ro kinh tế trực tiếp ảnh hưởng tới gross margin mục tiêu ≥60%.
- 🔧 Thêm vào S6 ít nhất 2 AC: (1) PLATFORM_ADMIN xem được chi phí LLM ước tính per-tổ-chức theo tuần/tháng; (2) Khi chi phí LLM của một tổ chức vượt ngưỡng cảnh báo (PO định nghĩa — ví dụ 12k/user/tháng), hệ thống tự động gửi cảnh báo cho PLATFORM_ADMIN. Đây là điều kiện để giữ AI trong gói chuẩn như BRD yêu cầu.

### [HIGH] (product) PRD-AI-E2-S3 — EXEC — persona có pain rõ ràng trong VISION — không được phục vụ bởi bất kỳ story AI nào trong MVP
- 📍 PRD-AI-E2-S3:45
- ⚠️ VISION mô tả EXEC cần 'bức tranh toàn cảnh theo thời gian thực' và coi đây là vấn đề cốt lõi số 4 (lãnh đạo thiếu dữ liệu ra quyết định). BRD định vị trợ lý AI là điểm khác biệt cạnh tranh so với Base/1Office. Tuy nhiên PRD-AI, E2 và tất cả 6 story chỉ phục vụ STAFF, DEPT_MGR, ORG_ADMIN, PLATFORM_ADMIN — EXEC hoàn toàn vắng mặt. Design note của S3 thừa nhận EXEC là người dùng tiếp theo nhưng ghi 'giai đoạn sau'. Nếu EXEC không thấy giá trị từ AI ở MVP, nhóm ra quyết định (người ký hợp đồng thuê bao) sẽ không có lý do đủ mạnh để trả 89k/user/tháng.
- 🔧 Thêm một AC cho S3: trợ lý cho phép EXEC hỏi 'tóm tắt tình hình tổ chức hôm nay' và nhận bản tóm tắt cấp tổ chức (% đi làm, số việc trễ, phòng nào quá tải) — đúng pain trong VISION. Không cần tạo story mới, chỉ mở rộng S3 thêm EXEC vào danh sách personas và thêm một AC cụ thể.

### [HIGH] (product) PRD-AI-E2-S4 — Tính năng đào tạo & bài kiểm tra không có nguồn gốc từ pain point trong BRD/VISION
- 📍 PRD-AI-E2-S4:44
- ⚠️ VISION liệt kê 4 vấn đề cốt lõi của SME; BRD có 5 trigger mua hàng — không có điểm nào đề cập đào tạo nội bộ hay thi trắc nghiệm. Story này được thêm vào theo đề xuất của AI trong buổi phỏng vấn PO (không phải từ khảo sát người dùng). Nếu ship trong MVP (horizon: now, size: L), team tốn 1–2 sprint cho một tính năng mà khách hàng chưa hỏi tới, trong khi G2 (giảm 50% thời gian hành chính) và G3 (kích hoạt ≥70% tổ chức) là những mục tiêu chưa được đảm bảo. Kano: đây là delighter — không phải must-be — nhưng đang chiếm slot must trong MVP.
- 🔧 Hạ moscow xuống 'could' và dời sang horizon 'next'. Trước khi đưa trở lại backlog, kiểm chứng bằng câu hỏi cụ thể với ít nhất 3 SME pilot: 'Bạn có đang soạn đề kiểm tra thủ công không? Bạn sẵn sàng trả thêm cho tính năng này không?' Nếu không có bằng chứng từ pilot, giữ 'later'.

### [HIGH] (product) PRD-AI-E2-S5 — S5 hướng dẫn ORG_ADMIN tới các màn hình chưa có story nào build (dựng phòng ban, bulk import nhân viên)
- 📍 PRD-AI-E2-S5:24
- ⚠️ S5 cam kết trợ lý sẽ dẫn ORG_ADMIN từng bước: 'dựng sơ đồ phòng ban → thêm nhân viên → gán trưởng phòng → cấu hình ca làm/chấm công'. §3 đã ghi nhận GAP-ORG (không có story nào build màn hình tạo phòng ban hay import nhân viên hàng loạt). Hậu quả mới và cụ thể hơn: S5 sẽ bị từ chối nghiệm thu vì trợ lý không thể link tới màn hình không tồn tại — hoặc tệ hơn, trợ lý sẽ link tới màn hình HR-E1-S6 (quản hồ sơ từng nhân viên) rồi ORG_ADMIN phải thêm nhân viên thủ công từng người một, trái với kịch bản thành công 'nhập 40 nhân viên từ file' của VISION. Luồng kích hoạt 30 ngày sẽ thất bại trước khi bắt đầu.
- 🔧 Thêm ít nhất 2 story trong PRD-HR hoặc PRD-PLATFORM (horizon: now, moscow: must): (1) ORG_ADMIN tạo và chỉnh sửa cây phòng ban (tên phòng, cấp bậc, gán trưởng phòng); (2) ORG_ADMIN import nhân viên hàng loạt từ file Excel/CSV. Cho đến khi có các story này, S5 không có nền để build. Nếu GAP-ORG không được lấp, nên hạ S5 xuống 'should' và gắn điều kiện tiên quyết tường minh.  _(known/escalated)_

### [HIGH] (product) PRD-AI-E2-S6 — BRD đặt ràng buộc cứng chi phí LLM ≤15% doanh thu/tổ chức nhưng không có cơ chế quota hay throttle per-org trong bất kỳ story AI nào
- 📍 BRD:180
- ⚠️ BRD dòng 180 ghi: 'chi phí LLM mỗi tổ chức phải ≤ 15% doanh thu gói — đo tại pilot; vượt ngưỡng thì xét quota/gói AI riêng'. Story PRD-AI-E2-S6 (cấu hình & kiểm soát) chỉ có kill switch bật/tắt toàn bộ — không có AC nào định nghĩa cơ chế theo dõi lượng query/tổ chức/tháng, không có ngưỡng cảnh báo khi chi phí AI tiệm cận trần, không có throttle khi vượt. Một tổ chức dùng AI nhiều bất thường sẽ âm thầm ăn margin mà không ai biết cho đến khi hóa đơn LLM đến tháng.
- 🔧 Thêm vào PRD-AI-E2-S6 hoặc PRD-PLATFORM: (1) PLATFORM_ADMIN thấy lượng AI query/tổ chức/tháng và chi phí ước tính; (2) cấu hình ngưỡng cảnh báo và throttle per-org (vd sau X query/ngày, trả lời 'hết lượt dùng miễn phí' hoặc yêu cầu nâng gói AI); (3) kill switch S6 mở rộng thành 'giảm tốc/quota' chứ không chỉ on/off. Điều này cũng là công cụ để enforce điều kiện 'xét quota/gói AI riêng' trong BRD.

### [HIGH] (product) PRD-APPROVAL — Trigger mua của APPROVAL chưa được kiểm chứng, nhưng toàn bộ 5 story đã approved và coi là must
- 📍 PRD-APPROVAL:65
- ⚠️ Trigger mua duy nhất của APPROVAL ('thanh tra hỏi chứng từ phê duyệt') được spec tự ghi nhận là 'kiểm ở pilot' — tức là nhu cầu chưa có bằng chứng thực địa. Nếu pilot không xác nhận nhu cầu này, 5 story phức tạp (S1–S5) với hệ thống ký xác thực đa bậc, luồng duyệt, hash tờ trình... sẽ là sản phẩm không ai mua add-on. BRD định giá APPROVAL theo lượng tờ trình/tháng (add-on DEC-60) — nếu SME không thấy đủ đau thì không trả thêm.
- 🔧 Trước khi spec APPROVAL ở trạng thái 'sẵn sàng build', thêm một câu hỏi kiểm chứng cụ thể vào kế hoạch pilot: hỏi trực tiếp 3–5 tổ chức pilot xem họ có bị thanh tra/kiểm toán hỏi về chứng từ phê duyệt trong 12 tháng gần nhất không, và họ giải quyết thế nào. Nếu <2/5 có case thật thì xem xét hạ S3 (ký xác nhận phức tạp) xuống should cho v2 và giữ lại S1+S2 (tạo tờ trình + luồng duyệt) làm lõi.

### [HIGH] (product) PRD-COMM — Không có story nào xây dựng 'cơ chế kéo' Zalo-switch — giả định trung tâm của COMM thiếu bằng chứng thực thi
- 📍 PRD-COMM:95
- ⚠️ PRD-COMM dòng 95-97 đặt 'cơ chế kéo' (bắt buộc giao việc/ca xử lý trong app) là điều kiện để Zalo tự teo, và dòng 96 định nghĩa ngưỡng đo ≥60% nhóm Zalo ngừng dùng sau 8 tuần. Nhưng không có một story nào trong 14 story COMM (hay TASK, HR) spec-ra luồng 'bắt buộc' này — ai bắt buộc, bắt buộc theo cơ chế gì, admin có màn hình nào thiết lập policy không. BRD-G3 (kích hoạt ≥70% org) phụ thuộc COMM tạo thói quen hằng ngày, nhưng COMM không có story nào cho first-use experience, empty-state, hay onboarding check. Nếu không có cơ chế kéo được build, người dùng thấy app chat trống trong khi nhóm Zalo vẫn sống → giả định đổ, metric sai.
- 🔧 Thêm ít nhất một story cho ORG_ADMIN: thiết lập policy 'kênh giao tiếp chính thức' (ví dụ: thông báo nội bộ phải qua COMM, không qua Zalo) và xem báo cáo adoption COMM (% nhân viên đã mở app trong 7 ngày). Nếu không có story này, cơ chế kéo chỉ là nguyên tắc trên giấy.

### [HIGH] (product) PRD-COMM-E1-S7 — S7 (màu + tag tin nhắn) giữ 'must' — tính năng hoàn toàn mới với người dùng Zalo — chặn go-live cả cụm COMM nếu chậm
- 📍 PRD-COMM-E1-S7:10
- ⚠️ DEC-70 ghi nhận PO đã bác đề xuất hạ S7 xuống 'should' và chấp nhận rủi ro 'trễ-S7-trễ-cả-cụm'. Vấn đề sản phẩm chưa được giải quyết: S7 là tính năng hoàn toàn mới (không có trên Zalo) yêu cầu người dùng học cách dùng kho màu + tag — trong khi giả định Zalo-switch (PRD-COMM dòng 95) là rủi ro số một của toàn COMM. Gắn go-live COMM vào một tính năng học-mới trong khi mục tiêu BRD-G3 là kích hoạt ≥70% org trong 30 ngày là mâu thuẫn chiến lược: người dùng đang phải học app mới + học thao tác mới cùng lúc. Nếu pilot cho thấy người dùng không dùng màu/tag, không có cơ chế rút lại vì nó đã là 'must'.
- 🔧 Thêm AC đo tỉ lệ dùng S7 trong pilot (% tin nhắn được gán màu/tag sau 4 tuần); nếu dưới 20% thì đây là gold-plating và nên rút khỏi core scope v1. Ít nhất, thêm điều kiện nghiệm thu S7 phải pass TRƯỚC khi chặn go-live E1, với cơ chế fallback nếu S7 chậm (deploy E1 trừ S7 trước, S7 fast-follow).  _(known/escalated)_

### [HIGH] (product) PRD-COMM-E2 — Broadcast (kênh thông báo chính của quản lý) bị xếp 'should' trong khi các tính năng chat phụ trợ lại là 'must'
- 📍 PRD-COMM-E2:9
- ⚠️ BRD-G2 đo hiệu quả quản trị bằng việc quản lý gửi thông tin vận hành tới đúng người, đúng lúc. Broadcast là công cụ chính để ORG_ADMIN/EXEC/DEPT_MGR làm việc đó. Thế nhưng E2 (broadcast + ghim) được đánh 'should' trong khi E1-S7 (gán màu + tag tin nhắn — tính năng 'vượt Zalo' mà nhân viên chưa từng dùng) lại là 'must'. Nếu sprint bị áp lực thời gian và broadcast bị cắt, quản lý không có kênh thông báo chính thức → COMM chỉ còn là Zalo-clone không phân quyền → không giữ được giả định Zalo-switch và không đo được metric BRD-G2.
- 🔧 Nâng PRD-COMM-E2-S1 (broadcast có phân quyền) lên 'must'. Đây là tính năng cốt lõi của vai ORG_ADMIN/DEPT_MGR/EXEC trong COMM — không có broadcast thì các vai này không có job-to-be-done khác biệt so với nhân viên thường. Có thể giữ E2-S2 (ghim) ở 'could'.

### [HIGH] (product) PRD-HR-E1-S1 — Story dùng nhãn vai trò của Cleanmatic cũ, không phải persona BoGo
- 📍 PRD-HR-E1-S1:40
- ⚠️ Người đọc spec (đội build, PO mới, pilot SME) thấy 'nhân viên giao nhận hoặc nhân viên xử lý' — đây là vai trò của chuỗi giặt là Cleanmatic, không có trong 5 persona BoGo (STAFF, DEPT_MGR, ORG_ADMIN, EXEC, PLATFORM_ADMIN). Tương tự ở E1-S2:38 và E2-S1:39 ('quản lý hệ thống'), E2-S3:37 (lặp lại 'nhân viên giao nhận/xử lý'). Nếu đội build viết test-case hay dựng demo theo nhãn này, họ sẽ nghĩ tới nghiệp vụ giao nhận hàng — không phải nhân viên SME tổng quát. Rủi ro thêm: ORG_ADMIN bật thử sản phẩm thấy ngôn ngữ lạ, mất lòng tin ngay từ đầu.
- 🔧 Thay tất cả nhãn 'nhân viên giao nhận hoặc nhân viên xử lý' bằng 'nhân viên (STAFF)'; thay 'quản lý hệ thống' bằng 'quản trị tổ chức (ORG_ADMIN)'. Rà toàn bộ 14 story HR, tập trung dòng 'Là...' trong thân story.

### [HIGH] (product) PRD-HR-E1-S6 — Không có story nào build tính năng nhập hàng loạt nhân viên từ file, trong khi VISION đặt đây là kịch bản thành công ngày đầu tiên
- 📍 VISION:64
- ⚠️ VISION cam kết: 'Admin dựng xong sơ đồ 5 phòng ban, nhập 40 nhân viên từ file, gán trưởng phòng — chiều cùng ngày cả công ty chấm công'. Đây là kịch bản kích hoạt (activation) cốt lõi của BRD-G3 (≥70% tổ chức đăng ký phải kích hoạt thành công trong 30 ngày đầu). Nhưng PRD-HR-E1-S6 (quản lý hồ sơ nhân viên) chỉ mô tả tạo từng nhân viên riêng lẻ — không có AC nào về import file. Một tổ chức 40 người phải nhập tay từng hồ sơ sẽ bỏ cuộc trước khi đến bước chấm công, phá vỡ trial 14 ngày và chỉ số activation rate.
- 🔧 Thêm AC vào PRD-HR-E1-S6 (hoặc tạo story riêng) cho phép ORG_ADMIN import nhân viên hàng loạt từ file Excel/CSV: template tải sẵn, validate lỗi theo dòng, hiển thị xem trước trước khi lưu. Đây là must-be cho activation, không phải delighter.  _(known/escalated)_

### [HIGH] (product) PRD-HR-E2 — Epic E2 vẫn tham chiếu 'PRD-FINANCE' đã bị loại bỏ — luồng phiếu chi lương không còn ai sở hữu
- 📍 PRD-HR-E2:40
- ⚠️ Dòng phạm vi epic E2 ghi 'Khi bảng lương approved → tạo phiếu chi lương tại PRD-FINANCE'. Nhưng PRD-FINANCE đã bị loại hoàn toàn theo DEC-45. Story E2-S2 đã sửa đúng thành 'xuất Excel cho kế toán ngoài', nhưng phần Scope của epic mô tả tích hợp không tồn tại. Đội build đọc epic trước sẽ cố xây integration với một PRD không còn. Hơn nữa, nếu ai sau này muốn build luồng chi lương tự động, họ không có 'nhà' để đặt — khoảng trống này sẽ thành nợ spec.
- 🔧 Xóa dòng 'Liên kết phiếu chi: Khi bảng lương approved → tạo phiếu chi lương tại PRD-FINANCE' khỏi phần Phạm vi của PRD-HR-E2. Thay bằng: 'Chi lương: kết xuất Excel/CSV cho kế toán thực hiện chuyển khoản thủ công (không có chi lương tự động trong v1 — DEC-45).'

### [HIGH] (product) PRD-PLATFORM-E1-S1 — Không có story nào xây dựng màn hình dựng sơ đồ phòng ban — bước đầu tiên của kịch bản thành công ORG_ADMIN trong VISION
- 📍 PRD-PLATFORM-E1-S1:24
- ⚠️ VISION dòng 64 mô tả kịch bản thành công của ORG_ADMIN: 'Trong buổi sáng đầu tiên, Admin dựng xong sơ đồ 5 phòng ban, nhập 40 nhân viên từ file, gán trưởng phòng'. PRD-PLATFORM-E1-S1 đưa ORG_ADMIN vào trial nhưng ac chỉ tạo tài khoản ORG_ADMIN đầu tiên và hiển thị checklist tĩnh. Không có story nào trong toàn bộ spec xây màn hình tạo/quản lý phòng ban (thêm phòng, đặt tên, gán cấp cha/con). PRD-AI-E2-S5 (onboarding) dẫn dắt ORG_ADMIN tới 'dựng sơ đồ phòng ban' nhưng không có story nào build màn hình đó. Đây là GAP-ORG đã biết nhưng hậu quả sản phẩm chưa được phát biểu đủ: tổ chức không tạo được phòng ban thì nhóm chat hệ thống (COMM-E1-S2), phân quyền xem lương theo phòng (HR-E2-S3), giao việc theo phòng (TASK-E1-S2), và phân quyền APPROVAL đều bị chặn.
- 🔧 Tạo story mới trong PRD-PLATFORM-E1 (hoặc PRD-HR nếu sơ đồ tổ chức thuộc về HR): ORG_ADMIN tạo cây phòng ban nhiều cấp, đặt tên phòng, gán trưởng phòng (DEPT_MGR). Story này là prerequisite cứng của COMM-E1-S2, HR-E2-S3, TASK-E1-S2 và APPROVAL-E1-S2 — phải vào sprint sớm nhất.  _(known/escalated)_

### [HIGH] (product) PRD-RESOURCE-E1-S2 — Theme màu/biểu tượng thư mục được đánh moscow=must trong khi rủi ro adoption 'kho rỗng' vẫn OPEN
- 📍 PRD-RESOURCE-E1-S2:27
- ⚠️ Story S2 đóng gói ba yêu cầu vào một: kanban (có giá trị nghiệp vụ), gắn tag (hữu ích để tìm kiếm), và theme màu/icon (8 màu, 12 biểu tượng — thuần UX polish). Cả ba đều là must. Trong khi đó, sổ rủi ro epic (PRD-RESOURCE-E1 dòng 37–41) ghi nhận rủi ro 'nhân viên vẫn lưu file ở máy/chat vì thói quen → kho rỗng' với status OPEN. Kano: kho rỗng là vấn đề must-be chưa giải quyết; theme màu là delighter. Nếu kho rỗng thì màu sắc không giúp ích gì.
- 🔧 Tách theme màu/icon ra khỏi moscow=must, hạ xuống should hoặc could. Đầu tư phần story đó vào cơ chế kéo adoption thực sự: ví dụ một màn onboarding gợi ý tổ chức import hồ sơ từ Google Drive/thiết bị ngay khi dựng kho lần đầu — đây mới là vấn đề sống còn cần giải quyết trước khi RESOURCE có giá trị.

### [HIGH] (product) PRD-SUPPORT-E1-S1 — AI hỗ trợ tuyến đầu phụ thuộc kho hướng dẫn chính thức của BoGo — nhưng không có story nào xây kho này
- 📍 PRD-SUPPORT-E1-S1:25
- ⚠️ S1 ghi rõ AI 'trả lời dựa DUY NHẤT trên kho hướng dẫn chính thức của BoGo (do đội nền tảng biên soạn)'. Không có story nào trong toàn bộ spec xây dựng kho nội dung này: không có epic 'content ops', không có story PLATFORM_ADMIN biên soạn/cập nhật hướng dẫn, không có depend. Nếu go-live với kho rỗng hoặc thiếu, AI không trả lời được câu nào — phân hệ hỗ trợ sụp ngay từ tuyến đầu, đội support phải gánh toàn bộ, đây là rủi ro giữ chân (BRD-G3) cao nhất của PRD-SUPPORT.
- 🔧 Thêm một story hoặc task rõ ràng cho PLATFORM_ADMIN: 'Biên soạn và xuất bản kho hướng dẫn sử dụng BoGo trước go-live' — liệt kê số bài tối thiểu cần có (ít nhất phủ 80% thao tác phổ biến: chấm công, nghỉ phép, giao việc, đổi mật khẩu, thêm nhân viên). Đây là dependency launch-blocking của S1.

### [HIGH] (product) PRD-TASK — E4 (dashboard điều hành) không xuất hiện trong phần 'Trong phạm vi' của PRD-TASK
- 📍 PRD-TASK:52
- ⚠️ PRD-TASK mô tả phạm vi tại dòng 52–64 nhưng không đề cập đến 'dashboard tổng quan tổ chức' hay E4 — dù E4 là moscow:must với 5 story và depends_on PRD-HR. Người đọc PRD-TASK sẽ không biết dashboard tổng quan (nhân sự + công việc theo phòng ban) thuộc phân hệ này. Build team dễ không cấp budget sprint cho E4 khi lên kế hoạch từ PRD scope. Hơn nữa E4 đọc dữ liệu HR — việc đặt trong PRD-TASK tạo ra ranh giới sở hữu mờ: ai là người duyệt thay đổi contract dữ liệu HR khi E4-S1 cần?
- 🔧 Thêm E4 ('Dashboard tổng quan điều hành: nhân sự + công việc, báo cáo định kỳ') vào phần 'Trong phạm vi' của PRD-TASK. Đồng thời ghi rõ E4 là lớp trình bày đọc từ PRD-HR — phân hệ HR là nguồn dữ liệu và là chủ sở hữu dữ liệu chấm công/phép; E4 chỉ tham chiếu.

### [HIGH] (product) PRD-TASK-E1 — EXEC không có trong E1/E2/E3 — lãnh đạo không tạo hoặc giao việc được theo spec
- 📍 PRD-TASK-E1:18
- ⚠️ VISION mô tả kịch bản thành công của EXEC là 'giao việc xuống và theo dấu được đến cùng' (vision.md dòng 76). Nhưng E1 (Tạo & Giao việc) chỉ liệt kê personas STAFF và DEPT_MGR — EXEC không có tên. E2 (Theo dõi & Cộng tác) cũng tương tự. Kết quả: EXEC chỉ xuất hiện ở E4 (dashboard đọc), không có story nào giúp EXEC chủ động giao việc xuyên phòng hoặc theo sát tiến độ một việc cụ thể. Pilot đầu tiên mà CEO muốn giao việc trực tiếp — họ sẽ không làm được, và đây là một trong hai use case cốt lõi của persona mua hàng.
- 🔧 Bổ sung EXEC vào personas của E1 và E2. Làm rõ trong E1-S2 AC rằng EXEC giao việc xuyên phòng ban được (hiện chỉ có 1 dòng ngoại lệ ở AC line 31 nhưng persona không được khai báo). Nếu flow của EXEC thực sự giống DEPT_MGR thì ghi chú rõ để đội build không bỏ sót quyền.

### [HIGH] (product) PRD-TASK-E2-S4 — Ai là 'người duyệt' trong luồng Chờ duyệt chưa được định nghĩa rõ
- 📍 PRD-TASK-E2-S4:27
- ⚠️ E1-S1 cho phép bật tùy chọn 'cần duyệt khi hoàn thành' nhưng không có trường nào cho phép chọn người duyệt cụ thể khi tạo việc. E2-S4 dòng 27 ngầm hiểu 'người giao = người duyệt', nhưng dòng 30 lại nhắc đến 'người duyệt mặc định' như một thực thể riêng có thể bị vô hiệu hóa. Hai AC này mâu thuẫn nhau — đội build sẽ phải tự suy đoán và hai người trong team có thể cài đặt theo 2 cách khác nhau. Nếu 'người giao ≠ người duyệt' thì thiếu hẳn một story hoặc AC tạo việc giúp chọn người duyệt.
- 🔧 Làm rõ trong E1-S1 hoặc E1-S2: khi bật 'cần duyệt', người tạo có chọn người duyệt cụ thể không, hay mặc định là người giao? Nếu có thể chọn riêng, bổ sung AC vào E1-S1 (trường chọn người duyệt khi tạo việc). Xóa mâu thuẫn giữa E2-S4:27 và E2-S4:30.

### [HIGH] (spine) BRD — Chỉ số BRD-G2 'admin-time-reduction ≥50%' không có story nào định nghĩa cách đo baseline
- 📍 BRD:117
- ⚠️ BRD-G2 đặt mục tiêu giảm ≥50% thời gian xử lý hành chính (BRD:117), đo qua 'khảo sát pilot trước/sau'. Đây là chỉ số thành công kinh doanh cốt lõi — là bằng chứng dùng để thuyết phục khách trả phí sau trial. Nhưng không có story nào trong 80 story định nghĩa: ai làm khảo sát baseline, khi nào, đo những gì cụ thể, và lưu kết quả ở đâu. PRD-TASK có ghi 'khảo sát 2 tổ chức pilot đếm số lần giao việc/ngày qua chat' nhưng đây là ghi chú trong PRD prose, không phải AC của story nào cả. Nếu đội build không có AC cụ thể, khảo sát baseline sẽ không được làm — đến cuối pilot không có số để đo '50%', không chứng minh được giá trị với khách.
- 🔧 Thêm AC đo baseline vào PLATFORM-E1-S1 hoặc tạo story riêng 'Khảo sát baseline pilot' (moscow: must, gắn BRD-G2): định nghĩa rõ template khảo sát (loại task, thời gian, kênh hiện tại), thời điểm làm (trước go-live với 3-5 tổ chức pilot), và cách lưu kết quả để so sánh sau 3 tháng.

### [HIGH] (spine) BRD — Không có story nào sở hữu quy trình thông báo vi phạm dữ liệu cá nhân theo NĐ13/2023
- 📍 BRD:204
- ⚠️ BRD ghi nhận NĐ13 như rủi ro nhưng phạm vi chỉ giới hạn ở khuôn mặt sinh trắc. Toàn bộ spec không có story hay AC nào mô tả: ai phát hiện sự cố rò rỉ dữ liệu, trong bao lâu phải thông báo cơ quan (NĐ13/2023 quy định 72 giờ), nội dung thông báo gồm gì, ORG_ADMIN của tổ chức bị ảnh hưởng có được thông báo không và bằng cách nào. BoGo lưu trữ dữ liệu nhân sự đa tổ chức — một sự cố ảnh hưởng nhiều tenant. Thiếu quy trình này là lỗ hổng tuân thủ pháp lý nghiêm trọng, phát hiện ra khi đang bị thanh tra là không kịp vá.
- 🔧 Thêm AC vào PRD-PLATFORM-E1-S2 (PLATFORM_ADMIN quản vòng đời) hoặc tạo story riêng: (1) PLATFORM_ADMIN xác nhận và ghi nhận sự cố vi phạm dữ liệu; (2) hệ thống tự thông báo cho ORG_ADMIN của tổ chức bị ảnh hưởng trong ≤ 1 giờ; (3) tạo bản ghi sự cố (ai bị ảnh hưởng, loại dữ liệu, thời điểm phát hiện) phục vụ nghĩa vụ báo cáo 72 giờ với cơ quan. Tham chiếu vào ràng buộc pháp lý của BRD.

### [HIGH] (spine) PRD-HR — VISION cam kết 2 'sơ đồ tổ chức là xương sống' — spec không có màn hình nào xây xương sống đó
- 📍 VISION:32
- ⚠️ VISION cam kết nền tảng thứ hai: 'phòng ban, vai trò và quyền hạn trong BoGo phản chiếu đúng cấu trúc thật của doanh nghiệp — nhiều phòng ban, nhiều cấp' (VISION:32). Cam kết này ngầm yêu cầu một màn hình quản trị cơ cấu tổ chức — nơi ORG_ADMIN tạo cây phòng ban nhiều cấp, gán vai trò (trưởng phòng, phó phòng...), và gắn nhân viên vào đúng node. Không có story nào làm điều này. HR-E1-S6 có trường 'phòng ban thuộc về' nhưng đây chỉ là data entry, không phải quản lý entity phòng ban. Hệ quả: (a) nhóm chat hệ thống không biết cần tạo nhóm gì, (b) luồng duyệt không biết ai là người kế nhiệm khi trưởng phòng vắng, (c) báo cáo theo phòng không có gì để nhóm. Cam kết 2 của VISION về cơ bản chưa được spec.
- 🔧 Xem fix của finding spine-gap-org-escalated — cùng vấn đề, cần story 'Quản lý cơ cấu tổ chức' với AC: tạo/sửa/xoá phòng ban nhiều cấp; gán vai trò cho từng vị trí; hiển thị sơ đồ cây (hoặc bảng phân cấp đơn giản); export danh sách phòng ban. Story này phải là must, đặt trước HR-E1-S6 trong thứ tự build.  _(known/escalated)_

### [HIGH] (spine) PRD-PLATFORM — ORG_ADMIN không có màn hình tra cứu log hoạt động thống nhất cho toàn tổ chức mình
- 📍 PRD-PLATFORM-E1-S2:24
- ⚠️ Log 'ghi nhận ai làm gì' được ghi rải rác ở nhiều story (HR-E1-S6, COMM-E3-S2, RESOURCE-E1-S1, PLATFORM-E2-S3...) nhưng không có một giao diện nào cho ORG_ADMIN xem lại toàn bộ hoạt động trong tổ chức theo dòng thời gian. Khi có tranh chấp lương, khiếu nại chấm công, hoặc đội thanh tra lao động hỏi chứng từ (đây là trigger mua thứ 3 trong BRD), ORG_ADMIN không có nơi tra duy nhất. Đây là điều kiện để VISION hứa 'thay giấy tờ bằng dữ liệu' trở thành thật — nhưng không có story nào build nó.
- 🔧 Thêm story vào PRD-PLATFORM-E1 hoặc PRD-TASK-E4: ORG_ADMIN tra cứu nhật ký hoạt động toàn tổ chức (thao tác quan trọng: thay đổi lương, phê duyệt, sửa chấm công, tải tài liệu nhạy cảm) lọc theo người dùng, loại hành động, khoảng thời gian — phục vụ audit nội bộ và đáp ứng yêu cầu thanh tra. Có thể scope nhỏ: chỉ các hành động có rủi ro cao (lương, chấm công, phân quyền).

### [HIGH] (spine) PRD-TASK — EXEC chỉ có 1 must story trong horizon now — kịch bản thành công của lãnh đạo gần như trống
- 📍 VISION:76
- ⚠️ VISION mô tả kịch bản thành công của EXEC: '8h sáng mở BoGo thấy tỉ lệ đi làm toàn công ty, 3 việc lớn đang trễ hạn và phòng nào đang quá tải — điều phối ngay trong app' (VISION:78). Trong 80 story chỉ có 11 story gắn EXEC, và trong horizon now chỉ có 4 story có EXEC — trong đó chỉ PRD-TASK-E4-S1 (dashboard) là must. Tất cả story EXEC còn lại đều là should hoặc nằm ở horizon next (APPROVAL). Kịch bản 'điều phối ngay trong app' không được spec — không có story nào cho EXEC giao việc xuống cho trưởng phòng cụ thể từ dashboard, không có story tóm tắt AI cho EXEC (AI-E2-S3 chỉ phục vụ STAFF và DEPT_MGR, không gắn EXEC). Với persona là người ra quyết định mua phần mềm (EXEC thường là người ký ngân sách), trải nghiệm quá mỏng là rủi ro chuyển đổi.
- 🔧 Rà lại EXEC story coverage trước khi go-live MVP: ít nhất cần (1) TASK-E4-S1 dashboard EXEC có thể drill-down đến phòng ban cụ thể và giao việc nhanh từ điểm nóng (thêm AC vào TASK-E4-S1), (2) cân nhắc nâng AI-E2-S3 để phục vụ cả EXEC (thêm AC 'tóm tắt tiến độ toàn tổ chức cho lãnh đạo'). Không cần tạo story mới nếu mở rộng được AC hiện có.

### [HIGH] (spine) VISION — 37 must story dồn vào horizon 'now' (0–3 tháng) — không có phân chia sprint nội bộ, rủi ro trượt toàn bộ
- 📍 VISION:19
- ⚠️ VISION xác định horizon 'now' là 0–3 tháng MVP (VISION:19). Có 37 must story trong now, cộng thêm 25 should story (tổng ~62 story active trong now). Không có bất kỳ phân chia sprint hay thứ tự ưu tiên nội bộ nào giữa các story trong cùng horizon. Spec không phân biệt 'go-live tháng 1' với 'go-live tháng 3'. Rủi ro: đội build không biết cái gì cần xong trước để tổ chức pilot đầu tiên có thể đăng nhập và hoạt động. Ví dụ: dashboard (TASK-E4-S1) phụ thuộc vào phòng ban (chưa có story), phòng ban phụ thuộc vào nhân viên (HR-E1-S6), nhân viên phụ thuộc vào chấm công (HR-E1-S1/S2). Không có thứ tự này trong spec.
- 🔧 Thêm trường 'sprint' hoặc 'wave' (wave-1, wave-2, wave-3 trong horizon now) vào frontmatter của các story now, hoặc ít nhất thêm một bảng thứ tự go-live vào PRODUCT.md. Gợi ý wave-1 (tháng 1): phòng ban + nhân viên + chấm công + đăng ký. Wave-2 (tháng 2): lương + giao việc + chat. Wave-3 (tháng 3): AI + dashboard + thanh toán.

### [HIGH] (spine) VISION — VISION đặt 'nhập 40 nhân viên từ file' là kịch bản thành công ngày đầu tiên nhưng không có story nào build tính năng import hàng loạt
- 📍 VISION:64
- ⚠️ VISION:64 mô tả rõ: 'nhập 40 nhân viên từ file, gán trưởng phòng — chiều cùng ngày cả công ty chấm công và nhận việc trên BoGo'. Đây là điều kiện cần để BRD-G3 (kích hoạt ≥70%) đạt được — tổ chức 40 người không thể nhập tay từng nhân viên một trong nửa ngày. Nhưng xem qua toàn bộ 80 story: HR-E1-S6 chỉ quản lý hồ sơ đơn lẻ, không có AC nào cho import từ file Excel/CSV. Tính năng trợ lý onboarding (AI-E2-S5) được đánh moscow:should và nó chỉ dẫn dắt thao tác — không thay thế được bulk import. Nếu đội build không tự điền AC này vào, kịch bản thành công của VISION trở thành bất khả thi trong 14 ngày trial.
- 🔧 Tạo story cho PRD-HR-E1 hoặc PRD-PLATFORM-E1: ORG_ADMIN import danh sách nhân viên từ file Excel/CSV với template chuẩn (tên, mã NV, SĐT, phòng ban, chức danh, ngày vào làm); hệ thống báo lỗi dòng không hợp lệ và cho phép import phần hợp lệ; số user import không vượt hạn gói. Đây là điều kiện cần của kịch bản thành công VISION và mốc kích hoạt BRD-G3 — phải là must/now.  _(known/escalated)_

### [HIGH] (tech) PRD-AI-E2-S1 — AC hiệu năng '95% câu hỏi ≤ 10 giây' không đo được trong thực tế
- 📍 PRD-AI-E2-S1:27
- ⚠️ AC viết '95% câu hỏi được trả lời trong ≤ 10 giây' nhưng không nói rõ: đo trên cái gì (tải thật bao nhiêu org đồng thời?), kho tài liệu cỡ nào (1 trang hay 500 trang?), mạng người dùng ra sao. Đội build sẽ không biết nghiệm thu ra sao; nếu sản phẩm chạy 12 giây ở pilot lại không có chuẩn để kết luận pass hay fail.
- 🔧 Thêm điều kiện đo vào AC: 'Đo trên kho tài liệu ≤ X trang, với ≤ Y org dùng đồng thời, trên kết nối 4G/WiFi bình thường, hệ thống đạt p95 ≤ 10 giây. Nếu vượt ngưỡng, trợ lý hiển thị trạng thái đang xử lý và không để người dùng tự hỏi lại.' Chốt X và Y ngay sau khi có số liệu pilot đầu tiên.

### [HIGH] (tech) PRD-AI-E2-S1 — Không có AC cho trường hợp dịch vụ LLM hoặc vector-store bị lỗi hoặc timeout
- 📍 PRD-AI-E2-S1:24
- ⚠️ Bốn AC của S1 chỉ bao gồm: trả lời đúng, không đủ tự tin, chéo tổ chức, hiệu năng. Không AC nào định nghĩa hành vi khi API LLM bên thứ ba không phản hồi, khi pipeline tìm kiếm tài liệu lỗi, hoặc khi quota chi phí cạn. Đội build sẽ tự quyết và có thể crash hoặc hiển thị lỗi kỹ thuật cho người dùng.
- 🔧 Thêm một AC lỗi cụ thể: 'Giả sử dịch vụ LLM hoặc pipeline tài liệu không phản hồi trong vòng 15 giây hoặc trả về lỗi, thì trợ lý hiển thị thông báo rõ ràng bằng tiếng Việt và gợi ý kênh liên hệ người thật — không hiển thị lỗi kỹ thuật.' Cùng AC này nên được nhân sang S2 và S3 vì cùng phụ thuộc LLM.  _(known/escalated)_

### [HIGH] (tech) PRD-AI-E2-S4 — S4 'Đào tạo nội bộ & bài kiểm tra' (size L, should) gộp 4 hệ con khác nhau trong một story
- 📍 PRD-AI-E2-S4:10
- ⚠️ Một story size L gộp: (1) AI sinh câu hỏi từ tài liệu, (2) luồng duyệt và chỉnh sửa câu hỏi (human-in-the-loop), (3) phát bài và theo dõi ai làm/chưa làm, (4) chấm tự động và báo cáo theo phòng. Đây thực chất là một mini-LMS. Không thể ước lượng sprint khi một story chứa cả input, review, output và analytics. Nếu trễ ở bất kỳ mảnh nào sẽ kéo cả story fail.
- 🔧 Tách S4 thành ít nhất hai story: S4a 'AI sinh bài kiểm tra từ tài liệu — có người duyệt trước khi phát' (phần AI, must) và S4b 'Phát bài, chấm tự động và xem kết quả theo phòng' (phần LMS, should hoặc next). S4a có thể giao trước khi S4b sẵn sàng.  _(known/escalated)_

### [HIGH] (tech) PRD-AI-E2-S5 — S5 phụ thuộc ngầm vào PRD-PLATFORM-E1-S1 (workspace creation) nhưng không khai báo depends_on
- 📍 PRD-AI-E2-S5:24
- ⚠️ AC1 của S5 mở đầu 'Giả sử tổ chức mới kích hoạt không gian làm việc' và AC3 kiểm tra 'đã có ≥ 1 phòng ban' — cả hai cần workspace do PLATFORM-E1-S1 tạo ra. Nếu sprint build S5 trước khi PLATFORM-E1-S1 xong, không có môi trường thật để test. Chuỗi phụ thuộc build này hoàn toàn bị ẩn trong spec.
- 🔧 Thêm `depends_on: [PRD-PLATFORM-E1-S1]` vào frontmatter của S5. Bổ sung ghi chú thiết kế: 'Story này chỉ có thể nghiệm thu sau khi workspace provisioning (PLATFORM-E1-S1) hoàn chỉnh — onboarding assistant cần hook vào sự kiện first-login của ORG_ADMIN do PLATFORM tạo ra.'

### [HIGH] (tech) PRD-APPROVAL — APPROVAL tính phí theo 'lượng tờ trình/tháng' nhưng không có story nào đo đếm và giới hạn số lượng tờ trình
- 📍 PRD-APPROVAL:66
- ⚠️ BRD và PRD-APPROVAL ghi rõ APPROVAL là add-on tính phí theo lượng tờ trình/tháng (DEC-60). Nhưng không có story nào trong phân hệ APPROVAL hay PLATFORM định nghĩa: (1) đơn vị tính — mỗi lần trình = 1, hay mỗi cấp duyệt = 1?; (2) cơ chế đếm và lưu counter; (3) hành vi khi vượt hạn mức — chặn hay tính thêm; (4) PLATFORM_ADMIN cấu hình hạn mức cho add-on APPROVAL. So sánh: RESOURCE có PLATFORM-E2-S4 riêng cho quota storage — APPROVAL thiếu phần tương đương hoàn toàn. Không có cơ chế đo đếm thì không thể phát hành hoá đơn add-on.
- 🔧 Tạo story mới PRD-PLATFORM-E2-S5 'Hạn mức và đo đếm tờ trình theo gói add-on APPROVAL' (tương đương S4 cho RESOURCE): PLATFORM_ADMIN cấu hình hạn mức tờ trình/tháng; tổ chức thấy số đã dùng / hạn mức; cảnh báo 80%/100%; định nghĩa đơn vị tính; hành vi khi vượt trần. Bổ sung tham chiếu từ PRD-APPROVAL-E1-S1 đến story này.

### [HIGH] (tech) PRD-APPROVAL-E1-S2 — Luồng duyệt đa cấp (S2) phụ thuộc sơ đồ tổ chức từ PRD-HR nhưng không có AC kiểm thử khi sơ đồ thiếu dữ liệu hoặc không hợp lệ
- 📍 PRD-APPROVAL-E1-S2:27
- ⚠️ AC dùng 'lấy từ sơ đồ tổ chức PRD-HR' để xác định người duyệt cấp 1 và đường leo thang — nhưng không có AC cho trường hợp: tổ chức chưa dựng sơ đồ, phòng ban không có trưởng phòng, hoặc người duyệt không gắn vào vai trò rõ ràng. Khi sơ đồ tổ chức rỗng hoặc chưa hoàn chỉnh, luồng duyệt sẽ thất bại theo cách không xác định — lỗi ngầm thay vì thông báo rõ.
- 🔧 Thêm AC edge path: 'Tờ trình được tạo cho phòng ban chưa có trưởng phòng trong sơ đồ → hệ thống chặn trình và thông báo rõ lý do ngay khi bấm Trình; ORG_ADMIN nhận thông báo yêu cầu cập nhật sơ đồ'. Thêm pre-condition trong ghi chú thiết kế: APPROVAL-E1-S2 chỉ hoạt động đúng khi PRD-HR-E1-S1 (sơ đồ tổ chức) đã hoàn chỉnh — ghi nhận đây là runtime dependency.

### [HIGH] (tech) PRD-APPROVAL-E1-S3 — Story ký xác nhận (S3) được đánh size M nhưng thực tế phải build 4 luồng xác thực riêng biệt
- 📍 PRD-APPROVAL-E1-S3:11
- ⚠️ Story này chứa: (1) passkey/WebAuthn trên thiết bị, (2) PIN fallback, (3) OTP email/SMS, (4) khuôn mặt camera — mỗi luồng có edge case riêng, cộng thêm đăng ký thiết bị tin cậy, thu hồi thiết bị, bậc đảm bảo cấu hình được, bản ghi ký bất biến + hash toàn tệp. Kỹ sư ước lượng size M sẽ bị đánh giá thấp ít nhất 3–4 lần. Sprint planning sẽ commit sai, gây trượt lịch.
- 🔧 Tách S3 thành ít nhất 2 story: (S3a) cơ chế passkey + PIN + bản ghi ký bất biến với hash; (S3b) OTP/khuôn mặt camera bậc CAO + đăng ký/thu hồi thiết bị tin cậy. Đánh lại size: S3a = L, S3b = M. Bổ sung AC kiểm thử rõ: 'Thay tệp đính kèm sau khi ký → hệ thống phát hiện hash lệch trong vòng X giây'.

### [HIGH] (tech) PRD-APPROVAL-E1-S3 — Ký xác nhận trên web không đạt 'bậc thường mặc định' vì passkey phụ thuộc phần cứng thiết bị — mâu thuẫn với ràng buộc web parity DEC-66
- 📍 PRD-APPROVAL-E1-S3:26
- ⚠️ BRD DEC-66 bắt buộc web phải parity với mobile cho mọi vai trò. Nhưng AC S3 ghi 'trên web/máy không có sinh trắc → dùng PIN hoặc OTP'. Điều này có nghĩa ORG_ADMIN và EXEC dùng web để duyệt chứng từ bậc CAO sẽ luôn phải nhập OTP — đường dùng tệ hơn mobile. Hơn nữa, với bậc thường trên web, người duyệt chỉ còn PIN (passkey không có), trong khi mobile có FaceID một chạm — tạo ra trải nghiệm hai cấp rõ ràng. Nếu ORG_ADMIN làm việc chủ yếu trên web, mọi lần duyệt đều phải nhập PIN, không phải 'một chạm'.
- 🔧 Thêm AC riêng cho web: 'Trên web, bậc Thường = PIN (không có passkey); bậc Cao = OTP email/SMS. Đây là ngoại lệ DEC-66 được phép vì passkey gắn phần cứng thiết bị di động'. Ghi nhận rõ ngoại lệ này trong phần 'Ghi chú thiết kế' và tham chiếu DEC-66 để tránh đội build bị hỏi khi nghiệm thu parity.

### [HIGH] (tech) PRD-COMM — Hạ tầng push notification không có PRD sở hữu — 5 story COMM phụ thuộc trực tiếp vào nó mà không có spec
- 📍 PRD-COMM:82
- ⚠️ COMM E1-S3 (@mention), E1-S8 (nhắc hẹn lặp), E2-S1 (broadcast), và hầu hết story còn lại đều ghi 'dùng hạ tầng thông báo nội bộ chung — đội build lo'. DEC-30 ghi nhận rủi ro này là chấp nhận được. Nhưng không có PRD nào sở hữu SLA, fallback, retry, và cơ chế idempotency của lớp push. Nếu đội build không tự xây spec trước sprint COMM, mỗi story COMM sẽ nghiệm thu trạng thái thông báo theo cách khác nhau — hoặc bỏ luôn. Tại pilot, nhân viên không nhận @mention hoặc nhắc hẹn là kênh nội bộ mất uy tín ngay tuần đầu.
- 🔧 Thêm một story kỹ thuật (tech enabler) vào PRD-PLATFORM hoặc một epic mới, sở hữu spec hạ tầng thông báo chung: SLA in-app ≤5 giây + push ≤30 giây, offline queue + TTL, idempotency key, fallback khi FCM/APNs lỗi. Các story COMM (và TASK, HR) tham chiếu story đó thay vì nói 'đội build lo'. Nếu không muốn tạo story riêng, ít nhất thêm AC vào PRD-PLATFORM-E1 và đặt nó là depends_on của COMM-E2-S1.

### [HIGH] (tech) PRD-COMM-E1-S1 — S1 nhắn tin 1-1 gộp 9 AC thuộc 5 chức năng riêng biệt — khó ước lượng và khó nghiệm thu từng phần
- 📍 PRD-COMM-E1-S1:23
- ⚠️ Story size M nhưng thực chất bao gồm: (a) real-time chat + trạng thái đã đọc, (b) đính kèm file/ảnh với liên kết tự làm mới, (c) gắn link ERP entity với phân quyền động, (d) reply/trích dẫn, (e) voice message với ghi âm + thanh tiến trình, (f) offline reconnect ≤30 giây + không rơi tin, (g) thu hồi tin + log. Mỗi mục đó là một bề mặt kỹ thuật riêng (WebSocket/SSE, object storage, auth proxy, audio encoding, client queue). Nghiệm thu một AC fail không có cách pass riêng từng AC còn lại — sprint có thể bị chặn toàn bộ ở một điểm nhỏ (ví dụ voice). Đội build không thể ước lượng chuẩn và không thể giao tăng dần.
- 🔧 Tách S1 thành ít nhất 3 story: (1) Chat cơ bản (text + trạng thái + real-time + reconnect); (2) Đính kèm file + voice message (storage, liên kết tự làm mới, ghi âm); (3) Gắn link ERP (hợp đồng entity_type, phân quyền động, deep-link). Thu hồi tin có thể nằm trong story chat cơ bản. Sau khi tách, đánh lại size và ràng buộc thứ tự build.  _(known/escalated)_

### [HIGH] (tech) PRD-COMM-E1-S1 — File và voice message được hứa giữ 'bất kỳ lúc nào trong suốt vòng đời hội thoại' nhưng không có chính sách lưu trữ hay trần chi phí
- 📍 PRD-COMM-E1-S1:25
- ⚠️ AC hứa file và voice message luôn truy được — không có giới hạn thời gian hay dung lượng trên tổ chức. BRD hạch toán COGS hạ tầng ~10k/user/tháng cho toàn bộ BoGo, nhưng không có con số riêng cho storage chat. Mỗi tổ chức 30 nhân viên gửi ảnh/file/voice hằng ngày có thể tích luỹ hàng chục GB/tháng — chi phí object storage không kiểm soát được, và không nằm trong COGS add-on RESOURCE (thứ đó là v2). Không có AC nào về quota/tổ chức, TTL file, hay cảnh báo khi gần đầy — đội build sẽ tự quyết hoặc để vô hạn.
- 🔧 Thêm AC vào S1: (1) Định nghĩa 'vòng đời hội thoại' — file giữ bao lâu (ví dụ: 12 tháng hoặc tới khi hội thoại bị lưu trữ/xoá); (2) Đặt quota storage/tổ chức cấu hình được (mặc định ví dụ 5GB); (3) Hệ thống cảnh báo khi dùng ≥80% quota; (4) Vượt quota thì từ chối đính kèm mới và báo lỗi rõ. Phối hợp với BRD để ghi rõ storage là gộp vào gói chuẩn hay sẽ là add-on.

### [HIGH] (tech) PRD-COMM-E1-S1 — Voice message và file đính kèm giữ 'bất kỳ lúc nào trong suốt vòng đời hội thoại' nhưng không có trần dung lượng per-org cho media COMM — mâu thuẫn với kinh tế gói 89k
- 📍 PRD-COMM-E1-S1:25
- ⚠️ S1 cam kết file/voice message 'BẤT KỲ LÚC NÀO trong suốt vòng đời hội thoại'. Voice message mặc định ≤3 phút/tin, có thể có hàng nghìn tin/ngày trong một tổ chức 40 người. Không có AC nào đặt trần dung lượng voice/media cho COMM (khác với RESOURCE-E1-S1 đã có hạn mức từ E2-S4). BRD đặt COGS ≤35k/user/tháng gồm hạ tầng ~10k — media storage vô hạn có thể phá vỡ con số này ở tổ chức dùng nhiều voice message mà không có cơ chế kiểm soát.
- 🔧 Thêm AC vào E1-S1: 'Dung lượng lưu trữ media (ảnh, file, voice) của COMM nằm trong hạn mức tổng theo gói, quản lý cùng cơ chế [[PRD-PLATFORM-E2-S4]]; chạm trần → thông báo ORG_ADMIN và chặn tải lên media mới (không chặn text); dữ liệu cũ không bị xóa tự động.' Nếu PO muốn giữ vòng đời vô hạn, cần xác định con số lưu trữ media vào unit economics và cập nhật COGS estimate.

### [HIGH] (tech) PRD-COMM-E3-S1 — Full-text search index tin nhắn không có AC phân tách theo tenant — lọc quyền sau khi đã lập index chung là mô hình rủi ro cross-tenant
- 📍 PRD-COMM-E3-S1:28
- ⚠️ E3-S1 chỉ nói 'lọc kết quả theo quyền trước khi trả' nhưng không định nghĩa index được xây dựng per-tenant hay trên một index chung. Với full-text search (thường dùng Elasticsearch/Typesense), một index chung cho tất cả tổ chức và lọc quyền ở tầng truy vấn là mô hình phổ biến nhưng rủi ro cao: một bug trong query filter hoặc một lỗi cấu hình tenant_id có thể làm lộ tin nhắn của tổ chức khác mà không có AC kiểm thử ở tầng index. VISION cam kết 'không tổ chức nào nhìn thấy dữ liệu của tổ chức khác' và BRD đặt đây là ràng buộc kinh doanh bắt buộc.
- 🔧 Thêm AC vào E3-S1: 'Kết quả tìm kiếm của tổ chức A không bao giờ chứa bất kỳ nội dung nào từ tổ chức B — bao gồm metadata (tên người, tên nhóm) của tổ chức khác; kiểm tra này là điều kiện DoD của story và nằm trong bộ test cross-tenant bắt buộc (cùng bộ với PRD-PLATFORM-E1-S1).' Ghi chú thiết kế nên gợi ý xây index per-tenant hoặc shard theo tenant_id thay vì lọc sau trên index chung — đây là quyết định kiến trúc ảnh hưởng đến security isolation.

### [HIGH] (tech) PRD-HR-E1-S1 — Chấm công offline dùng 'timestamp thiết bị' — không có AC chống giả mạo đồng hồ thiết bị
- 📍 PRD-HR-E1-S1:33
- ⚠️ AC offline dòng 33 yêu cầu 'lưu cục bộ với timestamp thiết bị'. Thiết bị Android/iOS cho phép người dùng chỉnh thủ công giờ hệ thống khi tắt 'tự động đồng hồ mạng'. Nhân viên có thể chỉnh đồng hồ thiết bị về giờ ca, chấm PIN offline, rồi bật lại mạng để đồng bộ — timestamp gian lận không bị phát hiện. Cờ 'offline-sync' chỉ giúp DEPT_MGR rà soát thủ công nhưng không cung cấp căn cứ kỹ thuật để phân biệt timestamp hợp lệ và timestamp bị chỉnh. Đây là lỗ hổng chống gian lận nghiêm trọng trong kịch bản chính PRD-HR-E1 hướng tới.
- 🔧 Bổ sung AC: 'Khi đồng bộ offline record, hệ thống so sánh timestamp thiết bị với server time tại thời điểm nhận sync; nếu chênh lệch > [ngưỡng cấu hình, mặc định 30 phút], bản ghi tự động nâng cờ SUSPECT_TIMESTAMP để DEPT_MGR phê duyệt thủ công trước khi tính công — không tự tính công từ bản ghi bị cờ.' Thêm thiết kế ghi chú: server timestamp là nguồn chuẩn, thiết bị chỉ là fallback cần xác thực.

### [HIGH] (tech) PRD-HR-E1-S1 — Không có AC cho trường hợp nhân viên từ chối đồng ý sinh trắc — hệ thống bị kẹt
- 📍 PRD-HR-E1-S1:32
- ⚠️ AC9 yêu cầu 'bắt buộc ký đồng ý điện tử trước khi đăng ký khuôn mặt'. Nghị định 13/2023 cho phép cá nhân từ chối cung cấp dữ liệu sinh trắc học. Không có AC nào mô tả hành vi hệ thống khi nhân viên từ chối: nút đăng ký có ẩn không? Nhân viên có thể chỉ dùng PIN vĩnh viễn không? Phòng HR có được thông báo không? Thiếu path này đồng nghĩa đội build có thể block onboarding cho nhân viên từ chối — vi phạm pháp luật về quyền lao động và NĐ 13/2023, hoặc thiết kế không nhất quán với chính sách consent đã tuyên bố.
- 🔧 Thêm AC: 'Nếu nhân viên từ chối ký đồng ý sinh trắc → hệ thống KHÔNG block onboarding; nhân viên được cấu hình dùng PIN/mã NV làm phương thức chấm công chính (permanent fallback); hồ sơ ghi trạng thái consent = Từ chối; ORG_ADMIN được thông báo. Chấm công bằng PIN trong trường hợp này KHÔNG cần DEPT_MGR rà soát riêng (không phải fallback bất thường).'

### [HIGH] (tech) PRD-HR-E1-S1 — Quyền yêu cầu xóa vector sinh trắc không có luồng thực thi — nhân viên biết quyền nhưng không có nút/màn hình nào để thực hiện
- 📍 PRD-HR-E1-S1:32
- ⚠️ AC9 của HR-E1-S1 khẳng định nhân viên 'có quyền yêu cầu xóa vector khi nghỉ việc' theo NĐ13/2023. HR-E1-S6:30 xác nhận xóa trong 30 ngày hoặc ngay khi yêu cầu. Tuy nhiên không có AC nào trong bất kỳ story nào mô tả: STAFF gửi yêu cầu xóa như thế nào (màn hình nào, nút nào), ai nhận và xử lý yêu cầu đó, nhân viên được xác nhận bằng cách nào rằng vector đã bị xóa. Đây là quyền pháp lý bắt buộc — thiếu luồng thực thi là vi phạm NĐ13/2023 mà không có cửa thoát khi thanh tra.
- 🔧 Thêm AC vào PRD-HR-E1-S6: 'STAFF xem và rút đồng ý dữ liệu sinh trắc từ hồ sơ cá nhân của mình bất kỳ lúc nào; gửi yêu cầu xóa → ORG_ADMIN nhận thông báo; xóa hoàn tất trong ≤ 30 ngày (hoặc ≤ 24 giờ nếu đã nghỉ việc); sau khi xóa hệ thống gửi xác nhận cho nhân viên (email/in-app) kèm log tham chiếu; nhân viên rút đồng ý chỉ chấm công bằng PIN dự phòng.' Cũng cần AC ngăn chặn nhân viên rút đồng ý khi đang có ca chưa kết thúc mà không có phương thức dự phòng đã đặt.

### [HIGH] (tech) PRD-HR-E1-S6 — Không có AC cho nhập hàng loạt nhân viên — VISION đặt kịch bản '40 nhân viên từ file trong buổi sáng đầu tiên' nhưng story không bao gồm bulk import
- 📍 PRD-HR-E1-S6:24
- ⚠️ VISION:64 mô tả kịch bản thành công ORG_ADMIN: 'nhập 40 nhân viên từ file'. AI-E2-S5:25 thậm chí trích dẫn trực tiếp 'làm sao thêm 30 nhân viên một lúc' là câu hỏi onboarding điển hình mà trợ lý AI cần trả lời — nhưng link mở 'đúng màn hình thao tác tương ứng' sẽ trỏ tới một màn hình không tồn tại trong spec. HR-E1-S6 chỉ có AC tạo từng nhân viên (không có bulk). Nếu ORG_ADMIN phải nhập tay 40 người trong ngày đầu, tỉ lệ kích hoạt BRD-G3 ≥70% gần như không đạt được.
- 🔧 Thêm AC vào PRD-HR-E1-S6 hoặc tạo story riêng: 'ORG_ADMIN tải lên file (Excel/CSV mẫu) nhập hàng loạt nhân viên; hệ thống validate từng dòng (trùng mã, thiếu trường bắt buộc, sai định dạng) và hiển thị báo cáo lỗi theo dòng trước khi nhập; dòng hợp lệ nhập vào, dòng lỗi hiển thị rõ lý do để sửa; nhập thành công/thất bại ghi log; phải có ≥ 1 phương thức nhập hàng loạt trước khi AI-E2-S5 go-live.' Đây là điều kiện tiên quyết của kịch bản thành công VISION.  _(known/escalated)_

### [HIGH] (tech) PRD-HR-E2 — Epic E2 scope vẫn đề cập 'tạo phiếu chi lương tại PRD-FINANCE' — PRD-FINANCE đã bị loại hoàn toàn
- 📍 PRD-HR-E2:40
- ⚠️ Dòng 40 của PRD-HR-E2.md (phần Phạm vi epic, không phải design note) mô tả: 'Khi bảng lương approved → tạo phiếu chi lương tại PRD-FINANCE.' PRD-FINANCE đã bị xoá khỏi scope theo DEC-45 và không tồn tại trong repo. Đây là dead reference trong phần scope chính thức, không phải ghi chú. Đội build đọc epic scope sẽ tạo integration point sang một PRD không tồn tại — hoặc mất thời gian tra cứu rồi phát hiện mâu thuẫn. PRD-HR-E2-S2 AC3 đã xử lý đúng (ghi rõ 'NGOÀI phạm vi v1') nhưng epic scope chưa được cập nhật khớp.
- 🔧 Sửa dòng 40 trong PRD-HR-E2.md: xoá 'Liên kết phiếu chi: Khi bảng lương approved → tạo phiếu chi lương tại PRD-FINANCE.' và thay bằng: 'Xuất Excel/CSV bảng lương để kế toán tự xử lý chi lương qua ngân hàng (không tích hợp phần mềm kế toán trong v1 — DEC-45).' Khớp với AC đã được phê duyệt ở PRD-HR-E2-S2.

### [HIGH] (tech) PRD-HR-E2-S4 — Thưởng KPI (must) phụ thuộc vào story TASK-E3-S2 chỉ là 'should' — không có ràng buộc build ordering
- 📍 PRD-HR-E2-S4:26
- ⚠️ PRD-HR-E2-S4 được đánh `moscow: must` và tự tính thưởng KPI từ dữ liệu tỉ lệ hoàn thành công việc ([[PRD-TASK-E3-S2]]). Nhưng PRD-TASK-E3-S2 chỉ là `moscow: should`. Nếu sprint cắt TASK-E3-S2, thưởng KPI toàn bộ hiển thị N/A — chu kỳ lương đầu tiên không phản ánh đúng thu nhập nhân viên. Điều này vi phạm cam kết 'thưởng KPI là phần lương quan trọng với SME, không bỏ qua' (design note dòng 46). Vấn đề không được ghi vào `depends_on` nên đội build không thấy ràng buộc.
- 🔧 Hoặc nâng PRD-TASK-E3-S2 lên `moscow: must` (khớp với tầm quan trọng của KPI payroll), hoặc thêm AC vào PRD-HR-E2-S4 mô tả rõ hành vi khi TASK data chưa khả dụng: 'Khi chỉ tiêu TASK chưa có data → bảng lương vẫn tạo được, cột thưởng KPI hiển thị N/A với nhãn giải thích rõ lý do — không chặn approve bảng lương'. Và bổ sung `depends_on: [PRD-TASK-E3-S2]` trong frontmatter để build ordering rõ ràng.

### [HIGH] (tech) PRD-PLATFORM-E1-S1 — Máy trạng thái thuê bao mâu thuẫn: trial hết hạn không qua ân hạn, nhưng spec gói có trả phí thì có
- 📍 PRD-PLATFORM-E1-S1:30
- ⚠️ E1-S1 line 30 quy định trial hết hạn → THẲNG vào KHOÁ GHI, không qua ân hạn. E2-S2 line 25 quy định 'Quá hạn gói → vào ÂN HẠN 7 ngày'. Đồng thời E2-S2 line 47 tuyên bố đây là 'một máy trạng thái thuê bao duy nhất'. Đội build nhìn hai luồng khác nhau cho cùng một FSM: trial đi thẳng sang KHOÁ GHI còn subscription paid đi qua ÂN HẠN. Không biết trial có ân hạn hay không — test case cho transition này không viết được vì spec mâu thuẫn. Rủi ro: khoá nhầm tổ chức vừa tạo xong (chưa kịp thêm nhân viên, lỡ vài ngày) gây mất niềm tin ngay đầu onboarding.
- 🔧 Bổ sung một AC tường minh trong E1-S1 (hoặc E2-S2): trial hết hạn CÓ hay KHÔNG có ân hạn 7 ngày trước khi vào KHOÁ GHI — PO chốt một con số rõ. Ví dụ AC: 'Trial hết hạn (ngày 14 hoặc 30 nếu được gia hạn) mà chưa thanh toán → vào ÂN HẠN X ngày [PO điền] chỉ cảnh báo; hết ân hạn → KHOÁ GHI. Luật này ghi rõ trong sơ đồ trạng thái đính kèm epic E1 hoặc E2 làm tài liệu căn cứ duy nhất.'

### [HIGH] (tech) PRD-PLATFORM-E1-S1 — Điều kiện gia hạn trial '60% nhân viên chấm công' không định nghĩa mẫu số — không kiểm thử được
- 📍 PRD-PLATFORM-E1-S1:27
- ⚠️ AC gia hạn điều kiện (DEC-55) nêu '≥ 60% nhân viên đã chấm công ít nhất 1 lần trong 7 ngày đầu'. Mẫu số là gì? Tổng nhân viên đã được tạo tài khoản? Tổng user theo gói đã mua? Tổ chức mới chỉ có 1 admin, chưa nhập nhân viên — 60% của 0 không tính được, điều kiện không bao giờ trigger. Không có ngưỡng tối thiểu nhân viên nào được đặt ra. Đội build sẽ phải tự giả định, dẫn đến logic không nhất quán giữa frontend (hiển thị tiến trình %) và backend (tính điều kiện).
- 🔧 Sửa AC thành: 'Mẫu số = tổng nhân viên có tài khoản ACTIVE trong tổ chức tại thời điểm kiểm tra (không tính tài khoản bị vô hiệu hoá); điều kiện chỉ trigger khi tổ chức có ít nhất [PO điền, ví dụ 3] nhân viên. Nếu không đủ nhân viên tối thiểu vào cuối ngày 7 → không gia hạn tự động, KHÔNG thông báo gây nhầm lẫn.' Thêm AC hiển thị tiến trình '% nhân viên đã chấm công' cho ORG_ADMIN theo dõi trong 7 ngày đầu.

### [HIGH] (tech) PRD-PLATFORM-E1-S1 — Không có story nào định nghĩa cách PLATFORM_ADMIN hỗ trợ khôi phục tài khoản ORG_ADMIN duy nhất mà không vi phạm ranh giới dữ liệu
- 📍 PRD-PLATFORM-E1-S1:29
- ⚠️ E1-S2 thiết lập RANH GIỚI CỨNG: PLATFORM_ADMIN không mở được dữ liệu nội bộ của tổ chức. Nhưng nếu ORG_ADMIN duy nhất của tổ chức đang trả phí mất quyền truy cập (quên mật khẩu + mất thiết bị xác minh), không có cơ chế self-recovery nào được spec và PLATFORM_ADMIN bị chặn bởi ranh giới của chính spec. Đây là tình huống thực tế gặp thường xuyên ở SME VN — không có luồng xử lý nghĩa là đội hỗ trợ sẽ xử lý ad-hoc, phá vỡ ranh giới tenant hoặc để tổ chức trả phí bị khoá vĩnh viễn.
- 🔧 Thêm AC vào PRD-PLATFORM-E1-S2 hoặc PRD-SUPPORT-E1-S3: 'Khôi phục tài khoản ORG_ADMIN qua kênh hỗ trợ: PLATFORM_ADMIN gửi magic-link một lần tới email đã đăng ký tổ chức (email là siêu dữ liệu, không phải dữ liệu nội bộ tổ chức — không vi phạm ranh giới); nếu email cũng mất quyền thì yêu cầu xác minh danh tính tổ chức bằng giấy tờ pháp nhân qua kênh hỗ trợ; mọi thao tác khôi phục ghi log riêng.' Phân biệt rõ: khôi phục credential ≠ đọc dữ liệu nội bộ.

### [HIGH] (tech) PRD-PLATFORM-E2-S2 — Lịch nhắc hạn (14/7/3/1 ngày) không xác định có áp cho trial 14 ngày không — và nếu áp thì ngày nhắc đầu tiên trùng ngày đăng ký
- 📍 PRD-PLATFORM-E2-S2:24
- ⚠️ E2-S2 quy định nhắc 'Trước hạn gói 14/7/3/1 ngày'. Nếu áp cho trial 14 ngày: nhắc '14 ngày trước' = ngay ngày đăng ký → người dùng vừa tạo tài khoản đã nhận email đòi gia hạn, trải nghiệm tệ và gây spam. Nếu trial 30 ngày (đã gia hạn điều kiện): nhắc '14 ngày trước' = ngày 16, hợp lý hơn. Hiện spec không nói rõ trial có áp lịch 14/7/3/1 hay có lịch riêng — đội build sẽ tự chọn, dẫn đến hành vi nhắc không như PO mong muốn.
- 🔧 Thêm một AC trong E2-S2 (hoặc E1-S1) chỉ rõ lịch nhắc trial: ví dụ 'Trial: nhắc vào ngày [PO chọn, ví dụ 7/3/1 ngày trước hết hạn, KHÔNG nhắc ở mốc 14 ngày vì trial chỉ có 14 ngày]. Paid subscription: nhắc 14/7/3/1 ngày.' Tách biệt hai lịch để test case viết được.

### [HIGH] (tech) PRD-PLATFORM-E2-S3 — Webhook thanh toán không có AC xác thực chữ ký (HMAC/secret) — mở cổng giả mạo gạch nợ
- 📍 PRD-PLATFORM-E2-S3:25
- ⚠️ E2-S3 xử lý biến động số dư (VietQR) và webhook cổng thanh toán nhưng không có AC nào yêu cầu xác thực nguồn gốc webhook (HMAC signature, IP whitelist, hoặc shared secret). Bất kỳ ai biết endpoint webhook đều có thể gửi payload giả, trigger gạch nợ giả, mở gói cho tổ chức chưa trả tiền — hoặc ngược lại trigger dunning sai. Đây là lỗ hổng bảo mật cơ bản của hệ thống thanh toán.
- 🔧 Thêm AC bắt buộc: 'Mọi webhook nhận vào (VietQR bank/đối soát và cổng thanh toán) phải được xác thực trước khi xử lý: kiểm tra chữ ký HMAC (hoặc cơ chế secret của nhà cung cấp) và timestamp không quá [5 phút] để chống replay attack. Request không hợp lệ → reject 401, ghi log cảnh báo bảo mật. Cơ chế xác thực cụ thể chốt cùng hợp đồng nhà cung cấp trước sprint.'

### [HIGH] (tech) PRD-TASK-E1-S1 — STAFF được liệt kê là persona tạo việc nhưng quyền tạo task của STAFF không được định nghĩa
- 📍 PRD-TASK-E1-S1:20
- ⚠️ Frontmatter dòng 20 khai personas: [STAFF, DEPT_MGR] cho E1-S1, nhưng user story bên trong viết "Là trưởng phòng" và không AC nào nêu STAFF được phép tạo task hay không. E1-S2 dòng 31 chỉ giới hạn phạm vi giao việc của trưởng phòng; không đề cập STAFF. Nếu STAFF được tạo task, câu hỏi tức thì là: họ giao cho ai? Phạm vi ra sao? Nếu STAFF không được tạo task, personas bị sai và gây hiểu nhầm khi build. Đây là RBAC gap ảnh hưởng trực tiếp đến thiết kế permission model.
- 🔧 Làm rõ trong E1-S1: hoặc (a) xóa STAFF khỏi personas nếu chỉ DEPT_MGR/ORG_ADMIN/EXEC tạo được task; hoặc (b) thêm AC cụ thể: "STAFF chỉ tạo task tự giao cho bản thân (không giao cho người khác) — phạm vi: chỉ chính mình là người nhận." Một trong hai phải được chọn trước khi build.

### [HIGH] (tech) PRD-TASK-E4-S1 — Dashboard tổng quan cần HR expose số liệu đi làm/vắng theo phòng ban nhưng không có story HR nào định nghĩa endpoint này
- 📍 PRD-TASK-E4-S1:24
- ⚠️ AC dòng 24 của E4-S1 yêu cầu hiển thị "nhân sự đi làm / nghỉ / chưa chấm công — từ phân hệ Nhân sự, nhóm theo phòng ban". Ghi chú thiết kế (dòng 48) ghi "HR + TASK expose số tổng hợp theo phòng ban" nhưng không một story nào trong cụm HR-E1 định nghĩa API/endpoint tổng hợp (aggregate) này. PRD-HR-E1-S4 chỉ cung cấp lịch sử chấm công cá nhân và toàn đội, không phải số gộp theo phòng ban dạng dashboard. Khi đội build TASK bắt đầu E4, họ sẽ phát hiện contract với HR chưa tồn tại — phải mở lại HR spec giữa sprint.
- 🔧 Bổ sung AC vào PRD-HR-E1-S4 (hoặc thêm story mới): "HR expose endpoint tổng hợp trả về: số nhân viên đi làm/vắng/chưa chấm công theo phòng ban cho ngày hiện tại, có phân quyền tổ chức." Ghi rõ trong E4-S1 rằng story này depends_on AC đó của HR — thay vì chỉ ghi chú thiết kế dạng build-coordination.

### [HIGH] (tech) PRD-TASK-E4-S5 — Gửi báo cáo qua email phụ thuộc hạ tầng email không được spec trong bất kỳ story nào
- 📍 PRD-TASK-E4-S5:25
- ⚠️ E4-S5 yêu cầu kênh gửi "EMAIL + thông báo trong app" (dòng 25) với logic retry đầy đủ (dòng 28), xử lý bounce cứng — nhưng không một story hay epic nào trong toàn bộ spec BoGo định nghĩa hạ tầng email transactional (provider SMTP/SaaS, cấu hình domain, rate limit, deliverability). Đây là dependency ngoài hệ thống cần hợp đồng API, cấu hình DNS (SPF/DKIM), và test trước khi sprint này bắt đầu. Bỏ qua là E4-S5 không thể build hoặc test được đầu-cuối.
- 🔧 Thêm NFR rõ ràng vào E4-S5 hoặc PRD-PLATFORM: "Hệ thống dùng dịch vụ email transactional [provider cụ thể, vd: SendGrid/AWS SES/Mailgun]; cấu hình domain xác thực (SPF, DKIM) trước sprint; rate limit và quota phải đủ cho [X] báo cáo/ngày tại mốc 100 tổ chức." Ghi là dependency cứng của E4-S5.


## MEDIUM (72) — compact

- [M] (craft) PRD-AI-E2-S5: AC 'trợ lý trả lời ngắn gọn' không đo được — không có ngưỡng cụ thể nào để QA pass/fail  · fix: Thay 'trả lời ngắn gọn' bằng một tiêu chí đo được, ví dụ: 'trả lời ≤ 3 câu hoặc ≤ 100 từ, kèm liên kết mở đúng màn hình thao tác tương ứng'. PO chốt ngưỡng cụ thể.
- [M] (craft) PRD-COMM: PRD-COMM còn nhắc 'SALES...' như một PRD đang hoạt động trong ranh giới DRY  · fix: Dòng 82: xoá '...SALES...)'; thay bằng '(TASK-E1-S2, HR...)'. Dòng 106: thay 'việc/đơn/ca' bằng 'việc/ca' — 'đơn' không còn là thực thể trong BoGo v1.
- [M] (craft) PRD-COMM-E1-S1: Hợp đồng ERP-link vẫn liệt kê 'order→SALES' dù SALES đã bị loại (DEC-45)  · fix: Xoá `order` khỏi danh sách `entity_type` ban đầu; cập nhật ví dụ thành `entity_type: shift|task`; bỏ dòng `order→SALES`. Nếu PO có ý định thêm loại thực thể khác (vd tờ trình từ APPROVAL), ghi rõ tên PRD sở hữu ở đây.
- [M] (craft) PRD-COMM-E1-S7: Lỗi đánh máy 'AI gán' trong AC gán màu — đội build đọc thành tính năng AI  · fix: Sửa thành 'chạm vào màu thấy ai gán, lúc nào' (chữ thường).
- [M] (craft) PRD-HR-E1-S1: User story dùng role name 'giao nhận / xử lý / bán hàng' — đặc ngữ Cleanmatic cũ, không tồn tại trong BoGo  · fix: Thay tất cả 'nhân viên giao nhận hoặc nhân viên xử lý' bằng 'nhân viên (STAFF)' trong câu 'Là...'. Trong PRD-HR-E2-S1:40, xoá ví dụ '(giao nhận, xử lý, bán hàng)' hoặc thay bằng ví dụ phù hợp SME đa ngành, vd: '(văn phòng, kho vận, kinh doanh)'.
- [M] (craft) PRD-HR-E1-S2: User story dùng 'quản lý hệ thống' thay vì persona chuẩn của spec  · fix: Thay 'quản lý hệ thống' bằng 'ORG_ADMIN' ở cả hai story: PRD-HR-E1-S2 dòng 38, PRD-HR-E2-S1 dòng 39. Frontmatter personas của cả hai đã đặt đúng — chỉ cần sửa câu user story.
- [M] (craft) PRD-HR-E2: Epic PRD-HR-E2 dẫn link đến PRD-FINANCE đã bị loại khỏi BoGo  · fix: Trong PRD-HR-E2:40, xoá dòng 'Liên kết phiếu chi' và thay bằng: 'v1 không chi lương tự động; ORG_ADMIN xuất Excel bảng lương (PRD-HR-E2-S2) để kế toán chi qua ngân hàng.' Trong PRD-HR-E2-S1:50, xoá tham chiếu PRD-FINANCE-E3 vì hoa hồng/bán hàng đã ngoài phạm vi BoGo.
- [M] (craft) PRD-PLATFORM-E1-S1: Ghi chú thiết kế của story đã approved vẫn nêu 'đề xuất — PO chốt khi duyệt PRD' cho hai mốc đã nằm trong AC được phê duyệt  · fix: Xoá câu 'Các mốc khoá ghi 30 ngày / giữ dữ liệu 60 ngày là đề xuất — PO chốt khi duyệt PRD' khỏi design note. Story đã approved, các mốc đã nằm trong AC được phê duyệt — ghi chú này không còn đúng và gây nhầm lẫn về trạng thái quyết định.
- [M] (craft) PRD-REPORT: PRD-REPORT dẫn con số '≤ 30 phút hành chính/ngày/hub' như ngưỡng BRD-G2 nhưng con số này không có trong BRD  · fix: Sửa dòng 78 thành: '—nối thẳng BRD-G2 (giảm ≥ 50% thời gian xử lý hành chính; con số 30 phút/ngày/hub là ước tính từ DEC, đo tại pilot)' để rõ nguồn gốc con số.
- [M] (craft) PRD-REPORT: Phần 'Vai trò & ranh giới DRY' của PRD-REPORT liệt kê 5 phân hệ đã bị loại như phân hệ đang hoạt động mà không có cảnh báo  · fix: Thêm callout cảnh báo ngay trên section 'Vai trò & ranh giới DRY': '> ⚠️ NỘI DUNG DƯỚI ĐÂY MÔ TẢ PHÂN HỆ CLEANMATIC ĐÃ LOẠI — chỉ giữ làm lịch sử; phân hệ kế nhiệm: PRD-TASK-E4.'
- [M] (craft) PRD-RESOURCE: PRD-RESOURCE frontmatter ghi 'approved' nhưng tiêu đề trạng thái trong thân ghi '🔄 Draft'  · fix: Chuẩn hóa về một chỗ: nếu PO đã ký duyệt nội dung spec nhưng chưa build, giữ status: approved và đổi prose thành '✅ Approved — phân hệ v2, horizon next. Build chỉ khởi động sau khi pilot v1 đạt cổng (DEC-62).' Nếu thực sự chưa duyệt, đổi frontmatter thành status: draft.
- [M] (craft) PRD-SUPPORT: Sign-off của PRD-SUPPORT sao chép nguyên văn ghi chú từ PRD-PLATFORM kể cả các open item thanh toán không liên quan  · fix: Thay notes của PRD-SUPPORT bằng nội dung phù hợp: 'Giá trị chốt-trước-sprint: nhà cung cấp hạ tầng chat hỗ trợ (dịch vụ thuê ngoài hoặc build — xem E1-S2 design note); giờ trực SLA ≤ 15 phút (PO chốt khi vận hành).'
- [M] (craft) PRD-SUPPORT-E1-S2: Giá trị SLA '≤ 15 phút' ghi là 'đề xuất, PO chốt' ngay trong AC đã approved — không kiểm được  · fix: PO quyết định một con số hoặc để trống và ghi rõ 'TBD — chốt trước sprint'. Cách viết AC: 'Phản hồi đầu tiên của người hỗ trợ trong ≤ [X] phút giờ hành chính (8h–18h T2–T6).' — bỏ cụm 'đề xuất' khỏi AC vì AC là điều kiện nghiệm thu, không phải ghi chú mở.
- [M] (market) BRD: Fiine PRO 59k có chấm công + giao việc + chat — khoảng cách giá 30k/user/tháng chưa được biện hộ đủ  · fix: Thêm vào BRD một dòng so sánh Fiine rõ ràng hơn: BoGo khác ở độ sâu tính lương (payroll rules + BHXH), tích hợp HĐĐT/eContract pháp lý, và AI trên kho tài liệu tổ chức — những thứ Fiine không có. Nếu những điểm đó đúng và được kiểm chứng ở pilot, giá 89k có thể bảo vệ được; nếu không, cần xem lại định giá.
- [M] (market) BRD: Rào cản 'đổi từ Zalo' được ghi nhận là rủi ro nhưng không có cơ chế giảm chuyển đổi cụ thể trong spec  · fix: Thêm vào BRD hoặc PRD-COMM một chiến lược chuyển đổi cụ thể: ví dụ ORG_ADMIN có thể bật 'chế độ song song Zalo' (thông báo giao việc gửi đồng thời qua Zalo trong tuần đầu), hoặc thiết kế onboarding STAFF bắt buộc qua app (chấm công GPS là hook tự nhiên vì không làm được qua Zalo). Đây là quyết định product — cần PO chốt trước pilot.
- [M] (market) BRD: Mục tiêu 100 tổ chức / 3,2 tỷ ARR chưa được đối chiếu với năng lực bán hàng thực tế của team  · fix: Bổ sung vào BRD một dòng năng lực bán hàng: số nhân sự GTM + CS hiện có, số tổ chức tối đa có thể phục vụ song song, và mốc nào sẽ tuyển thêm. Nếu mô hình hoàn toàn self-service (không cần hands-on), nêu rõ điều kiện để tin được điều đó — hiện chưa có story nào xây onboarding automation đủ mạnh để loại bỏ nhu cầu can thiệp tay.
- [M] (market) BRD: So sánh giá với 1Office trong BRD dùng con số cũ — giá thực tế 1Office all-in-one là 100k/user (Standard/Pro) hoặc 150k/user (gói đầy đủ)  · fix: Cập nhật mục Mô hình doanh thu BRD: thêm ghi chú rằng 1Office yêu cầu cam kết tối thiểu 1 năm, BoGo chu kỳ tháng là lợi thế cho SME nhỏ chưa chắc chắn. Đồng thời bỏ lập luận 'giá thấp hơn' như điểm khác biệt chủ đạo — thay bằng 'chu kỳ linh hoạt + tất cả trong một app'.
- [M] (market) PRD-AI-E2-S4: S4 đào tạo nội bộ (should) mở thêm thị trường LMS — phân tán focus và giảm moat AI cốt lõi  · fix: Hạ S4 xuống horizon next hoặc later. Tập trung horizon now vào S1 + S2 + S5 (onboarding + hỏi đáp + tra cứu HR) — đây mới là 'AI gắn dữ liệu vận hành' thực sự. S4 có thể quay lại khi kho tài liệu tổ chức đã có dữ liệu thật từ PRD-RESOURCE (v2).
- [M] (market) PRD-AI-E2-S4: Fastdo fTrain (đào tạo + kiểm tra + chứng chỉ) đã là module riêng trên thị trường — AI-E2-S4 không tạo được lợi thế cạnh tranh trong mảng LMS  · fix: Xem lại lý do giữ S4 (should) trong MVP: nếu trigger mua 'đào tạo nội bộ' chưa kiểm chứng tại pilot thì đẩy S4 xuống horizon next cùng với RESOURCE (kho tài liệu — nền tảng cần có trước để S4 hoạt động đúng). Nếu giữ, cần định vị rõ điểm khác biệt so với Fastdo fTrain là gì (tích hợp dữ liệu vận hành? AI sinh đề từ quy trình thật của tổ chức?) và đưa vào competitive table của PRD-AI.
- [M] (market) PRD-COMM-E1-S1: Voice message lưu 'suốt vòng đời hội thoại' tạo COGS audio storage không được tính trong mô hình kinh tế đơn vị của BRD  · fix: Thêm vào mô hình unit economics của BRD ước tính riêng cho COGS storage (audio + file đính kèm) tách khỏi COGS compute. Hoặc thêm chính sách lưu trữ vào COMM-E1-S1: ví dụ giữ voice message tối đa 90 ngày (sau đó xoá tự động), tương tự Zalo Business. Điều này vừa kiểm soát COGS vừa phù hợp thói quen người dùng Việt Nam (không kỳ vọng voice message lưu vĩnh viễn).
- [M] (market) PRD-PLATFORM: PLATFORM_ADMIN chỉ có dashboard mức-dùng (xem) — không có công cụ chủ động can thiệp vào vòng đời tổ chức để đạt G1  · fix: Thêm vào E1 (PLATFORM_ADMIN vòng đời) tối thiểu hai AC: (1) danh sách tổ chức trial sắp xếp theo điểm ưu tiên outreach (dựa trên % activation + ngày còn lại + số nhân viên); (2) ít nhất một hành động nhanh từ dashboard (gửi email khuyến khích gia hạn / chuyển gói với template mặc định, không cần mở tool ngoài). Điều này trực tiếp hỗ trợ đội bán hàng đạt BRD-G1.
- [M] (product) PRD-AI-E2-S2: Rủi ro NĐ13 được chấp nhận nhưng điều kiện rà soát pháp lý không phải AC — không có cổng kiểm tra trước go-live  · fix: Thêm một AC cứng vào S2 (hoặc vào điều kiện hoàn thành E2): 'Trước go-live pilot, có văn bản xác nhận của tư vấn pháp lý rằng việc gửi dữ liệu nhân sự qua API LLM bên thứ ba là hợp lệ theo NĐ 13/2023 (hoặc đã áp dụng biện pháp che PII/DPA phù hợp).' Điều này biến 'ghi chú cảnh báo' thành cổng nghiệm thu thực sự.
- [M] (product) PRD-APPROVAL-E1-S3: S3 phụ thuộc 'rà pháp lý trước khi bật' camera ký duyệt nhưng không có dependency hay story theo dõi việc này  · fix: Thêm một task pháp lý explicit vào điều kiện DoD của APPROVAL-E1-S3: 'Xác nhận của tư vấn pháp lý rằng vector khuôn mặt dùng cho ký duyệt nằm trong phạm vi consent NĐ13 đã thu thập ở chấm công, hoặc xác định cần thu consent bổ sung'. Nếu consent phải thu bổ sung, thêm story onboarding tương ứng.
- [M] (product) PRD-APPROVAL-E1-S4: Nhắc hạn & leo thang (S4) là should trong khi đây là điều kiện cần thiết để lời hứa 'không tờ trình nào mất tích' thành sự thật  · fix: Nâng moscow của S4 lên must. Nếu size S và phạm vi đơn giản (nhắc + leo thang theo sơ đồ tổ chức sẵn có), đây không phải gánh nặng build lớn và là điều kiện để giá trị cốt lõi không rỗng tuếch.
- [M] (product) PRD-COMM: COMM ủy thác push notification cho 'hạ tầng thông báo nội bộ chung' nhưng không có PRD hoặc story nào trong toàn spec build hạ tầng đó  · fix: Thêm story vào PRD-PLATFORM (hoặc PRD-COMM nếu phù hợp) cho 'Hạ tầng thông báo nội bộ chung' (in-app notification + push FCM/APNs): ai sở hữu, AC tối thiểu (delivery ≤5 giây khi online, queue khi offline), ai tiêu thụ (COMM, TASK, HR). Không có story này, toàn bộ notification AC trên spec là giả định, không phải cam kết.
- [M] (product) PRD-COMM: Thông báo per-event (giao việc, duyệt phép, bảng lương) được uỷ thác cho 'hạ tầng thông báo chung' nhưng PRD-COMM dòng 82 đánh rõ đây KHÔNG thuộc COMM — và không có PRD nào sở hữu hạ tầng này  · fix: Thêm một story vào PRD-PLATFORM hoặc tạo epic kỹ thuật riêng: xây hạ tầng push notification xuyên suốt (FCM/APNs cho mobile, WebSocket/SSE cho web) phục vụ mọi phân hệ. Story này là infrastructure blocker của ít nhất 8 story đã approved có AC thông báo. Gán ownership rõ (PRD + team) trước khi sprint.
- [M] (product) PRD-COMM-E1: EXEC được liệt kê trong personas của E1 và E2 nhưng không có story nào giải quyết job-to-be-done riêng của EXEC trong COMM  · fix: Hoặc xoá EXEC khỏi personas COMM nếu thực sự không có story riêng (trung thực về scope), hoặc thêm AC vào E2-S1 cho EXEC broadcast toàn tổ chức với thống kê đọc — đây là job EXEC thực sự cần: 'gửi thông điệp toàn công ty và biết ai đã đọc'.
- [M] (product) PRD-COMM-E1-S4: S4 (tạo việc từ tin nhắn) là 'must' nhưng phụ thuộc hoàn toàn vào TASK-E1 sẵn sàng — nếu TASK trượt thì 'must' COMM thành stub không dùng được  · fix: Tách S4 thành 2 phần: (a) điểm vào UI 'Tạo việc từ tin' — có thể build và ship độc lập (show form, nhập tiêu đề + người nhận); (b) liên kết hai chiều tin↔việc + mang ngữ cảnh từ TASK — phụ thuộc TASK-E1. Phần (a) là must, phần (b) là fast-follow sau khi TASK-E1 xong. Cách này giữ được giá trị cốt lõi mà không chặn go-live COMM.
- [M] (product) PRD-COMM-E1-S5: Poll 'ẩn danh' hệ thống vẫn lưu liên kết người–phiếu — khoảng cách giữa cam kết và thực tế tạo rủi ro niềm tin  · fix: Thêm AC: trước khi tạo poll ẩn danh, hiện modal giải thích rõ 'ẩn danh = không ai xem được ai bầu gì; hệ thống chỉ lưu 1 phiếu/người để chặn bầu trùng, không lộ danh tính'. Không dùng chữ 'nặc danh tuyệt đối' nhưng phải spec màn hình giải thích — hiện tại AC chỉ ghi 'ghi trong điều khoản dữ liệu nội bộ' (design note) mà không có AC bắt buộc.
- [M] (product) PRD-HR-E1-S1: Luồng đăng ký khuôn mặt mâu thuẫn: ORG_ADMIN upload ảnh, nhưng NĐ13 yêu cầu chính nhân viên ký đồng ý — không story nào mô tả kịch bản này  · fix: Làm rõ luồng chuẩn: ORG_ADMIN tạo hồ sơ trước (không có vector); nhân viên nhận link/thông báo, tự mở app, tự chụp khuôn mặt và ký đồng ý điện tử — SAU ĐÓ mới có thể chấm công. Thêm AC cho luồng này vào E1-S6 hoặc E1-S1, xóa dòng 'ORG_ADMIN upload ảnh' để tránh xây nhầm.
- [M] (product) PRD-HR-E1-S6: ORG_ADMIN upload ảnh nhân viên để đăng ký khuôn mặt nhưng NĐ 13/2023 yêu cầu chính chủ thể dữ liệu ký đồng ý — luồng đang vi phạm quyền consent  · fix: Sửa luồng đăng ký khuôn mặt thành: (1) ORG_ADMIN tạo hồ sơ nhân viên và gửi link đăng ký sinh trắc cho chính nhân viên đó qua SMS/email; (2) nhân viên tự chụp ảnh, tự đọc và ký consent điện tử trên thiết bị của mình; (3) vector được tạo và lưu chỉ sau khi nhân viên xác nhận. ORG_ADMIN không bao giờ là người thực hiện bước thu thập sinh trắc. Sửa AC trong PRD-HR-E1-S1 và PRD-HR-E1-S6 tương ứng.
- [M] (product) PRD-HR-E2-S2: Story tạo bảng lương gộp 4 hành vi độc lập vào một story, làm mờ ranh giới nghiệm thu và tăng rủi ro trễ toàn bộ chu trình lương  · fix: Tách E2-S2 thành ít nhất 2 story: (A) tạo nháp + approve + xuất Excel (core payroll path); (B) hiệu chỉnh thủ công + khóa đồng thời + cảnh báo tạo lại + gắn cờ muộn (edge-case payroll operations). Story A là must và phải hoàn thành trước; story B là should, có thể vào sprint sau mà không chặn story A.
- [M] (product) PRD-HR-E2-S2: Bảng lương approve xong không có luồng thông báo/chi lương điện tử — ORG_ADMIN phải tự chuyển khoản từng người qua internet banking  · fix: Spec v1 có thể giữ 'không tự chuyển tiền' nhưng cần thêm AC cho ORG_ADMIN xuất file chuẩn định dạng ngân hàng (Vietcombank, MB Bank, BIDV batch payment template) trực tiếp từ bảng lương đã approve — thay vì chỉ Excel thô. Điều này đưa BoGo đến 'click để chi lương hàng loạt qua ngân hàng' mà không cần tích hợp API ngân hàng, đóng khoảng cách với lời hứa BRD-G2.
- [M] (product) PRD-HR-E2-S4: KPI bonus được nâng lên must trong MVP khi giả định nhu cầu chưa được kiểm chứng và nguồn dữ liệu quá hẹp sau khi SALES/FINANCE bị loại  · fix: Hạ E2-S4 xuống moscow: should và thêm điều kiện cổng: chỉ nâng lại must sau khi ít nhất 2 tổ chức pilot xác nhận họ cần KPI-linked bonus tính từ dữ liệu TASK+chấm công. Ghi rõ trong story: 'nếu không đủ pilot demand → build sau activation baseline'.
- [M] (product) PRD-SUPPORT-E1-S2: Hạ tầng chat hỗ trợ chưa xác định nhà cung cấp/giải pháp — spec tự nhận biết nhưng không resolve trước go-live now  · fix: Trước sprint, PO cùng đội build đánh giá tối thiểu 2–3 giải pháp chat hỗ trợ (ví dụ: Crisp, Chatwoot self-hosted, hoặc tự xây trên COMM infra có rào kỹ) theo tiêu chí: COGS/tháng, ranh giới dữ liệu tenant, có SLA queue, có CSAT. Chốt thành một DEC rõ ràng và thêm story hạ tầng riêng. Không để quyết định này ngầm trong story M.
- [M] (product) PRD-TASK-E1-S3: Story công việc định kỳ có mức độ phức tạp kỹ thuật nặng cho một story moscow:should trong MVP  · fix: Tách E1-S3 thành hai phần rõ ràng có AC riêng và cột milestone riêng: (1) lập mẫu + tạo công việc từ mẫu (đơn giản, đủ dùng); (2) job chống trùng + bù kỳ lỡ. Phần (1) có thể vào MVP; phần (2) là horizon next. Ghi chú thiết kế của chính story đã gợi ý hướng này (dòng 51) nhưng chưa được phản ánh vào moscow/scope.
- [M] (product) PRD-TASK-E3-S2: KPI công việc được tham chiếu vào thưởng lương nhưng rủi ro cốt lõi (nhân viên gian lận trạng thái) chưa được đặt ra  · fix: Thêm ghi chú thiết kế cảnh báo rõ rằng KPI công việc chỉ đáng tin khi tỉ lệ công việc 'cần duyệt' đủ cao (khuyến nghị DEPT_MGR bật cần-duyệt cho các công việc quan trọng). Cân nhắc thêm AC: KPI theo mỗi nhân viên hiện tỉ lệ % công việc có bằng chứng (file/bình luận đính kèm khi chuyển Chờ duyệt) vs không có bằng chứng — để DEPT_MGR tự đánh giá chất lượng số liệu.
- [M] (product) PRD-TASK-E4-S3: E4-S3 (danh mục báo cáo) giải quyết bài toán điều hướng thay vì bài toán thông tin — gold-plating MVP  · fix: Đánh moscow:wont cho E4-S3 trong MVP. Đưa vào horizon next khi số phân hệ có báo cáo tăng lên (APPROVAL, RESOURCE). Trong MVP, mỗi phân hệ tự có link báo cáo là đủ dùng.
- [M] (product) PRD-TASK-E4-S5: E4-S5 ngầm đưa hạ tầng email transactional vào MVP mà không có story nào trong PRD khác đặt nền  · fix: Bổ sung phụ thuộc (depends_on) của E4-S5 vào một story hoặc task hạ tầng email transactional. Nếu PRD-PLATFORM chưa có story này, tạo story hạ tầng email ở PRD-PLATFORM (dùng chung cho toàn hệ thống: reset mật khẩu, thông báo, báo cáo). Hoặc nếu muốn đơn giản hơn, cắt kênh email trong E4-S5 MVP — chỉ gửi in-app, email là horizon next.
- [M] (spine) BRD: Ràng buộc web parity (DEC-66) không kèm bất kỳ yêu cầu nào về khả năng tiếp cận (accessibility)  · fix: Thêm ràng buộc vào BRD hoặc PRODUCT.md: 'Giao diện web tuân thủ tối thiểu WCAG 2.1 cấp AA cho các luồng nghiệp vụ cốt lõi (đăng nhập, chấm công web, giao việc, chat)'. Thêm AC vào story web của từng phân hệ core (HR-E1-S1 web fallback, TASK-E1-S2, COMM-E1-S1) kiểm tra điều hướng bàn phím và tương phản màu. Không cần toàn bộ spec — chỉ các luồng người dùng STAFF và ORG_ADMIN bắt buộc qua hàng ngày.
- [M] (spine) PRD-PLATFORM: North Star metric chỉ đo được từ phía PLATFORM_ADMIN — ORG_ADMIN không thấy vị trí tổ chức mình trên thang kích hoạt  · fix: Tách AC 'ORG_ADMIN thấy tỉ lệ nhân viên dùng hằng tuần so với mốc 60%' ra khỏi AI-E2-S5 (should) và đặt vào PLATFORM-E1-S1 hoặc tạo một widget/widget nhỏ trong màn hình chính của ORG_ADMIN (must). Đây là pull mechanism đơn giản, không cần AI — chỉ cần hiển thị một con số.
- [M] (spine) PRD-PLATFORM-E1-S2: PLATFORM_ADMIN chỉ có công cụ thao tác từng tổ chức một — thiếu công cụ vận hành hàng loạt khi nền tảng có 100 tổ chức  · fix: Mở rộng E1-S2 hoặc thêm story mới cho PLATFORM_ADMIN: (1) thông báo hàng loạt theo segment (tổ chức active / trial / sắp hết hạn) với nội dung tùy chỉnh; (2) xuất bảng sức khoẻ tổ chức ra CSV (tên, gói, mức dùng tuần, ngày cuối active) để đội kinh doanh phân tích. Scope nhỏ: chỉ xuất + gửi thông báo; không cần CRM đầy đủ.
- [M] (tech) PRD-AI-E2: Không có AC nào yêu cầu kiểm thử rằng context window LLM không chứa dữ liệu của hai org khác nhau trong cùng một request  · fix: Thêm một AC vào S6: 'Trước go-live, đội build chạy test xuyên tổ chức: gửi đồng thời câu hỏi từ 2 org khác nhau, xác nhận prompt gửi đến LLM của mỗi request chỉ chứa dữ liệu của đúng org đó, không lẫn tổ chức kia — log kiểm tra được lưu ít nhất 30 ngày.' Test case này phải pass trước khi mở cho org thứ 2 trở lên.
- [M] (tech) PRD-AI-E2-S2: S2 không làm rõ dữ liệu lương của chính người hỏi có nằm trong phạm vi trả lời không  · fix: Bổ sung một AC làm rõ biên giới: 'Phiên bản MVP trả lời về phép, bảng công, trạng thái xin nghỉ của chính người hỏi. Dữ liệu lương (số tiền, bảng lương) KHÔNG nằm trong phạm vi MVP — nếu người dùng hỏi về lương, trợ lý hướng dẫn xem tại màn hình Bảng lương trong phân hệ HR.' Nếu PO muốn đưa lương vào, cần quyết định rõ và ghi vào một DEC riêng.
- [M] (tech) PRD-AI-E2-S3: S3 không có AC cho trường hợp phân hệ Giao việc (PRD-TASK) không phản hồi  · fix: Thêm một AC lỗi vào S3: 'Giả sử phân hệ Giao việc không phản hồi khi trợ lý truy vấn, thì trợ lý thông báo rõ ràng bằng tiếng Việt và gợi ý mở trực tiếp màn hình Giao việc — không trả về danh sách rỗng mà không giải thích.' Chuẩn hóa pattern này sang S2 (PRD-HR).
- [M] (tech) PRD-AI-E2-S6: AC 'thay đổi áp dụng ngay' khi cập nhật kho tri thức không định nghĩa được 'ngay' là bao lâu  · fix: Sửa AC thành: 'Sau khi ORG_ADMIN tải lên/cập nhật/gỡ tài liệu, hệ thống hoàn tất đánh chỉ mục trong tối đa [X phút] và trợ lý dùng phiên bản mới nhất cho mọi câu hỏi sau đó. Trong thời gian đánh chỉ mục, trợ lý hiển thị chỉ thị kho đang cập nhật để người dùng biết câu trả lời có thể chưa phản ánh tài liệu vừa thay đổi.' Chốt X dựa trên cỡ tài liệu tối đa cho phép upload.
- [M] (tech) PRD-AI-E2-S6: DEC-59 yêu cầu migrate tài liệu từ cơ chế tạm sang RESOURCE nhưng không có story nào sở hữu việc đó  · fix: Tạo một story migration tại PRD-RESOURCE với AC: 'Tài liệu đã tải trực tiếp qua AI-E2-S6 (giai đoạn now) được tự động chuyển sang kho RESOURCE của đúng tổ chức đó khi RESOURCE ra mắt, giữ nguyên ngày hiệu lực và tên tài liệu; ORG_ADMIN nhận thông báo xác nhận migration hoàn tất; không mất dữ liệu, không lẫn tổ chức.' Story này nên là must để không chặn v2 go-live.
- [M] (tech) PRD-APPROVAL-E1-S2: Luồng phê duyệt lấy cơ cấu tổ chức từ PRD-HR nhưng không có AC kiểm thử khi sơ đồ tổ chức thay đổi trong khi tờ trình đang chờ duyệt  · fix: Thêm AC: 'Nếu trong khi tờ trình đang chờ duyệt cấp X, người duyệt cấp X đổi vai trò không còn thẩm quyền → tờ trình gắn cờ CẦN XỬ LÝ và thông báo ORG_ADMIN kèm tờ trình liên quan; ORG_ADMIN chỉ định lại người duyệt hoặc điều chỉnh mẫu luồng; tờ trình không tự chuyển trạng thái cho đến khi có hành động của ORG_ADMIN.' Trường hợp phòng ban bị giải thể cũng cần AC tương tự.
- [M] (tech) PRD-COMM-E1-S1: AC 'không rơi tin khi mất mạng' không định nghĩa giới hạn queue phía client — thời gian offline dài hoặc nhiều tin gây hành vi không rõ  · fix: Thêm AC: (1) Client giữ hàng đợi tin chưa gửi tối thiểu X phút (đề xuất: 2 giờ) hoặc cho đến khi app bị force-quit; (2) Tối đa Y tin trong hàng đợi (đề xuất: 50 tin); vượt giới hạn thì hiện cảnh báo 'Đang gặp sự cố kết nối — tin chưa được gửi'; (3) Khi nối lại, tin được gửi đúng thứ tự tạo ra; (4) Sau khi gửi, hiện trạng thái ĐÃ GỬI trên từng tin trong hàng đợi.
- [M] (tech) PRD-COMM-E1-S2: AC cam kết SLA ≤5 phút cập nhật nhóm hệ thống nhưng ghi chú thiết kế ngay trong story cảnh báo SLA này có thể không đạt nếu HR chạy batch  · fix: Tách SLA thành hai AC: (1) Nếu HR phát sự kiện real-time (≤1 phút per DEC-34): COMM cập nhật nhóm trong ≤5 phút sau khi nhận sự kiện — testable khi chạy stub HR. (2) Nếu HR chạy batch: COMM xử lý batch trong ≤15 phút sau khi job HR hoàn thành, ghi log cụ thể thời điểm nhận. Bổ sung AC: trong khoảng trễ tối đa đó, nhân viên chuyển phòng không nhận tin nhắn nhóm đích mới — hệ thống không để lọt.
- [M] (tech) PRD-COMM-E1-S5: Poll 'ẩn danh' chỉ được bảo vệ ở lớp giao diện người dùng — không có AC ngăn admin xem liên kết người-phiếu ở tầng dữ liệu  · fix: Thêm AC: (1) Không có màn hình nào trong hệ thống (bao gồm admin dashboard của ORG_ADMIN) hiện liên kết người-phiếu cho poll ẩn danh; (2) API không trả về user_id cùng lựa chọn cho poll ẩn danh kể cả khi gọi bằng token ORG_ADMIN; (3) PLATFORM_ADMIN chỉ thấy tổng hợp ở cấp tổ chức (số poll, tổng phiếu), không thấy nội dung phiếu cụ thể; (4) Thiết kế kỹ thuật tách bảng dedup (user_id + poll_id) khỏi bảng kết quả — bảng dedup không expose qua API nào.
- [M] (tech) PRD-COMM-E1-S8: Nhắc hẹn lặp (S8) không có AC về idempotency khi server khởi động lại — nguy cơ gửi trùng hoặc bỏ sót  · fix: Thêm AC: 'Mỗi lần nhắc hẹn kích hoạt chính xác một lần cho mỗi thời điểm — kể cả khi server khởi động lại; scheduler dùng idempotency key = (reminder_id + scheduled_time); nếu key đã gửi thì bỏ qua, ghi log.' Tham chiếu cơ chế tương tự E2-S1 (broadcast hẹn giờ).
- [M] (tech) PRD-HR-E1: Không có AC nào về web parity cho toàn cụm HR (DEC-66) — đặc biệt nghiêm trọng với chức năng quản lý  · fix: Thêm vào ít nhất 3 story quản lý trọng yếu (PRD-HR-E2-S2, PRD-HR-E1-S6, PRD-HR-E3-S4) một AC web parity theo pattern: 'Mọi chức năng trong story này khả dụng đầy đủ trên trình duyệt web (không chỉ mobile app); layout bảng dữ liệu trên web hiển thị đủ cột không bị cắt. [Với S1-S1/S2 — chấm công khuôn mặt/GPS là ngoại lệ mobile-only; web hiển thị trạng thái chấm công và cho phép ORG_ADMIN rà soát/chỉnh sửa.]'
- [M] (tech) PRD-HR-E1-S1: AC không định nghĩa hành vi khi nhân viên chưa được phân ca — nút chấm công sẽ làm gì  · fix: Thêm AC: 'Nếu nhân viên không có ca nào được phân công cho ngày hiện tại → nút Chấm công vào bị khoá (disabled) kèm thông báo: [Bạn chưa được phân ca hôm nay. Liên hệ trưởng phòng.] Nhân viên không thể chấm công khi không có ca được giao.'
- [M] (tech) PRD-HR-E1-S1: AC 'thông báo lỗi rõ ràng' là untestable — không phân biệt các loại lỗi nhận diện  · fix: Tách AC thành 3 trường hợp kiểm thử được: (1) 'Confidence < 50% → hiển thị: Ánh sáng có thể quá tối — hãy di chuyển đến vùng sáng hơn rồi thử lại'; (2) 'Confidence 50–94% (khuôn mặt nhận ra nhưng chưa đủ ngưỡng) → hiển thị: Khuôn mặt nhận diện chưa rõ — giữ yên và nhìn thẳng vào camera'; (3) 'Camera không khởi động được → hiển thị: Không mở được camera — thử lại hoặc dùng phương thức dự phòng.'
- [M] (tech) PRD-HR-E1-S1: Đồng bộ chấm công offline không có AC xử lý xung đột khi server đã có dữ liệu chấm công khác cho cùng khoảng thời gian (ví dụ: DEPT_MGR chỉnh thủ công trong lúc nhân viên offline)  · fix: Thêm AC vào HR-E1-S1 AC offline: 'Khi đồng bộ, nếu server đã có bản ghi chấm công cho cùng nhân viên + cùng ca + cùng khoảng thời gian (chỉnh thủ công): hệ thống gắn cờ XUNG ĐỘT, giữ cả hai bản ghi trong hàng đợi rà soát của DEPT_MGR thay vì ghi đè tự động; DEPT_MGR chọn bản ghi nào hợp lệ trước khi đưa vào bảng lương; log cả hai bản gốc.'
- [M] (tech) PRD-HR-E2-S2: Không có AC cho hành vi khi bảng lương được chạy đồng thời hai lần (duplicate run)  · fix: Thêm AC: 'Nếu bảng lương kỳ X đã tồn tại (dù là nháp hay đã duyệt) → bấm Tạo bảng lương kỳ X lần nữa sẽ hiển thị cảnh báo: Bảng lương kỳ này đã được tạo. Bạn có muốn TẠO LẠI không? (Thao tác này sẽ xoá bảng lương nháp hiện tại và tạo mới từ dữ liệu chấm công gốc.) Yêu cầu xác nhận trước khi reset; không cho tạo trùng tự động.'
- [M] (tech) PRD-HR-E3-S1: Cân bằng phép (leave balance) bị trừ ngay sau khi approve — không có AC cho trường hợp hủy/từ chối sau khi đã trừ  · fix: Thêm AC: 'Nhân viên có thể huỷ yêu cầu nghỉ phép đã được duyệt trong vòng [X giờ trước ngày nghỉ, cấu hình mặc định 24 giờ] → số ngày phép năm được hoàn lại tự động; DEPT_MGR nhận thông báo huỷ. Sau thời hạn đó, chỉ DEPT_MGR/ORG_ADMIN mới có thể huỷ — và phải ghi lý do, số ngày hoàn lại ghi vào log.'
- [M] (tech) PRD-PLATFORM-E1-S1: Ghi chú thiết kế của story đã approved vẫn ghi 'là đề xuất — PO chốt khi duyệt PRD' cho hai mốc quan trọng trong AC  · fix: PO cần xác nhận rõ ràng: (a) 30 ngày KHOÁ GHI và 60 ngày giữ dữ liệu là đã chốt → xoá câu 'là đề xuất' trong note và ghi lý do chốt vào DEC; hoặc (b) đây thực sự chưa chốt → sửa AC trong frontmatter thành placeholder [PO điền] và tái phê duyệt story.
- [M] (tech) PRD-PLATFORM-E2-S1: Pro-rata khi HẠ gói/giảm user chỉ nói 'hiệu lực kỳ kế tiếp, không hoàn tiền' — không có AC cho trường hợp tổ chức giảm user xuống dưới số user đang active  · fix: Thêm AC: 'Khi ORG_ADMIN hạ gói hoặc giảm số user xuống thấp hơn số user đang active → hệ thống CẢNH BÁO và yêu cầu ORG_ADMIN chọn tài khoản nào bị vô hiệu hóa; không tự cắt; gói chỉ hiệu lực kỳ kế tiếp sau khi ORG_ADMIN đã xử lý dư tài khoản hoặc xác nhận chấp nhận hệ thống tự cắt theo thứ tự lần đăng nhập gần nhất.'
- [M] (tech) PRD-PLATFORM-E2-S2: Lịch nhắc hạn (14/7/3/1 ngày) không có AC xử lý trường hợp tổ chức gia hạn điều kiện +16 ngày (trial 30 ngày) — mốc nhắc đầu tiên có thể trùng hoặc gần ngày đăng ký  · fix: Thêm AC vào E2-S2: 'Lịch nhắc hạn (14/7/3/1 ngày) chỉ áp dụng cho GÓI TRẢ PHÍ, không cho giai đoạn trial; trong trial chỉ gửi thông báo gia hạn điều kiện (E1-S1 AC4) và nhắc một lần ≤3 ngày trước khi trial hết hạn; chuyển sang gói trả phí mới bắt đầu lịch nhắc 14/7/3/1 ngày tính từ hạn gói trả phí.'
- [M] (tech) PRD-PLATFORM-E2-S4: Story E2-S4 (horizon: next) nằm trong epic E2 (horizon: now) — tạo nhập nhằng về thứ tự build  · fix: Hoặc: (a) tách E2-S4 ra epic riêng PRD-PLATFORM-E2b (horizon: next, depends_on PRD-RESOURCE-E1) để ranh giới sprint rõ ràng; hoặc (b) ghi chú DoD của epic E2 rõ: 'E2-S4 thuộc next, KHÔNG chặn E2 hoàn thành ở now' — đội build đọc được và không phụ thuộc nhau.
- [M] (tech) PRD-PLATFORM-E3-S2: Hợp đồng điện tử treo không có đường leo thang: chỉ gắn cờ PLATFORM_ADMIN nhưng không có AC xử lý hợp đồng chưa ký vô hạn  · fix: Thêm AC: 'Hợp đồng chưa ký sau [PO điền, ví dụ 30 ngày]: PLATFORM_ADMIN được phép [PO chọn: (a) tạm dừng tự động sau cảnh báo trước 7 ngày; hoặc (b) chỉ ghi nhận, không khoá]. Lý do và quyết định ghi vào DEC trước sprint E3.' Không để đội build tự chọn hành vi pháp lý.
- [M] (tech) PRD-REPORT-E1-S2: Các story PRD-REPORT (moscow: wont) vẫn chứa AC tham chiếu phân hệ và persona không tồn tại trong BoGo — nếu đọc nhầm sẽ implement sai  · fix: Vì toàn bộ cây PRD-REPORT đã được đánh moscow:wont và có banner DEC-48 rõ ràng, không cần sửa nội dung AC — nhưng nên thêm một NOTE đầu mỗi file REPORT (epic và story): 'Tham chiếu trong AC này (ZONE_MGR, SYS_ADMIN, PRD-SALES, PRD-FINANCE, PRD-INVENTORY, PRD-PAYMENT) là của hệ thống Cleanmatic đã loại — chỉ giữ file làm lịch sử. Xem PRD-TASK-E4 cho kế nhiệm BoGo.' Giảm rủi ro đọc nhầm.
- [M] (tech) PRD-RESOURCE-E1-S1: Quản lý phiên bản tài liệu không định nghĩa chính sách giữ/xoá lịch sử phiên bản — ảnh hưởng trực tiếp COGS lưu trữ  · fix: Thêm AC: 'Chính sách giữ phiên bản cấu hình được theo gói: mặc định giữ N bản gần nhất hoặc trong X tháng (PO chốt N và X khi duyệt sprint); phiên bản ngoài chính sách đánh dấu archived, ORG_ADMIN thấy rõ dung lượng lịch sử; archived tính vào hạn mức dung lượng của tổ chức (tham chiếu PLATFORM-E2-S4)'. Đây là điều kiện để bóc đúng COGS storage cho add-on.
- [M] (tech) PRD-RESOURCE-E1-S3: DEC-59 yêu cầu migrate tài liệu từ PRD-AI-E2-S6 vào kho RESOURCE khi v2 ra mắt nhưng không có story nào build công cụ migration này  · fix: Tạo story kỹ thuật PRD-RESOURCE-E1-S5 'Migration tài liệu từ AI-E2-S6 sang kho RESOURCE' (size S-M, can-be-should): AC gồm idempotent migration script, xác nhận từng tài liệu được mã hoá đúng khoá tổ chức, nguồn tri thức AI cập nhật sang bản mới, rollback plan nếu migration thất bại. Gắn vào điều kiện go-live của RESOURCE.
- [M] (tech) PRD-SUPPORT-E1-S1: AI hỗ trợ tuyến đầu phụ thuộc 'kho hướng dẫn chính thức BoGo' nhưng không có story nào build công cụ biên soạn và quản lý kho hướng dẫn đó  · fix: Thêm AC vào S1: 'PLATFORM_ADMIN cập nhật được nội dung kho hướng dẫn (thêm/sửa/xoá bài) mà không cần deploy; cập nhật có hiệu lực với AI trong ≤ X giờ'. Hoặc tạo story riêng PRD-SUPPORT-E1-S4 'Công cụ biên soạn kho hướng dẫn BoGo' (size S): PLATFORM_ADMIN quản lý bài hướng dẫn, tag theo tính năng/phân hệ, xem preview trả lời AI trước khi publish.
- [M] (tech) PRD-SUPPORT-E1-S2: Hạ tầng chat hỗ trợ được ghi nhận là 'hạng mục riêng chưa spec' ngay trong ghi chú thiết kế của story M  · fix: Trước sprint: (1) PO quyết build hay thuê ngoài hạ tầng chat; (2) nếu thuê ngoài — thêm AC 'tiêu chí nhà cung cấp (latency, data residency VN, giá/phiên, SLA uptime) và ghi tên đã chọn vào DEC'; (3) nếu build — tách story hạ tầng riêng size L. Cả hai trường hợp phải re-size S2 sau khi quyết.
- [M] (tech) PRD-SUPPORT-E1-S2: Hạ tầng chat hỗ trợ (SUPPORT) không được phép dùng COMM — nhưng spec không xác định hạ tầng thay thế cụ thể là gì, ai sở hữu, và thời điểm phải chốt trước sprint  · fix: Thêm AC kỹ thuật tối thiểu: 'Hạ tầng chat hỗ trợ phải: (1) tách biệt hoàn toàn với PRD-COMM để không vi phạm ranh giới tenant; (2) hỗ trợ gửi text + ảnh đính kèm ≤10MB; (3) lưu lịch sử phiên cho cả hai phía; (4) chọn giải pháp (tự build hoặc thuê dịch vụ như Crisp/Intercom/tương đương) và tài liệu hoá trước khi sprint bắt đầu — là điều kiện DoD của epic E1.' Thêm trường depends_on chỉ tới quyết định kiến trúc cụ thể.
- [M] (tech) PRD-TASK-E1-S3: Task bù kỳ lỡ khi hệ thống phục hồi không nêu rõ có gửi thông báo không  · fix: Thêm một clause vào AC dòng 29: "Task bù kỳ lỡ tạo ra KHÔNG gửi push notification tới người nhận (vì task đã quá hạn khi tạo); hệ thống chỉ ghi log và hiển thị badge cảnh báo trong danh sách task của người nhận với nhãn 'task trễ từ kỳ [ngày]."
- [M] (tech) PRD-TASK-E4-S1: Yêu cầu bảo mật "tuyệt đối không hiển thị dữ liệu tổ chức khác" không có AC kiểm thử ở tầng dashboard  · fix: Thêm một AC kiểm thử ranh giới đa tổ chức dành riêng cho dashboard: "Khi người dùng Tổ chức A mở dashboard, toàn bộ chỉ số nhân sự và công việc trả về đều thuộc Tổ chức A; không có dữ liệu của tổ chức khác dù API phân hệ nguồn có lỗi trả sai ngữ cảnh." Đây là AC nghiệm thu độc lập cho lớp trình bày.
- [M] (tech) PRD-TASK-E4-S3: Danh mục báo cáo tham chiếu "báo cáo của phân hệ Nhân sự" nhưng HR không có story định nghĩa danh sách báo cáo của mình  · fix: Thêm AC vào PRD-HR-E1-S4 (hoặc HR story phù hợp): "HR expose danh sách báo cáo chuẩn gồm: [liệt kê cụ thể, vd: Bảng chấm công theo kỳ, Bảng lương theo kỳ, v.v.] — mỗi báo cáo có ID, tên, quyền xem, và endpoint." E4-S3 `depends_on` AC này.

## LOW (22) — compact

- [L] (craft) PRD-AI-E1-S1: User story E1-S1 (wont/lịch sử) có prose body đặt tên Cleanmatic như sản phẩm còn hiệu lực  · fix: Thêm một dòng chú thích đầu phần ## User Story: `> 📦 Lịch sử Cleanmatic — story này đã wont (DEC-47). Không áp dụng cho BoGo.` Không cần sửa prose body.
- [L] (craft) PRD-COMM-E1-S2: Hai AC còn dùng 'hiện lỗi rõ ràng' mà không định nghĩa nội dung lỗi  · fix: Cụ thể hoá: ví dụ 'hiện thông báo lỗi inline bên dưới nút gửi, nội dung: "Bạn chỉ được gửi thông báo trong phạm vi [tên phòng/tổ chức của bạn]"' — một câu mẫu đủ để QA viết test case.
- [L] (craft) PRD-HR-E1-S1: 'Thông báo lỗi rõ ràng' trong AC chấm công không đủ điều kiện để kiểm thử  · fix: Viết lại: 'Thông báo lỗi phải nêu rõ lý do bằng tiếng Việt, vd: "Khuôn mặt không nhận diện được — vui lòng hướng về ánh sáng và thử lại" hoặc "Bạn đang ở ngoài khu vực làm việc". Không hiển thị mã lỗi kỹ thuật cho nhân viên.'
- [L] (craft) PRD-HR-E1-S1: User story body dùng job title Cleanmatic ('nhân viên giao nhận', 'nhân viên xử lý') không tồn tại trong BoGo  · fix: Sửa thành 'Là nhân viên (STAFF) làm việc tại địa điểm của tổ chức,' để khớp persona BoGo.
- [L] (craft) PRD-HR-E2: Phạm vi epic PRD-HR-E2 ghi 'nửa tháng hoặc tháng' mâu thuẫn với quyết định đã chốt 'chỉ tháng'  · fix: Sửa dòng 37 của PRD-HR-E2: thay 'nửa tháng hoặc tháng' bằng 'theo THÁNG (v1 chỉ hỗ trợ kỳ tháng — xem PRD-HR-E2-S1 AC4)'.
- [L] (craft) PRD-HR-E3-S4: Mục tiêu '2 giờ' trong câu 'Để...' không có AC kiểm chứng tương ứng  · fix: Thêm AC: 'Nếu yêu cầu chờ duyệt quá X giờ (cấu hình, gợi ý 2 giờ), hệ thống gửi nhắc nhở tự động cho DEPT_MGR.' Hoặc xoá khỏi câu Để và đưa vào chỉ số thành công PRD-HR nếu chỉ là mục tiêu pilot.
- [L] (craft) PRD-SUPPORT-E1: Mô tả phạm vi epic dùng 'chuyển tiếp thông minh' — tính từ không đo được  · fix: Thay 'chuyển tiếp thông minh' bằng 'chuyển tiếp kèm ngữ cảnh khi AI không giải được' — mô tả đúng cơ chế đã spec ở AC S1.
- [L] (market) BRD: Tanca mô hình one-time purchase tạo JTBD khác với subscription — segment 'tránh subscription dài hạn' chưa được nhận diện trong trigger mua  · fix: Tại pilot, khảo sát thêm câu hỏi: 'Điều gì khiến bạn chần chừ khi trả 89k/user/tháng?' — nếu 'ngại subscription' xuất hiện nhiều thì xem xét thêm tùy chọn trả năm (discount rõ, cam kết 12 tháng) hoặc gói starter giới hạn tính năng miễn phí để giảm rào cản thử nghiệm. Ghi nhận Tanca one-time model trong BRD như một positioning risk, không chỉ so sánh tính năng.
- [L] (product) PRD-AI-E1: 7 story E1 giữ `scope: in` dù `moscow: wont` — tín hiệu xây dựng mâu thuẫn  · fix: Đổi `scope: out` (hoặc `scope: wont`) cho tất cả 7 story E1 và epic E1 để khớp với `moscow: wont`. Thêm comment ngắn trong epic E1 giải thích đây là lịch sử Cleanmatic, không build.
- [L] (product) PRD-COMM-E1-S1: Các story COMM dùng framing mobile-first; DEC-66 yêu cầu web parity nhưng không có AC nào trong COMM verify web equivalent cho voice message và typing indicator  · fix: Thêm một AC vào S1 và S9: 'Trên web, voice message có đường thay thế tương đương (ví dụ: upload file âm thanh, hoặc record qua browser API nếu hỗ trợ); typing indicator hiển thị trên web như mobile'. Không cần spec chi tiết kỹ thuật nhưng phải có AC để đội build không tự cắt web.
- [L] (product) PRD-HR-E1-S3: AC expose endpoint cho ERP-link COMM được nhúng vào story phân công ca làm việc, che khuất mục tiêu chính của story  · fix: Chuyển AC endpoint này ra khỏi thân story E1-S3, đặt vào phần 'Ghi chú thiết kế' hoặc tạo một integration-contract riêng dưới dạng ghi chú kỹ thuật. Story E1-S3 chỉ nên chứa AC mà DEPT_MGR hoặc ORG_ADMIN có thể kiểm chứng từ góc người dùng.
- [L] (product) PRD-REPORT-E1-S1: STAFF được liệt kê là persona của các story dashboard/báo cáo gộp chéo — sai JTBD  · fix: Nếu REPORT được tái sinh cho BoGo, xoá STAFF khỏi personas của E1 và E2. Persona đúng là EXEC (bức tranh toàn cảnh theo thời gian thực — khớp VISION:76) và DEPT_MGR (xem tiến độ phòng mình). ORG_ADMIN phù hợp với trung tâm báo cáo, không nhất thiết với dashboard gộp chéo.
- [L] (product) PRD-RESOURCE-E1-S4: Bình luận trên tài liệu (S4) là should — ưu tiên phù hợp, nhưng cần rõ ràng về giá trị khi kho có thể rỗng  · fix: Không cần thay đổi moscow của S4. Nhưng PO nên xác định rõ: cơ chế kéo nào sẽ giúp kho không rỗng trước khi bắt đầu build S3/S4 — nếu chỉ dựa vào 'tài liệu đính kèm tờ trình PHẢI từ kho' thì hiệu quả phụ thuộc APPROVAL cũng được ra mắt và có người dùng, tức là hai v2 đều phải thành công.
- [L] (product) PRD-TASK-E4: E4 (dashboard tổng quan) đặt trong PRD-TASK nhưng phụ thuộc nặng vào dữ liệu PRD-HR — ranh giới sở hữu sản phẩm mờ  · fix: Ghi rõ trong E4 header hoặc ghi chú thiết kế E4-S1: contract dữ liệu HR mà E4 phụ thuộc (endpoint tổng hợp chấm công theo phòng ban) do PRD-HR sở hữu và maintain; thay đổi contract phải được cả TASK và HR owner đồng ý. Đây là build-coordination note, không cần tạo story mới.
- [L] (spine) PRD-REPORT: PRD-REPORT vẫn ghi owner 'Cleanmatic' — dấu hiệu pivot DEC-45 chưa được dọn dẹp hoàn toàn  · fix: Cập nhật PRD-REPORT: owner → 'Nhóm sản phẩm BoGo', approved_by → 'Product Owner (đại diện nhóm sản phẩm BoGo)' (giữ note DEC-45/DEC-48 trong body). Một dòng sửa, không cần re-approve vì PRD đã là wont.
- [L] (tech) PRD-AI-E2-S4: AC chấm tự động S4 không định nghĩa hành vi khi nhân viên nộp bài sau deadline hoặc muốn làm lại  · fix: Thêm điều kiện biên vào AC3: 'Hệ thống ghi nhận lần làm bài đầu tiên là kết quả chính thức; nếu bài đã đóng (hết hạn do người tạo thiết lập) thì không cho nộp thêm và hiển thị thông báo rõ. Người quản lý xem được tất cả lần thử nếu có nhiều lần.' Nếu PO không muốn cho làm lại, ghi rõ điều đó để tránh xây logic thừa.
- [L] (tech) PRD-HR-E2-S1: Design note vẫn tham chiếu PRD-FINANCE-E3 (đã loại) làm nơi cấu hình hoa hồng  · fix: Sửa design note: xoá tham chiếu PRD-FINANCE-E3 và ghi rõ quyết định: 'Thưởng hoa hồng theo doanh thu không có trong v1 (phân hệ SALES/FINANCE đã loại — DEC-45). Nếu tổ chức cần thưởng thêm ngoài KPI chuẩn, ORG_ADMIN điều chỉnh thủ công trên bảng lương nháp với ghi chú lý do (PRD-HR-E2-S2 AC2).'
- [L] (tech) PRD-HR-E2-S1: Ghi chú thiết kế HR-E2-S1 vẫn tham chiếu 'PRD-FINANCE-E3' (đã loại) là nơi cấu hình hoa hồng — chưa có AC cập nhật  · fix: Cập nhật ghi chú thiết kế: thay 'cấu hình riêng tại PRD-FINANCE-E3' bằng 'cấu hình hoa hồng nằm ngoài phạm vi v1 (PRD-FINANCE đã loại — DEC-45); nếu cần hoa hồng thì thêm AC riêng vào PRD-HR-E2 hoặc tạo story mới.' Đây là ghi chú thiết kế nên không cần re-approve nhưng cần được sửa để tránh nhầm lẫn.
- [L] (tech) PRD-PLATFORM-E1-S1: Story E1-S1 kích thước L có 8 AC bao phủ 4 mối quan tâm khác nhau — khó estimate và khó debug khi xảy ra lỗi  · fix: Tách thành 2 story nhỏ hơn: (a) E1-S1a: Đăng ký, OTP, provisioning multi-tenant nguyên tử, anti-abuse — core signup (AC 1,2,5,8); (b) E1-S1b: Trial lifecycle — countdown, gia hạn điều kiện, khoá ghi (AC 3,4,6,7). Cả hai đều Small/M, ước lượng và test độc lập được. DEC-55 gia hạn điều kiện là tính năng thêm không nên chặn ship phần signup core.
- [L] (tech) PRD-RESOURCE-E1-S2: Story xem kanban tài liệu (S2) ghép 3 tính năng độc lập vào một story M — khó tách sprint nếu cần cắt scope  · fix: Ghi nhận trong spec rằng ba yêu cầu này đến từ DEC-56 (chỉ đạo trực tiếp PO) và không tách được vì PO đã xác nhận cả ba là must. Hoặc nếu muốn giữ tính negotiable: tách S2 thành S2a (tag + trạng thái tài liệu, size S) và S2b (kanban kéo-thả + theme, size M) — PO cắt S2b nếu cần mà không mất tag.
- [L] (tech) PRD-SUPPORT-E1-S2: SLA phản hồi '≤ 15 phút giờ hành chính' được đánh dấu là đề xuất chưa PO chốt — không thể viết test nghiệm thu  · fix: PO chốt con số SLA trước khi story vào sprint: thay 'đề xuất' bằng giá trị xác định, ví dụ '≤ 15 phút'. Đây là thông số có thể đo được bằng timestamp phiên — thêm AC: 'Hệ thống ghi timestamp phản hồi đầu tiên mỗi phiên; dashboard PLATFORM_ADMIN hiển thị tỉ lệ phiên đạt SLA theo tuần'.
- [L] (tech) PRD-TASK-E4-S5: E4-S5 tuyên bố "tái dùng hạ tầng lập lịch của E1-S3" nhưng hai use case có điều kiện timeout/retry khác nhau chưa được reconcile  · fix: Làm rõ trong ghi chú thiết kế E4-S5: "Tái dùng PATTERN (idempotent job per period) — không nhất thiết cùng code module; nếu đội build chọn share job runner thì E1-S3 phải hỗ trợ retry policy cấu hình được per-job-type." Ghi rõ E4-S5 `depends_on: [PRD-TASK-E1-S3]` trong frontmatter thay vì chỉ trong ghi chú.