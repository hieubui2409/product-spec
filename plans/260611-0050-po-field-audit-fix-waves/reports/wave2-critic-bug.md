## PHÁN QUYẾT

### [BUG-F01] → CONFIRMED
- Kiểm chứng: Mở `docs/product/brd.md` sandbox — dòng 6 `status: approved`, dòng 16/20/24 `metric:` (số ít, giá trị prose); diff với PO-repo gốc: IDENTICAL (không bị lens sửa). Spec KIT `frontmatter-and-id-spec.md:89` đúng nguyên văn `metrics: [brands-onboarded]  # required, ≥1 metric slug`; spec CŨ của PO (`.claude-po-original/.../frontmatter-and-id-spec.md:87`) cũng yêu cầu `metrics:` số nhiều → "skill cũ viết sai schema của chính nó" đúng. `spec_graph.py:176` = `"metrics": goal.get("metrics") or [],`; block `goal_without_metric` tại `check_consistency.py:196-203` (literal ở dòng 201). Chạy lại `strict_gate.py --root .` → exit **2**, output `BLOCKED on errors` với đúng 3 dòng `goal_without_metric · BRD-G1/G2/G3`. `grep -rn goal_without_metric .claude-po-original/skills/product-spec/scripts/` → 0 match. `migrate_multidim_fields.py`: 0 match "metric"; bảng placeholder chỉ có risks/target_date/depends_on/competitive_parity (+ competitors cho brd). Chạy lại `record_outcome.py --append-alloc --goal BRD-G1 --metric "số hub" --target 50 --actual 25 --measured-on 2026-06-10` → JSON đúng nguyên văn `"metric 'số hub' is not in BRD-G1's metrics []; fix the slug or pass --force to record anyway", "written": false` (exit 1). Severity High xứng đáng: upgrade làm BRD approved đỏ 3 error + gate chặn + learn tê liệt, không đường migrate.

### [BUG-F02] → CONFIRMED
- Kiểm chứng: Chạy lại `status.py --root .` → exit 0, stdout **1.086.985 bytes** (khớp tuyệt đối); đếm signals: `Counter({'fence_breach': 2258, 'validate_no_marker': 1})` — khớp tuyệt đối; 1.913 signal có subject thuộc `.claude/` (khớp số lens nêu; sample subject `.claude-po-original/agents/memory-harvester.md ... touched outside the spec boundary`). `check_fence.py:36` đúng `FENCE_PREFIX = "docs/product/"`; `memory_gap.py` `_fence_signals` map 1-1 (out.append tại dòng 155, đúng vùng 151-160, không cap); `status.py:194` nhúng nguyên mảng — xác nhận bằng grep. Porcelain sandbox hôm nay 2.274 (lens đo 2.265 — chênh 9 do chính lens tạo thêm visuals/exports sau khi đo; file mới nằm trong fence nên fence_breach vẫn đúng 2.258). Ghi chú thêm (không đổi phán quyết): PO-repo THẬT hiện sạch (`git -C Cleanmatic-ERP status --porcelain -uall | wc -l` = 0, `.claude/` đã tracked 345 file) → flood chỉ nổ ở trạng thái sau-cài/đang-nâng-cấp-trước-commit hoặc cây bẩn — đúng như chính lens tự khoanh là "kịch bản"; cơ chế không-cap là thật và docstring status.py còn tự hứa "never an over-report".

### [BUG-F03] → CONFIRMED
- Kiểm chứng: Chạy lại `critique_scan.py --root . --scope PRD-SALES --lang vi` → **699.415 bytes** (khớp tuyệt đối); đo trên output: `source_files` 148 key / **422.111 chars**, target_ids 25, **123 key không thuộc target_ids** — cả 3 số khớp tuyệt đối. Scope=all → **1.236.477 bytes** (khớp tuyệt đối); digest 147 entry, `Counter({'full': 145, 'struct': 2})`, tổng digest ~516.204 chars ≈ 515.910 lens nêu (lệch do cách đếm serialization). `critique_signals.py:28` đúng `def _source_files`, thân hàm loop mọi node không lọc scope; `critique_bundle.py:117` đúng `"source_files": _source_files(root, graph),`; `workflow-critique.md:122` đúng "Spawn the selected lenses concurrently via Task, each given: the bundle path". Hai bundle PRD-SALES và all có `source_files` y hệt 436.532 chars (đo JSON) — chứng minh hoàn toàn không lọc theo scope. Severity High hợp lý (4 lens × ~700KB-1.24MB).

### [BUG-F04] → CONFIRMED
- Kiểm chứng: Chạy lại `python3 -B -m pytest .claude/skills/telemetry/scripts/tests -p no:cacheprovider` tại KIT HEAD → **`FAILED ...test_declared_chains_parsed_from_routing_docs`, 1 failed, 108 passed** (test assert `len(d["declared_chains"]) >= 1`, dòng ~46). `lens_workflow_chains.py:22-25` hardcode đúng 2 path `skill-workflow-routing.md`/`skill-domain-routing.md`; `ls` cả 2 → No such file; `git -C product-spec show e52e077 --stat` → `skill-domain-routing.md | 154 ---` và `skill-workflow-routing.md | 68 -`. Test vẫn tracked (`git ls-files | grep -c test_lens_workflow_chains` = 1). CI: grep `working-directory` toàn bộ workflows → chỉ product-spec / product-spec-critique / release (+dist) — không có telemetry. Chạy trực tiếp `lens_workflow_chains.gather()` trỏ KIT → `declared: []` — dead code xác nhận sống. Path lạ `cleanmatic-skills` trong traceback tái hiện y như lens mô tả (pycache cũ), không ảnh hưởng tính thật của fail.

### [BUG-F05] → CONFIRMED
- Kiểm chứng: `grep -c '^id:' docs/product/PRODUCT.md` → 0 (frontmatter mở đầu `product: Cleanmatic ERP`); template cũ lẫn mới đều có `id: PRODUCT` (dòng 9 cả hai). Chạy lại check → finding thật nguyên văn `"<missing-id> declares 7 personas; soft cap is 4..."`. `spec_graph.py:571-576` đúng block comment ID_SENTINELS ("...so the PO never sees an internal sentinel..."); `ID_PATTERN_BY_TYPE` (~597-602) chỉ có goal/prd/epic/story — không phủ product/vision, và run thật xác nhận không có finding missing-id nào (12 findings chỉ gồm 3 loại). Bundle scope=all: `target_ids[0] == '<missing-id>'` và `'<missing-id>' in source_files` → True, kiểm trực tiếp trên output. Chi tiết nhỏ: f-string nhúng `{n['id']}` thực ở `check_consistency.py:177` (lens ghi 176 — cùng statement, lệch 1 dòng, không sai bản chất).

### [BUG-F06] → ADJUSTED (mở rộng phạm vi — finding đúng nhưng nói nhẹ hơn thực tế)
- Kiểm chứng: Chạy lại `parse_critique_report.py --report docs/product/critique/20260609T233523Z-all.md` → `findings: []`, `lens_findings_hash: null`, `cache_present: false`, note đúng nguyên văn "lens-cache absent — fall back to a manual prose walk...". Frontmatter report: `bundle_version: 2` (d2), `critique_scope: all` (d3), `level: 5` — không có `lens_findings_hash`. `.memory/critique-lens-cache/` đúng 3 hash: `8b0de699ccd73cab / d0cfd9cefda771be / d22c65fa93962a47`. Đối chứng PRD-CARE: 22 findings, cache_present True, freshness True. 2 blocker có thật trong report (dòng 18 North Star 30vs50; dòng 20 Zalo-switch 5/10 PRD).
- Lý do ADJUSTED: tôi parse **toàn bộ 15 report** critique của PO → chỉ 3 report parse được (CARE 22, FINANCE 22, TASK-121803Z 16); **12/15 trả 0 findings**, kể cả các report MỚI HƠN bản có cache cùng ngày 06-09 (TASK 125531Z/131658Z, COMM ×4, PAYMENT, REPORT — đều không có `lens_findings_hash`). Mất đường deterministic là TRẠNG THÁI MẶC ĐỊNH của 80% corpus critique PO, không phải ngoại lệ của riêng "báo cáo quan trọng nhất". Severity Medium giữ nguyên; diễn giải cần nói rõ blast radius.

### [BUG-F07] → CONFIRMED
- Kiểm chứng: File roadmap lens render (`roadmap-20260610T042203Z.html`) = 2.623.504 bytes ≈ 2.618.333 chars lens nêu (lệch bytes/chars do UTF-8 đa byte); block `<script>` lớn nhất đo được 2.571.817 chars (lens: 2.571.831 — nhiễu cách đo, cùng độ lớn). `du`: sandbox visuals 11MB, PO gốc 6,7MB; PO gốc có sẵn đúng 2 bản `roadmap-20260603T230937Z/231342Z.html` 2,5MB mỗi bản. Exports: 2 file `all-20260610T034005Z-af7d5c83.md` và `all-20260610T042805Z-af7d5c83.md` cùng hash, cùng 383.8K (đây là sản phẩm 2 lần chạy của chính lens — demo hành vi hợp lệ). Grep toàn bộ scripts: không có prune/retention nào. Ghi chú: `render_export.py:12-16` docstring tự ghi nhận "each invocation writes a fresh file — re-exporting unchanged content accumulates one timestamped file per run (the hash is the dedup key, not the filename)" → tích tụ là hành vi CÓ CHỦ ĐÍCH được document, nên đây là design-gap (đúng loại CLEANUP lens gán) chứ không phải lỗi vô tình — tác động PO không đổi.

### [BUG-F08] → ADJUSTED (bug thật, sai quy kết trích dẫn cam kết)
- Kiểm chứng: Tái lập đúng nguyên văn: pipe stdout `check_consistency.py --root .` vào consumer đóng sớm → stderr traceback `File ".../check_consistency.py", line 380, in main / print(json.dumps(output, ...)) / BrokenPipeError: [Errno 32] Broken pipe`, exit code **1**. Cỡ output thật của `check_consistency` trên data PO = **301.486 bytes** → claim "~300KB" chính xác, kịch bản `| head` hoàn toàn thực tế.
- Lý do ADJUSTED: câu `Docstring tự cam kết "never a bare traceback"` — cụm đó KHÔNG nằm trong docstring `check_consistency.py` (grep "traceback" → 0 match trong file); nó nằm ở `decision_register.py:392`, `generate_templates.py:275` (dẫn `validation-rules-spec.md:63`), `judgment_cache.py:547`. Contract bị vi phạm thật sự là `check_consistency.py:12` "Emits findings JSON ... **Always exits 0**." + validation-rules-spec.md (mục ~63, nêu đích danh check_consistency "always exit 0 with JSON on stdout") — exit 1 kèm bare traceback vi phạm cả hai. Kết luận và severity Low giữ nguyên, chỉ sửa nguồn trích.

### [BUG-F09] → CONFIRMED
- Kiểm chứng: Chạy lại `analyze_telemetry.py --lens all --format ascii` (default vi) trong sandbox → dòng 13 đúng nguyên văn `[i] VALIDATE-PROXY — not available on current data (no validate marker, no validate-script runs)` giữa output Việt. `lens_validate_proxy.py:82` đúng là chuỗi reason tiếng Anh hardcode; `telemetry_render.py:173` đúng `"val_na": "chưa có dữ liệu"` (bản dịch có sẵn nhưng không dùng) và `:182` đúng `"a_val_na": "VALIDATE-PROXY — {reason}"` format thẳng reason từ lens. Cam kết "narrate in Vietnamese (default)" có thật ở telemetry SKILL.md:44.

### [BUG-F10] → CONFIRMED
- Kiểm chứng: Chạy lại `assemble_audit_trail.py --root . --format ascii --lang vi | awk max-length` → **111 dòng, dòng dài nhất 368 ký tự** — khớp tuyệt đối. Trích "ASCII is downgraded, never removed" có thật tại product-spec SKILL.md:55. Severity Low + UX-PO hợp lý.

## BỎ SÓT (≤3)

### [BUG-M1] Goal thiếu `status` (spec ghi "required") không bị check nào bắt; `moscow` trên goal bị drop im lặng khỏi graph
- Mức độ: Medium
- Loại: CORRECTNESS
- Bằng chứng:
  - Spec, CÙNG block lens đã trích cho F01: `references/frontmatter-and-id-spec.md:90` — `status: draft  # required, closed enum` (mỗi goal entry); dòng 24 template brd: "Each entry MUST carry id/title/metrics/status/owner".
  - Data PO: goals trong `brd.md` chỉ có `id/title/metric/moscow` (moscow tại brd.md:17/21/25) — `status` duy nhất là file-level dòng 6; cả 3 goal không có `status` riêng.
  - Chạy thật `check_consistency.py --root .` → đúng 12 findings, `Counter({'persona_cap_exceeded': 7, 'goal_without_metric': 3, 'risk_high_ratio': 2})` — **không một finding nào** về goal thiếu status. Nguyên nhân: vòng ENUMS `check_consistency.py:141-143` (`v = n.get(field); if v is None: continue`) bỏ qua field vắng mặt; `_status_inconsistency._flag` đòi `cs >= 0 and ps >= 0` → goal status None thành -1, mọi so sánh bị skip — `prds/sales.md:5` `status: approved` + dòng 13 `brd_goals: [BRD-G1, BRD-G3]` trỏ vào goal vô-status mà câm lặng.
  - `spec_graph.py:170-182` `_node_from_goal` chỉ copy id/type/title/status/metrics/owner → `moscow: must/should` của PO trên goal **biến mất khỏi graph** (mọi view/filter moscow không bao giờ thấy), không unknown-key lint nào cảnh báo.
- Phân tích: đúng vết xe F01 — spec tuyên "required" nhưng validator chỉ enforce `metrics` (check mới thêm) còn `status` thì im lặng; dữ liệu PO lệch schema cả hai chiều (thiếu key required, thừa key ngoài schema) và kit nuốt sạch. Nếu phiên bản sau thêm check status như đã thêm `goal_without_metric`, BRD approved của PO lại đỏ thêm 3 error nữa mà vẫn không có đường migrate. Lens đứng ngay trên block spec này (trích dòng 89) mà bỏ qua dòng 90.
- Đề xuất: (a) thêm `goal_without_status` (warn trước, error sau chu kỳ migrate) song song `goal_without_metric`; (b) lint key lạ trong goal entry — bắt generic cả lớp lỗi `metric:`-vs-`metrics:` thay vì vá từng field; (c) đưa `status` (và mapping `moscow`) vào cùng đường migrate đề xuất ở F01.

## TÓM TẮT CHẤT LƯỢNG LENS

Độ tin cậy rất cao: mọi con số tôi tái đo đều khớp tuyệt đối (1.086.985 bytes / 2.258 fence_breach / 699.415 & 1.236.477 bytes / 148 key–422.111 chars–123 off-target / 22 findings PRD-CARE / 1 failed-108 passed / 111 dòng-368 ký tự / 301.486 bytes), trích dẫn file:dòng chính xác trong biên ±1, tự khai báo trung thực nguồn số liệu sandbox và giới hạn không chạy được. Kết quả: 8/10 CONFIRMED, 2 ADJUSTED (F06 nói nhẹ hơn thực tế — 12/15 report mất đường parse chứ không riêng report all; F08 gán nhầm câu cam kết "never a bare traceback" vào docstring sai file — contract vi phạm vẫn thật qua "Always exits 0"), 0 REFUTED. Các claim "đã kiểm và sạch" spot-check đều đúng (product-spec 658 passed, critique 172 passed, traceability 0 findings, 147 nodes/0 parse_errors). Thiên lệch duy nhất đáng ghi: nghiêng về số đo ấn tượng ở F02 (PO-repo thật hiện sạch nên flood chỉ nổ ở trạng thái install/cây bẩn — lens có khoanh là "kịch bản" nhưng không kiểm trạng thái repo thật), và một điểm mù ngay trong block schema mình đang trích (BUG-M1).

Status: DONE
Summary: Phản biện xong 10 finding lens BUG bằng tái chạy toàn bộ lệnh trong sandbox: 8 CONFIRMED, 2 ADJUSTED (F06 mở rộng phạm vi 12/15 report; F08 sửa quy kết trích dẫn contract), 0 REFUTED; bổ sung 1 finding bỏ sót BUG-M1 (goal thiếu `status` required + `moscow` bị drop im lặng) với bằng chứng đầy đủ.