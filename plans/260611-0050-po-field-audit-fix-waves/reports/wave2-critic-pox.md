## PHÁN QUYẾT

### [POX-F01] → ADJUSTED
- Kiểm chứng: Mở `python-package.yml` — đúng `:30` `python -m pip install flake8 pytest`, `:35` `flake8 . --count --select=E9,F63,F7,F82`, `:40` `pytest`; `git -C Cleanmatic-ERP log` → `9df10cf | hunghenei | 2026-06-10 07:58:11 +0700 | Add GitHub Actions workflow...`. `find … -name '*.py' | wc -l` → 125, ngoài `.claude/` → **0**. `grep -in 'github' pack.manifest.yaml` → exit 1 (0 match); workflows tracked của KIT chỉ là 6 file CI nội bộ (`product-spec-ci.yml`…). Thêm: PO-repo có 53 file `test_*.py`/`conftest.py` trong `.claude/` → pytest sẽ chạy test của kit không có venv/deps — "có thể đỏ CI vì lý do PO không hiểu" là đúng.
- Lý do (ADJUSTED — diễn giải, giữ High): chi tiết "commit từ tài khoản thứ hai" SAI — cả 2 commit cùng một account `hunghenei` (`91942101+hunghenei@users.noreply` qua GitHub web-flow vs `hunghenei@gmail.com` local). Tuy nhiên điều này lại **củng cố** cách đọc chính: committer `GitHub (web flow)` = PO bấm nút suggested-workflow ngay trên github.com. Bằng chứng "làm việc nhóm" vẫn còn `.session.md:24` "Product Owner: Nhóm (3+ người)". Phần còn lại đứng vững.

### [POX-F02] → CONFIRMED
- Kiểm chứng: `PRODUCT.md:51` "| Kết nối & Thanh toán nâng cao | PRD-PAYMENT | next |", `:54` "| Báo cáo & Phân tích | PRD-REPORT | later |" — nguyên văn đúng. `prds/payment.md:9` `horizon: now` (status approved 06-10), `prds/report.md:9` `horizon: now` (approved 06-10); thực tế **cả 10 PRD** đều `horizon: now`. `change-log.md:18` "Validate đầy đủ: 0 lỗi…" đúng vị trí. `grep -n 'PRODUCT'` trên cả 3 script check của HEAD → 0 match (re-run, exit 1). Suy luận và severity High hợp lý: bảng nền PO đọc đầu tiên lệch 2 PRD approved mà validate khẳng định sạch.

### [POX-F03] → CONFIRMED
- Kiểm chứng: `.session.md:4` `last_updated: 2026-06-02`, `:30` "- [x] BRD-G1 metric: 30 hub (không phải 50)", `:51` "Tất cả artifacts hiện ở trạng thái `draft`" — đúng từng dòng. `brd.md:16` metric "lên ≥50 … DEC-43, supersede mốc 30 trước". Đếm lại bằng python: **134 approved / 10 draft** — khớp con số báo cáo. `decisions.md:474` "PO từng chốt 30 ở .session.md 2026-06-02". HEAD: `check_consistency.py:282-311` chỉ check `.gitignore` không loại `.session.md` — không hề có staleness check. Củng cố thêm: `.session.md:33` "- [x] Cổng thanh toán: cả Sepay VÀ Casso từ MVP" ngược thẳng DEC-4 ("PO đảo quyết định (giả định #1 trước đó: Sepay+Casso cả hai)… MVP chỉ Sepay") — fact bị supersede **thứ tư** trong cùng file, lens còn chưa kể.

### [POX-F04] → CONFIRMED
- Kiểm chứng: `ls visuals/` — đúng 10 file đều 06-03 (board×3 23:09:37/23:11:45/23:13:41 cùng size 275.9K, explorer×3, roadmap×2, dashboard×1, sales-tree tên khác format); `md5sum` → 10 hash khác nhau (khớp claim "không byte-identical"). `grep -oE 'PRD-…-S…' board-…231341Z.html | sort -u | wc -l` → **56** vs 102 story. `stories/PRD-CARE-E1-S4.md:44` đúng nguyên văn "xem bằng Explorer view hoặc đọc frontmatter" (story approved 06-09). HEAD `grep 'latest'` trên render_html/board/explorer/export → 0. Chi tiết phụ "3 subgraph" tôi đếm ra 5 (`grep -o subgraph | wc -l`) — sai số nhỏ trong câu phụ, không ảnh hưởng kết luận. 10 file visuals còn bị git track → bản stale đẩy lên GitHub cho cả đội. High đúng mức.

### [POX-F05] → ADJUSTED
- Kiểm chứng: cả 3 vị trí trích đúng (`PRD-CARE-E1-S4.md:44`, `PRD-PAYMENT-E1-S3.md:36`, `PRD-INVENTORY-E3-S1.md:40` — cùng câu "nguồn chuẩn ở `acceptance_criteria`… Đã gỡ bản sao ở thân"); AC frontmatter CARE-E1-S4 dòng 21-31 đúng là 10 mục dài, escape quote + `[[ref]]` dày đặc. Đếm: **102/102 story** mang note này. `find docs -type d -name '*export*'` → 0.
- Lý do (ADJUSTED — diễn giải, giữ Medium): (a) "Repo có 2 người commit" sai — chỉ 1 account hunghenei (xem F01); kênh đọc GitHub web vẫn đứng vì PO là nhóm 3+ người. (b) "HEAD có sẵn workflow-export.md — giải MỘT PHẦN" thiếu một nửa sự thật: bundle 1.1.0 **PO đang cài cũng đã có** `references/workflow-export.md` (6.5K) + `render_export.py` (13.2K) — tức không phải tính năng sau-1.1.0; điều này làm luận điểm discoverability **mạnh hơn** (PO có sẵn công cụ suốt 8 ngày mà không hề dùng).

### [POX-F06] → CONFIRMED
- Kiểm chứng: `grep -n 'CSKH' vision.md` → đúng duy nhất dòng 13 (frontmatter `personas:`); body chỉ có 6 heading `###` chân dung (CUST:43, HUB:59, DELIVERY:75, PROCESS:90, ZONE_MGR:105, SYS_ADMIN:~118) — không có CSKH. `decisions.md:142` DEC-13 "Thêm persona CSKH vào PRODUCT.md/VISION". `PRODUCT.md:30` có hàng CSKH. `vision.md:6-7` `status: approved` + 2026-06-10. `.session.md:13` "6 chân dung người dùng". Mọi trích dẫn chính xác; Medium hợp lý.

### [POX-F07] → ADJUSTED
- Kiểm chứng: `critique/20260609T125531Z-PRD-TASK.md` frontmatter `pass: 2`, `prior_report: …121803…`; nguyên văn "Build `--fresh` (cache=none) vì các sửa nằm ở AC/frontmatter mà `body_hash` không bắt." đúng từng chữ. Tally: TASK pass-1 "blocker 4 · major 6 · minor 4" → pass-2 "blocker 0 · major 7 · minor 9" (size 19.7K→13.5K→9.4K khớp `ls`); COMM pass-4 "converged — đề nghị đóng sổ + duyệt", "blocker 3→0→0→0". 13 báo cáo ngày 06-09 — đếm đúng. "Tám phát hiện … đều đã đóng" nguyên văn có thật. HEAD `apply_critique_progress.py` docstring "skips already-resolved fingerprints… never re-litigates" đúng (file nằm ở `product-spec/scripts/`, không phải skill critique). CHANGELOG fingerprint thực ở dòng **177** (báo cáo ghi 176 — lệch 1 dòng, không trọng yếu), mục [1.4.0] — 2026-06-06, sau 1.1.0 của PO.
- Lý do (ADJUSTED — **nâng severity Medium → High**): câu hỏi lens bỏ ngỏ ("HEAD đã vá chưa?") tôi đã xác minh: **CHƯA**. `spec_graph.py:124` hash CHỈ body (`hashlib.sha256(body.encode…)`); `spec_graph.py:466` `CHANGED_FIELDS = ("status","scope","moscow","horizon","size","body_hash")` — **không có `acceptance_criteria`**; `critique_common.py:89` `_scoped_body_hashes` lấy từ chính body_hash đó. Hệ quả ở HEAD: sửa AC-only → map provenance **không đổi** → fast-path "spec unchanged" có thể **reuse kết quả cũ sai** (`critique_provenance.py:141-164`). Đây không còn là "đắt" (phải --fresh) mà là rủi ro đúng-sai của cache — nặng hơn lens đánh giá.

### [POX-F08] → CONFIRMED
- Kiểm chứng: `cat .memory/critique-state.json` → đúng 3 entry (CARE/FINANCE/TASK); TASK `last_ts: 2026-06-09T12:18:03`, `report: …121803Z` (pass 1) dù pass 2/3 tồn tại; COMM/PAYMENT/REPORT/all vắng. Lệch blocker_count xác nhận cả hai: state TASK=3 vs header report "blocker 4"; state CARE=5 vs "blocker 4 · major 11 · minor 3". `critique-findings-index.json` cũng dừng ở 3 report_ts (025434/084007/121803 — 8+8+4 entry). Nuance không làm đổ finding: `last_critique.json` VẪN được ghi lúc 23:37 bởi run scope=all → "ngừng ghi" đúng với critique-state + findings-index, không phải toàn bộ tầng memory. CHANGELOG HEAD dòng 316-322 đúng như trích ("critique-state.json (per-scope provenance fast-path marker)"). Medium hợp lý.

### [POX-F09] → CONFIRMED
- Kiểm chứng: `MANIFEST.json` → `"1.1.0"`, built `2026-06-03T05:39:31+00:00`, source `1c5113f…`. KIT tag axis đủ 15 tag claude-pack-v0.1.0…1.4.0 → product-spec-v2.0.0…2.3.0. `grep -cE 'update|upgrade|version…' INSTALL.md` → **0** (heading chỉ có Verify/Extract/Install/What gets installed/Troubleshooting/Uninstall). PO cài skill `spec-critique`; KIT là `product-spec-critique`. Backup brd.md không có `type:` (grep → 0) vs hiện tại `type: brd` — tiền lệ migration có thật (`migrate_multidim_fields.py` có cả trong bundle 1.1.0 của PO lẫn HEAD). Release skill HEAD: grep 'upgrade' chỉ ra ghi chú hook "upgrade-safe" — không có tài liệu upgrade cho người nhận. Medium FEATURE-GAP đúng.

### [POX-F10] → CONFIRMED
- Kiểm chứng: re-run `diff -rq` → only-in-bak **0** / only-in-current **72** / differ **86** — khớp tuyệt đối. `migrate_multidim_fields.py:21` "copying the original to `<file>.bak`" nguyên văn; `install.sh:258` `cp -f "$DST" "$DST.bak.$BACKUP_TS"` đúng dòng; thư mục backup không có README/restore. Củng cố thêm lens chưa thấy: `.gitignore:26-27` có `*.bak` và `docs/product/stories.bak-*/` nhưng KHÔNG khớp `docs/product.bak-*/` → **88 file backup đang bị git track** và đẩy lên GitHub — "không ai dám xoá" giờ còn kèm "đã phát tán". Low hợp lý (có thể tranh luận Medium nhờ chi tiết git-track, nhưng giữ Low chấp nhận được vì không mất dữ liệu).

### [POX-F11] → CONFIRMED
- Kiểm chứng: `decisions.md` = 33.209 bytes (~32.4K), đúng **44** block `^## DEC`; pseudo-frontmatter giữa file đúng như mô tả (dòng 3-8: `---\nid: DEC-1\nstatus…\naffects…\n---`); DEC-44 `affects:` ở dòng 480 đúng nguyên văn. Dashboard: `grep -c 'DEC-'` → **0**. HEAD `decision_register.py` tồn tại (16.601 bytes ≈ 16K, docstring "The register kills re-litigation" nguyên văn). Ghi chú nhỏ: bundle 1.1.0 của PO **cũng đã có** `decision_register.py` (13.6K) — "HEAD giải một phần ở tầng dữ liệu" hơi hào phóng cho HEAD, tầng dữ liệu có từ đời PO đang dùng; tầng trình bày thì đúng là chưa đâu có. Không đổi phán quyết.

## BỎ SÓT (≤3)

### [POX-M1] PRODUCT.md không có `id:` — thành node `<missing-id>` trong graph và lọt vào mọi provenance map critique
- Mức độ: Medium
- Loại: CONSISTENCY
- Bằng chứng: frontmatter `PRODUCT.md` của PO chỉ có `product/lang/version/created` — grep `^id:` → 0. HEAD `spec_graph.py:566` `return v or "<missing-id>"` (docstring :562 "Absent → `<missing-id>`"; `ID_SENTINELS` :576). PO `.memory/last_critique.json` chứa key thật `"<missing-id>": "ee56309f"`. Chạy `build_graph` (code HEAD trong SANDBOX, data PO) → `147 nodes`, in ra: `WEIRD NODE: '<missing-id>' | type: product | file: PRODUCT.md`.
- Phân tích: Tài liệu nền duy nhất PO mở đầu tiên không tham chiếu được bằng ID — nguyên tắc "DRY — cross-reference by ID" của chính kit bất khả thi với PRODUCT.md, và đây chính là rào kỹ thuật khiến rule consistency PRODUCT-map↔PRD (đề xuất F02) không viết nổi theo ID. Sentinel `<missing-id>` còn rò vào frontmatter báo cáo critique mà PO đọc. Lỗi xuất phát từ template kit (không cấp id cho type `product`), không phải PO.
- Đề xuất: template PRODUCT.md có `id: PRODUCT` + migration bù id cho bản đã cài; hoặc spec_graph fallback id theo type (`product` → `PRODUCT`) thay vì sentinel.

### [POX-M2] Ba câu hỏi kinh doanh "Vẫn còn mở" từ 06-02 không có nhà — story `must` đã approved vẫn ghi "cần PO xác định trước khi viết code", kit không nhắc lại bao giờ
- Mức độ: Medium
- Loại: FEATURE-GAP
- Bằng chứng: `.session.md:36-39` "### Vẫn còn mở: Ngưỡng điểm để lên từng hạng… / Tỉ lệ quy đổi điểm… / Ưu đãi cụ thể theo từng hạng…". `stories/PRD-SALES-E2-S3.md:6` `status: approved`, `:12` `moscow: must`, `:47` "Ngưỡng điểm từng hạng cần PO xác định trước khi viết code — đây là thông số kinh doanh quan trọng…". `grep 'Ngưỡng điểm|quy đổi điểm|hạng thành viên' decisions.md` → **0** trong 44 DEC. `grep 'còn mở|open question|parking'` trên `status.py` + `workflow-status.md` (cả bundle 1.1.0 của PO lẫn HEAD) → 0 — không workflow nào theo dõi câu hỏi mở.
- Phân tích: Đúng địa hạt POX mà lens tự đặt (F03 chỉ soi fact bị supersede, bỏ qua nửa kia của cùng file): câu hỏi kinh doanh PO tự ghi "còn mở" 8 ngày trước giờ chỉ sống trong một file stale không ai đọc lại; trong khi artifact mang chính tham số treo đó đã được approve ở mức `must`. Đội build sẽ đụng "PO chưa chốt" ngay sprint đầu — kit có đủ dữ liệu để nhắc (`--status`, validate, approve-gate) nhưng im lặng.
- Đề xuất: (a) "Câu hỏi mở" thành first-class (mục riêng trong `--status` + sổ open-questions, không sống trong `.session.md`); (b) khi `--approve` một artifact có marker dạng "cần PO xác định/chưa chốt", cảnh báo và đề nghị ghi DEC hoặc treo follow-up.

### [POX-M3] BRD không có node body_hash trong provenance — đợt sửa BRD ngày 06-10 (DEC-43) vô hình với toàn bộ lớp drift/fast-path của critique
- Mức độ: Medium
- Loại: ARCHITECTURE
- Bằng chứng: PO `.memory/last_critique.json` map body_hash = 144 key, kiểm bằng python: `has BRD: False`. SANDBOX build_graph (code HEAD): `BRD node: ABSENT` (147 node chỉ gồm `<missing-id>`+VISION+3 goal+10 PRD+30 epic+102 story; goal node `body_hash: None` — `spec_graph.py:180-182` "body_hash is None … never a body-change signal" — bị `_HASH_SENTINELS` skip khỏi map, `critique_common.py:84-86`). Thực tế PO: 06-10 sửa body brd.md (metric ≥50, `brd.md:16`; "BRD v0.5" — `change-log.md` mục 06-10) — thay đổi này không xuất hiện trong bất kỳ body_hash map nào.
- Phân tích: Đây là nhánh độc lập với F07 (F07 = frontmatter mù; M3 = nguyên một artifact mù): kể cả khi vá hash phủ AC theo đề xuất F07, BRD vẫn nằm ngoài provenance vì không có node. Critique drift/fast-path sẽ kết luận "spec unchanged" sau một lần sửa mục tiêu kinh doanh cấp cao nhất — đúng loại thay đổi đáng re-critique nhất.
- Đề xuất: thêm node BRD (document-level, giữ goals làm node con) hoặc đưa body_hash của brd.md vào map provenance; thêm test chốt "mọi file artifact có mặt trong body_hash map".

## TÓM TẮT CHẤT LƯỢNG LENS
Độ tin cậy tổng thể CAO: 9/11 CONFIRMED, 2 ADJUSTED không đổ finding nào; mọi trích dẫn path:line tôi re-run đều đúng nguyên văn (kể cả các con số khó như diff 0/72/86, tally blocker, 56/102 story id, 134/10 status), và lens trung thực tự bác giả thuyết "byte-identical" bằng md5sum. Hai vết trượt thực tế: đọc nhầm 2 email của cùng account hunghenei thành "2 người/tài khoản thứ hai" (lặp ở F01+F05), và vài con số phụ lệch nhẹ (3 vs 5 subgraph, CHANGELOG 176 vs 177). Thiên lệch nhẹ: hào phóng với HEAD — gán cho HEAD những thứ bundle 1.1.0 của PO đã có (workflow-export, decision_register), và **đánh giá non F07**: tôi xác minh tại HEAD hash vẫn chỉ phủ body, `CHANGED_FIELDS` không có `acceptance_criteria` → AC-edit vô hình với fast-path là rủi ro reuse-sai đang sống, đáng High. Bảng pain→cơ hội nhìn chung bám đúng bằng chứng và giữ đúng ranh giới tài-liệu-sản-phẩm.
