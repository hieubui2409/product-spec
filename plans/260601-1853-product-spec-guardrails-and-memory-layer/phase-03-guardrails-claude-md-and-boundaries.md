---
phase: 3
title: "guardrails-claude-md-and-boundaries"
status: done
priority: P1
effort: "4h"
dependencies: [2]
---

# Phase 3: guardrails-claude-md-and-boundaries

## Overview
Add the missing conversation/operating guardrails. Short always-loaded anchors in repo-root `CLAUDE.md`; full detail in a new on-demand `references/guardrails-and-boundaries.md` (golden rule: load only when needed). Depends on P2 (needs final reference filenames for the pointer table).

## Requirements
- Functional: the skill (a) redirects a PO when the conversation drifts off product-scope; (b) refuses code-writing + redirects to stories+AC, incl. the "spec dropped into a code repo, asked to write code" case; (c) has an anti-rationalization table; (d) frames no-silent-reversal + never-assume as named GATEs; (e) states principles apply every turn (flagged or not); (f) surfaces residual risk when a confirmation is skipped.
- Non-functional: PO-facing, non-technical vocabulary; CLAUDE.md additions kept SHORT (auto-loaded every turn = context cost); detail lives in the on-demand reference.

## Architecture
- **repo-root `CLAUDE.md`** (product-spec section, the section ABOVE the `<!-- BEGIN: cleanmatic:claude-pack` marker — safe to edit, project-owned, not ck-overwritten):
  - Short anchors: off-topic-redirect rule, no-code-redirect rule, "principles apply every turn", `<GATE-NO-SILENT-REVERSAL>` + `<GATE-NEVER-ASSUME>` named blocks (refusal + override + escalation), override-surface-risk one-liner.
  - Anti-Rationalization table (compact) — PO-spec shortcut-thoughts → rebuttals.
  - Update **Workflow Pointers** table: rename split refs (from P2) + add `references/guardrails-and-boundaries.md` with note **"load regardless of flag"**.
- **`references/guardrails-and-boundaries.md`** (new): full detail — off-topic redirect script/examples, no-code redirect + code-repo-context recognition + redirect template, philosophy-anchor recap, protocol-adherence recap (scripts-first/frontmatter-source/DRY/no-silent-reversal), override-surface-risk pattern.

## Related Code Files
- Modify: repo-root `CLAUDE.md` (product-spec section ABOVE the `<!-- BEGIN: cleanmatic:claude-pack` marker ONLY; never touch the claude-pack marker block; never touch `.claude/rules/*`)
- Create: `.claude/skills/product-spec/references/guardrails-and-boundaries.md`
- Read for context: `SKILL.md`, ck pattern source `fix/SKILL.md:79-90` (anti-rationalization shape — reference only, never edit)

## Verification (structural — no pytest)
1. **(red-team RT-9 — marker-text anchor, NOT line numbers)** Inserting content shifts line numbers, so do NOT anchor to "line 281". Instead: capture `awk '/BEGIN: cleanmatic:claude-pack/,/END: cleanmatic:claude-pack/' CLAUDE.md` **before and after** the edit → assert the extracted block is byte-identical. Also assert every new edit lands strictly BEFORE the `<!-- BEGIN: cleanmatic:claude-pack` line (the product-spec section).
2. Workflow Pointers table references only existing files (post-P2 names) + includes guardrails-and-boundaries.md.
3. `references/guardrails-and-boundaries.md` reachable from the pointer table; grep proves the off-topic + no-code rules present in BOTH a short CLAUDE.md anchor AND the detailed reference (anchor→detail link resolves).
4. No engineering jargon introduced into PO-facing prose (manual read check).

## Implementation Steps
1. Draft `references/guardrails-and-boundaries.md` (full guardrail detail).
2. Add short anchors + anti-rationalization table to CLAUDE.md product-spec section.
3. Update Workflow Pointers table (split-ref renames + new guardrails ref, "load regardless of flag").
4. Run structural checks #1–#3; manual PO-vocab read (#4).

## Success Criteria
- [ ] Off-topic redirect + no-code redirect (incl. code-repo case) present (CLAUDE.md anchor + reference detail).
- [ ] Anti-rationalization table + 2 named GATEs + applies-every-turn note in CLAUDE.md.
- [ ] `cleanmatic:claude-pack` marker block (281–387) byte-unchanged.
- [ ] Pointer table valid; guardrails ref loads regardless of flag.

## Risk Assessment
- Accidentally editing the claude-pack marker block (regenerated by claude-pack) → mitigated by check #1.
- CLAUDE.md bloat (auto-loaded) → keep anchors terse, push detail to the reference.
- Parallel-safe with P4 (distinct files: CLAUDE.md+newref vs split workflow refs).
